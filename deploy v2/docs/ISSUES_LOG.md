# ISSUES_LOG — Thammen (سجلّ المشكلات)

> **ما هذا:** سجلّ الأخطاء (A-ids) + المشكلات المفتوحة + المؤجَّلات (#42) + بوابة بيانات الأنواع + قيود ISS. **المخاطر** في `RISK_REGISTER.md`/`RISK_SUMMARY.md`؛ **العمل الأمامي** في `CLAUDE.md` «⚡ LIVE NOW». عند التعارض: **`/api/health` الحيّ + Session_Log الأحدث يفوزان** (#58).
> **المُعرِّفات ثابتة** — لا ترقيم جديد، لا إعادة استخدام.
> **آخر تحديث:** 2026-06-13 · **الحيّ:** b29 / Heroku **v200** / git `20b792f` (master==origin).
> **بُني** بإعادة استخراج مُتحقَّقة عدائيّاً من Session_Log §20.45–§20.61 (كل «مُغلق» أُكِّد بدليل سبرنت). يَخلُف لقطة 2026-06-09 المئوية.

-----

## 1 — المفتوح الآن (Open)

| # | الشدّة | السطر الواحد | محجوز على |
|---|---|---|---|
| **A5** | 🟡 Medium | `asset_type: unknown` يظهر بلا شرح للمستخدم — **الوحيد المفتوح من فئة الأخطاء** | أولويّة backlog (خارج المسار الحرج؛ سبرنت صغير قابل للتنفيذ الآن) |

**لا أخطاء Critical/High مفتوحة.** A6/A8 (High) مُغلقان؛ A7/A11/A12/A13/A14/A15/A16 (Medium) مُغلقة.

-----

## 2 — كتالوج الأخطاء (A1–A16) — مُغلق ما لم يُذكر

| ID | الشدّة | الحالة | أُغلق بـ |
|---|---|---|---|
| A1 | Medium | ✅ مُصنِّف v2 واعٍ بالـsubtype (Branch 0) | 2.16.6 / v28 |
| A2 | Medium | ✅ `ConfigDict(extra='forbid')` — رفض الحقول المجهولة (422) | 2.16.15 / v36 |
| A3·A4·A10 | Low/Med | ✅ حزمة الصيانة (asking=0 · `/api/evaluate` يقرأ asking · rental سالب · «تزوير»→«تنظيم») | 2.16.7 / v29 |
| A5 | Medium | 🟡 **OPEN** — انظر §1 | — |
| A6 | High | ✅ قوس الكمون (3 سبرنتات: توازي factors + BFS + misroute) | 2.18.0/.1/.1.1 / v99–v101 |
| A7 | Medium | ✅ مُغلق كـnot-a-bug/by-design + وسم حالة أمين «بانتظار مراجعة مُقيِّم (المرحلة 5)» | 2.22.0a.20 / v159 |
| A8 | High | ✅ شبكة تسويات زمنيّة (size مؤجَّل R²≈0.05؛ corner BLOCKED E12) | 2.20.0 / v39 |
| A11 | Medium | ✅ Branch 0 يفحص التنظيم → علم `subtype_zoning_mismatch` (9.1% مبانٍ حكوميّة) | 2.16.14 / v35 |
| A12 | Medium | ✅ حارس E4: خلية فيلا بلا وسيط أرض → fallback إجباريّ | 2.19.1 / v38 |
| A13 | Low | ✅ `is_plausible_listing()` يرفض إيجار/م² خارج [5,200] | 2.19.1 / v38 |
| A14 | Medium | ✅ فيلا cold-503 (= R2): توازي geometric_factors | 2.22.0a.7 / v146 |
| A15 | Medium | ✅ `hbu_note_ar/en` يُفصح «HBU لم يُقيَّم» عند غياب طبقة التنظيم (value-invariant) | 2.22.0b.12 / v181 |
| A16 | Medium | ✅ = R9؛ `area_match_key` تجميع الأشقّاء + امريخ الجنوبي→مريخ | 2.22.0a.18 / v157 |

-----

## 3 — المؤجَّلات (#42) — مرتَّبة بالأولويّة

| البند | الحالة | محجوز على |
|---|---|---|
| **D-3 · مسار جمع GT** (الكِت طرفاً-لطرف: خطّاف التقرير → واتساب → `validate_gt_sheet.py`) | 🟢 **مفتوح — القرار الملزم التالي** | كلمة البدء + تدفّق GT يدويّ/عضويّ (≥8 فاخرة جديدة + ≥6 قديمة + ≥6 مثمّن، **بمستند**) |
| **DEF-01 / B-2** محور النوع/الحالة الدائم (نصف R7-التبخيس — شريحة luxury_new مُعايَرة) | PARKED (اتجاه ما-بعد-البيتا، لا green-lit) | كوربوس GT **n≥20 موثَّق** [§20.27] |
| **DEF-03 / تفعيل a15** (capture + feedback، خامل = R11) | DEFERRED (مبنيّ+حيّ لكن خامل؛ صفر أثر) | counsel §8.1 PDPPL + §8.2 + فحص CC أمنيّ (= D-2) [§20.15] |
| **Sprint 2 · واجهة feedback** (تستهلك /api/feedback) | DEFERRED (غير مطلوب للبيتا) | تفعيل a15 (DEF-03) |
| **b11 low>high انعكاس النطاق** (عند `primary.high < cost`) | OPEN — **أوّل مرشَّح صيانة** (micro-fix) | غير مجدول؛ مُشاهَد حيّاً 54/788/10 + 55/1056/60 [§20.50] |
| **income_led/b13-trim إعادة حساب التفكيك** | OPEN (فجوة شقيقة، لا تعرّض حيّ — مُدخل-gated) | إيجار/عمر مُدخَل [§20.50] |
| **b18 finish-delta لا يشمل new/renovated** | DEFERRED (نطاق b18 = is_luxury/high الموقَّع) | Gate-2 توسعة موقَّع [§20.52] |
| **b16 ترقية وسم n≥20** (نصّ «معايَرة على تقييم واحد V001») | DEFERRED (السلّم في الكود؛ لا نصّ مُخترَع) | كوربوس GT n≥20 [§20.50] |
| **§6 v2 المتبقّي** — Fork C (lookup واعٍ a18/override) + (ii) إيجار-حسب-العمر (opex 0.20 ✅ b8) | DEFERRED | موثوقيّة العمر الآليّ E22 [§20.40] |
| **DEF-13 / soil-geotech (sabkha/karst) + ملحق منهجيّة RCN** | DEFERRED (طفيف؛ سلّم RCN+DRC قائمان) | طبقة GIS v2 [§20.45] |
| **عقيدة سقف الدخل بـDRC-النظام** (هل يُسقَّف income بكلفة النظام؟) | DEFERRED (قرار منهجيّة منفصل موقَّع) | Gate-2 PO إن أُريد [§20.55] |
| **DEF-02 / Phase-2 نقاء الحوض + النافذة الرفيعة (R8) ; DEF-08 size داخل الشريحة** | PARKED (تشخيص النقاء يُشحَن مُفصَحاً) | بعد أولويّات R7 [ISSUES DEF-02] |
| **DEF-04 / شقق MME** (2.21.1/2.29) | OUT-OF-V1 (auth) | جلسة MME مُصادَقة (DevTools) |
| **2.16.16 Confirmed Sales DB** | DEFERRED إلى أجل غير مسمّى (لا مصدر داخليّ) | مصدر بيع T1 مفتاحه PIN (لا يوجد) |
| **v4 «thinnest-flow» المتبقّي:** (3) قراءة الحساسية + (4) التفكيك في النتيجة | DEFERRED ((1) تأكيد ✅v169 · (2) نطاق-صدارة ✅v170) | B-2 (محور الحالة) مركون n≥20 [§20.34] |
| **a18 fast-follow** — ضربة حيّة على معيذر/نعيجة (عرض التجميع طرفاً-لطرف) | DEFERRED (المسار مُثبَت offline + بالضربة الحيّة) | عنوان معيذر/نعيجة للضرب الحيّ [§20.18] |
| **D-7 · تعميم هوية thm-report على الموقع كله** | DEFERRED (سبرنت مستقبليّ بقرار PO) | اختيار PO [PLAN §1 D7] |
| **مسح QR ورقيّ → /verify** (يدويّ، غير هندسيّ) | OPEN — البند اليدويّ الوحيد من سلّة الدخان | خطوة Anas (هدف الرمز مُثبَت ✓ حيّاً) [§20.61] |

-----

## 4 — «بوابة بيانات الأنواع» (العمل غير-الفيلّي — كل بند Gate-2 موقَّع مستقل)

| البند | الحالة |
|---|---|
| **(أ)** وعد GAI للمجمعات (44.35M يُحسَب ثم يُهمَل — الـDCF fork يسبق ترقية E20) | مرشَّح Gate-2 [§20.56] |
| **(ب)** `value_stack`/`leadership` للمباني (توسعة b20) | مرشَّح Gate-2 [§20.56] |
| **(ج)** تبويب الأنواع + بطاقات «قريباً» **+ نسخ مختصر المباني** («بيتك» + «رسملة معايَرة 6.5%» على المعدّل النموذجيّ + هبوطه الافتراضي) | مرشَّح Gate-2 (علم Soft-Gate-3 b29) [§20.57/§20.61] |
| **(د)** معايرة رسملة المباني (6.5%/7.5% نموذجيّة → MME أو GT) | مرشَّح Gate-2 [§20.57] |

-----

## 4ب — مؤجَّلات مراجعة الشخصيات العشر (2026-06-13؛ مُؤرَّضة على الحيّ، مرتَّبة بالتكرار×الشدّة)

> مراجعة 10 شخصيات على b29/v200 (وكلاء نقّاد تحقّقوا من index.html). NET-NEW = جديد؛ المُخطَّط مسبقاً مُشار إليه.

| # | البند (من تأثّر) | الإصلاح | الحالة |
|---|---|---|---|
| ~~DEF-UX1~~ ✅ | **كشف الصفقات المقارِنة للفيلا/البيت** (7/10: البنك·المثمّن·السمسار·المستثمر·المالك·الصحفي·المشترية) | accordion «🔑 N صفقة في شريحتك ومنطقتك — هي ما قرّر رقمك» في «كيف وصلنا» (b31؛ b34 density-open للمستثمر/المثمّن) — جدول `تاريخ · م² · ر.ق` (dir=ltr) + CC BY 4.0؛ من صفوف الشريحة الموضوعيّة `build_reference(return_transactions=True)` → `MoJValuation.bracket_transactions` → `valuation.comparables`؛ **مُبوّب `leader=='market' && method=='comparison_bracket'`** (matched فقط — يستبعد cost-led/income-led/geo-led/thin/أرض/رفض)؛ صفوف مجهولة (E12: لا PIN/عنوان) + raw rows/تواريخ (لا تطبيع — recon §7)؛ value byte-identical | ✅ **SHIPPED b38/v209 · CHANGELOG_v121 · §20.69** (🟢 engine-additive display-only/value-invariant؛ recon `docs/PHASE0_DEF_UX1_keystone_comparables_recon.md`: «مبنيّ-مجاناً» مُفنَّد [الصفوف تُحسَب ثم تُهمَل على المسار الحيّ — `evaluate_property.py:1576` بلا الـflag] لكن الكشف modest؛ isolated 25/25 + b37 R6 re-point + DoD 392/15/45/broad **106** + local E2E [أبو هامور matched **n=37 PRESENT** · Marikh cost-led/V001 geo-led/شقق **absent**] + R14 390×844 [0 console، بلا overflow، القيمة 2.4M ثابتة] + دخان v209 byte-identical؛ **UX1.1 الكَيستون الجغرافيّ ✅ SHIPPED b39/v210** (commit `65cfa35`، CHANGELOG_v122، §20.70: صفوف منطقة الموضوع الخام في الحوض الموسَّع `geo_v2['primary']['transactions']` + إفصاح أنّ المجاورة مُعدَّلة-الموقع جُمِّعت أيضاً [لا overclaim]؛ البوّابة وُسِّعت لـ`method in (comparison_widened, _widened_indicative)` مع `leader=='market'`؛ V001 → geo_widened pool_n=34؛ isolated 19/19 + b38 R6 re-point + DoD 392/15/45/broad 107 + R14 + دخان v210 value byte-identical)؛ **NEXT المتبقّي = حوض cost-led «رُئي ولم يقُد» (e.g. Marikh) + الحوض الجغرافيّ الكامل (صفوف المجاورة بمصدرها + التعديل)** — مؤجَّلان #42) |
| ~~DEF-UX2~~ ✅ | **إثراء نسخة-الحافظة بالدليل** (السمسار·الورثة·البنك) | n + حداثة + label + report_ref + verify | ✅ **SHIPPED b30/CHANGELOG_v113** |
| ~~DEF-UX3~~ ✅ | **رفض الشقق فوريّ صادق** (8/10) — بطاقة «أضف الإيجار» مُضلِّلة | reframe سطحَي الرفض (apartment+tower، `!hasValuation`): 🚧 «{الشقق\|الأبراج} غير مدعومة بعد — للفلل والأراضي فقط» + «why» (لا تسجيل MoJ لوحدات فردية) + كبح CTA «→ أدخل: الإيجار»؛ مسار الدخل UNTOUCHED (compound_large يُبقي CTA الصحيح E20؛ شقة-بإيجار byte-identical §20.56)؛ recon-reshaped (كشف client-side + تعطيل تبويب متعذّران — E17/لا تبويب → بوابة الأنواع ج) | ✅ **SHIPPED b36/v207 · CHANGELOG_v119 · §20.67** (🟢 frontend/value-invariant؛ §20.42 مبنيّ-مسبقاً ثم قياس مستقل؛ isolated 22/22 + DoD 392/15/45/broad 104 + R14 + دخان v207 byte-identical) |
| **DEF-UX4** 🟠 | **بانر حداثة ديناميكيّ + تعديل سوق أمامي** (6/10؛ المستثمر الأشدّ) | بانر أصفر >90 يوماً + slider «تعديل ±0–15%» يعرض رقماً ثانويّاً (الـcaveat مبثوث؛ الـslider NET-NEW) | NET-NEW عرض |
| **DEF-UX5** 🔴 RE-CLASSIFIED | **تبديل إنجليزيّ AR\|EN** (5/10؛ الصحفي = فتح السوق الدوليّ) | زر لغة + R14 bidi | 🔴 **مشروع توطين Gate-2 — NOT a 🟢 slice** (recon 2026-06-13 `docs/PHASE0_DEF_UX5_en_toggle_recon.md`: «الـbackend جاهز» **مُفنَّد** — تغطية `_en` **32.6%** [58 حقل `_ar`، 67.4%، بلا توأم]؛ الواجهة تستهلك **0** `_en` [بثّ ميّت]؛ **740/3072** سطر عربيّ مؤلَّف-عميلاً + Terms ~800 كلمة + قلب bidi/LTR بلا مصدر EN؛ يحتاج brief توطين متعدّد الملفّات موقَّع — engine `_en` + قاموس عميل + bidi) |
| **DEF-UX6** 🟠 | **أثر التحسين (delta قبل/بعد)** (4/10) | banner «+50,000 (1.2M→1.25M) — بناءً على [المدخل]» بعد إعادة التقييم | NET-NEW |
| **DEF-UX7** 🟠 | **MUC/القانون بارزاً على المختصر** (4/10؛ المحامي/البنك) — غائب عن الهبوط الافتراضي b29 | flag «⚖️ غير معتمد — المحكمة/البنك/IFRS تتطلب مُقيّماً» قرب hero ص1 + رابط §٩ | NET-NEW (قرار scope: الحذف كان مقصوداً «الزبدة في صفحة») |
| **DEF-UX8** 🟡 | **حواجز قدرة-تحمّل/LTV بحاسبة التمويل** (أم خالد·البنك·المهندس) | سطور إرشاد بنكيّ (LTV≤80–90% · القسط>30% دخل تحذير · نطاق فائدة قطر · تنبيه cost-led) | NET-NEW |
| ~~DEF-UX9~~ ✅ | **كشف BUA/RCN/معامل الاحتفاظ** (المهندس·المثمّن) | سطر «🔧 آليّة الكلفة (نهج DRC)» في أكورديون «كيف وصلنا» (`show()` how buffer) من `value_stack.cost.*` المبثوثة (BUA × كلفة الإحلال × معامل الاحتفاظ ← البناء المُهلَك + الأرض + الافتراضات)؛ مُغلَق ببنود الثلاثة؛ أرقام في جزر dir=ltr؛ صفوف التقرير/المختصر UNTOUCHED (DRY) | ✅ **SHIPPED b37/v208 · CHANGELOG_v120 · §20.68** (🟢 frontend/value-invariant display-only؛ isolated 22/22 + b36/b31 R6 re-points + DoD 392/15/45/broad 105 + R14 + دخان v208 byte-identical) |
| **DEF-UX10** 🟡 | **سيناريوهات الحالة على cost-led** (المهندس) | رفع شرط الـrender ليشمل cost-led (`scenarios` مبثوثة b23) | جزئيّ — بوابة الأنواع/#42 |
| صغائر | فجوة سعر-الطلب (أم خالد) · شرح «جبري»+tooltip · rubric الحالة · share-WhatsApp للورثة · إزالة الإيموجي من التقرير الرسميّ (البنك) | — | NET-NEW micro |

### 4ب-2 — التبسيط والإفصاح التدريجيّ + تحسين المدخل (دراسة 2026-06-13)

> **العمق الكامل:** `docs/STUDY_persona_simplicity_and_entry_v1.md` (خط أساس الحِمل المقيس + مصفوفة الحِمية لكل دور + قائمة قتل الإرباك + إعادة تصميم المدخل). **المبدأ:** «من أنت؟» (مُهمَل اليوم — عرضيّ فقط) يصير **مفتاح كثافة العرض**؛ المالك البسيط يرى **٥ عناصر** (TIER-1 الحاليّ = **٢١**)؛ القيمة ثابتة (b24)، الكثافة متغيّرة.

| ID | الاسم | النطاق | البوّابة |
|---|---|---|---|
| ~~DEF-UX11~~ ✅ | طيّ TIER-1 للمالك | «موكب التسعة ملاحظات» + لوحة الأدلّة → أكورديون واحد «🔍 كيف وصلنا لهذا الرقم؟» (مطويّ، أوّل)؛ TIER-1 = النواة الخمسية؛ byte-identical | ✅ **SHIPPED b31/Heroku v202** (commit `a5cdce9`، CHANGELOG_v114، §20.62؛ frontend/value-invariant: `how` buffer + الأكورديون الواحد؛ isolated 36/36 + b15 re-pointed 50/50 + DoD 392/15/45/broad 99 + R14 390×844 [الموكب مطويّ، النواة بالرقم، صفر console، بلا overflow] + دخان v202 [5 مراسٍ byte-identical]) — النطاق المسمّى = «التسعة + اللوحة»؛ طيّة الدراسة الكاملة §3 (tier-badge/MUC-word/moj-n) = micro مؤجَّل |
| ~~DEF-UX12~~ ✅ | الكثافة المقودة بالدور (المفصل) | `show()` يشتقّ `_dense` من `d.audience` (مبثوث أصلاً top-level — recon فنّد «تعديل الخادم»: `evaluate_unified.py` يضع `'audience': audience` في كل مسار، live-confirmed؛ `_acc(title,inner,open)` يقبل `open` أصلاً → **frontend-only، api.py UNTOUCHED**) ويمرّره كـ`open` لأكورديون «كيف وصلنا» b31: مستثمر/مثمّن → مفتوح («الأدلّة أولاً»)؛ مالك/مشتري/بائع → مطويّ (افتراض المالك البسيط b31)؛ single-purpose (أكورديون «كيف وصلنا» فقط؛ دلتا الأدوار = شرائح لاحقة)؛ القيمة byte-identical عبر كل الأدوار | ✅ **SHIPPED b34/Heroku v205** (commit `703a988`، CHANGELOG_v117، §20.65؛ 🟢 frontend/value-invariant: isolated 15/15 + 5 sibling R6 re-points [b15/b2p2/b31/b32/b33] + DoD 392/15/45/broad **102/102** + R14 390×844 [5 أدوار: مالك/مشتري/بائع مطويّ · مستثمر/مثمّن مفتوح · القيمة byte-identical 2.4M/2.4M/5.4M · صفر console · بلا overflow 390==390] + دخان v205 [الـHTML المخدوم يحمل `_dense`؛ audience مبثوث live؛ 5 مراسٍ value byte-identical لـv204]) — دلتا الأدوار + الأدوار الجديدة (ورثة/بنك = UX17، Gate-2) تتفرّع من هذا المفصل |
| ~~DEF-UX13~~ ✅ | تبسيط شاشة التأكيد | إسقاط لوحة الأدلّة من `showConfirm` (تعيش على النتيجة، أكورديون «كيف وصلنا» b31؛ لا pill) + معادلة الارتدادات E15 → tooltip (`cg-tip` ⓘ، يبقى رقم الحدّ الأقصى + CTA «عدّله») + `pbRows(pb, basisOnly)` يُخفي المرافق+فترة المسح (رسالة العمر قائمة عند حقل العمر في refine) ويُبقي PIN؛ byte-identical | ✅ **SHIPPED b32/Heroku v203** (commit `e81ed4f`، CHANGELOG_v115، §20.63؛ frontend/value-invariant: 4 تعديلات؛ isolated 29/29 + 4 إعادات توجيه siblings (b2p2/b2p3/b27/b31، R6/Lesson-2) + siblings green WITHOUT re-points + DoD 392/15/45/broad **100/100** + R14 390×844 [التأكيد نظيف: الأساس + الرقم المساحي + «≈ ٣١١ م² ⓘ — عدّله»، بلا لوحة أدلّة/مرافق/عمر/معادلة inline، صفر console، بلا overflow] + دخان v203 [5 مراسٍ value byte-identical لـv202]) — PIN يبقى (ليس في حصر القتل 17-20، Rule #38) |
| ~~DEF-UX14~~ ✅ | المدخل: تحسين الافتراضيّ (الخيار D) | (أ) سطر مساعدة لمدخل العنوان («لوحة عنوان المبنى أو فاتورة كهرماء — المنطقة، ثم الشارع، ثم رقم المبنى»؛ `.br-note` نظير تلميح PIN، بعد الحقول الثلاثة) + (ب) افتراضيّ معقول = تذكّر آخر هُويّة عقار محلياً (`_saveIdentity` في `run()` بعد بناء `bd` + `_restoreIdentity` على `DOMContentLoaded` + `clearIdentity` + رابط «مسح»؛ يحفظ `{tab,zone,street,building,pin}` لا audience؛ أوّل-زيارة فارغ؛ localStorage + in-memory fallback، لا cookie/خادم — a24/DPIA)؛ `run()` يبني `bd` بلا تغيير، التخزين يملأ مسبقاً فقط؛ byte-identical | ✅ **SHIPPED b33/Heroku v204** (commit `2214ad3`، CHANGELOG_v116، §20.64؛ 🟢 frontend/value-invariant: isolated 33/33 + b32 R6 format re-point [29/29] + DoD 392/15/45/broad **101/101** + R14 390×844 [سطر المساعدة ضمن 390 (39→351)، دورة حفظ/استرجاع/مسح + تذكّر تبويب الأرض، صفر console، بلا overflow 390==390] + دخان v204 [الـHTML المخدوم يحمل «لوحة عنوان المبنى» + `_IDENT_KEY`؛ 5 مراسٍ value byte-identical لـv203]) — الخيارات الأخرى للمدخل (C إكمال تلقائيّ = UX15 · A حقل ذكيّ = UX18 · B دبّوس خريطة = backlog) كلٌّ سبرنته |
| **DEF-UX15** ⛔ BLOCKED | المدخل: إكمال تلقائيّ (الخيار C، المُوصى به) | منسدلات zone→street→building من JSON ثابت مُفرَّغ من `QARS_Point` | ⛔ **BLOCKED on a QARS data-drain (recon 2026-06-13، b35):** لا مصدر zone/street/building محليّ (`moj_weekly.csv` = أسماء مناطق فقط)؛ khazna `QARS_Point` **geo-restricted محلياً** → التفريغ يحتاج **probe deploy على Heroku** (Gate-1) + التفريغ الكامل building-level = **162,516 نقطة (~3MB+ JSON)** غير عمليّ للـfrontend. **ليس سبرنت frontend سريع value-invariant** كـb31-b35 — إنه بيانات/بنية-تحتيّة. **شرط الإحياء:** قرار PO على نطاق التفريغ (zone-only autocomplete خفيف؟ أم hierarchical؟) + probe deploy لتفريغ `QARS_Point` من Heroku + مكان تخزين الـJSON (slug؟ pre-committed مثل `developer_inventory.sqlite`؟). ينتظر سبرنته الخاصّ بـ§5 audit |
| ~~DEF-UX16~~ ✅ | حاسبة التمويل للمشتري | حاسبة (دفعة%→قسط) أسفل الرقم عند `audience=buyer`؛ display-only؛ DRY — تعيد استخدام `_srPayment` (amortization المختصر b25/b28، «القسط ١٠٬٦٧٢») عبر `bcRecalc()` بـids منفصلة (`bc*` لا `sr*`)؛ القيم الافتراضيّة 20%/25سنة/4.5% (عقد b28)؛ القسط مشتقّ من `v.amount` → الأدوار الأخرى بلا تغيير | ✅ **SHIPPED b35/Heroku v206** (commit `b775381`، CHANGELOG_v118، §20.66؛ 🟢 frontend/value-invariant: isolated 17/17 + b34 R6 format re-point [15/15] + DoD 392/15/45/broad **103/103** + R14 390×844 [المشتري: الحاسبة القسط ١٠٬٦٧٢ = رقم المختصر؛ المالك/المستثمر: بلا حاسبة؛ القيمة byte-identical 2.4M/2.4M/5.4M؛ التفاعليّة مثبتة 20%/25y→10,672·50%/25y→6,670·50%/15y→9,180؛ صفر console؛ بلا overflow 390==390] + دخان v206 [الـHTML المخدوم يحمل `bcRecalc`؛ 5 مراسٍ value byte-identical لـv205]) — حواجز القدرة (UX8: LTV · القسط>30% دخل) تُبنى فوق هذه الحاسبة |
| **DEF-UX17** 🟡 | مسار الورثة/البنك | المخرج الأساسيّ = التقرير الكامل/شهادة لا المختصر + إخفاء الحاسبة/السيناريوهات؛ يحتاج تمييز هذين الدورين (خارج المُنتقي الخماسيّ) | 🟡 copy/scope Gate-2 (تسميات جديدة) |
| **DEF-UX18** 🟡 | المدخل: حقل ذكيّ واحد (الخيار A) | `/api/address-lookup?q=` يُحلّل address/PIN/area → يُلغي التبويب؛ يجتاز بوّابة #46 | 🔴 engine جديد / Gate-2 |
| (backlog) | المدخل: دبّوس الخريطة (الخيار B) | GPS tap → `CadastrePlots` spatial → PIN؛ مأمور بـE17، أعلى مكسب/جهد | 🔴 frontend(Leaflet)+engine / Gate-2 |

-----

## 5 — قيود ISS (issues حوكميّة/تصميميّة)

| ID | الحالة | القرار |
|---|---|---|
| **ISS-G03** | ✅ مُتبنّى | البيتا = مسار موازٍ **غير حاجز** — لا بوابة كوهورت (gate #6 محذوفة)؛ القرار الملزم = D-3؛ غطاء R13 (a24/a25) يبقى |
| **ISS-A07** | ✅ مُنفَّذ b14 | تماسُك التفكيك — سرد القيمة يطابق لوحة الـ10-سنوات/الطبقات (Case A/B/C) |
| **ISS-G04** | ✅ مُغلق | المثمّن = مرجع منهجيّة فقط؛ §20.9 DRC مستقلّ (لا إحياء API — §20.8) |
| **ISS-G05** | 🟡 مفتوح (انضباط مستمرّ) | نظافة الذاكرة عند العتبات + doc-delta قبل routing البناء (C1) |
| **ISS-A08** | ⛔ PARKED (محجوب) | معايرة MoJ الذاتيّة محجوبة تشفيريّاً + أخلاقيّاً (E12 — PN-hash لا يُعكَس؛ لا re-identification) |
| **ISS-A09** | 💡 مفتوح (فكرة مستقبليّة) | واصفات الحالة من نصّ الإعلانات إشارة R7 مستقبليّة (NLP+PIN صعب؛ الأسعار محظورة E1/E3) |
| **ISS-R01** | ⏳ محجوز للتسييل | استعلام Aqarat بوابة ما-قبل-التسييل (مُسوَّد + معلَّق؛ `docs/Aqarat_Enquiry_DRAFT_hold.md`) |

-----

## 6 — قاعدة الصيانة

عند كل إغلاق سبرنت: حدِّث §1 (المفتوح) + §2 (إن أُغلق خطأ) + §3 (إن استُهلك مؤجَّل) **هنا**، و`RISK_REGISTER.md`/`RISK_SUMMARY.md` للمخاطر. لا تُعاد كتابة الملف كاملاً إلا عند **بوابات المراحل**. مؤجَّل جديد يأخذ **DEF-**التالي؛ ISS جديد يأخذ المُعرِّف التالي في سلسلته.
