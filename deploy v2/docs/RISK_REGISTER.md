# RISK REGISTER — Thammen (thammen.qa)

> Living register of operational/methodology risks. Seeded 2026-05-30 (governance
> consolidation, BRIEF_governance_consolidated_2026-05-30.md §B). Append new risks; do not
> delete — mark `CLOSED` with date + evidence. Severity: 🔴 high · 🟠 medium · 🟡 low.

| ID | Sev | Risk | Evidence / trigger | Mitigation / status |
|---|---|---|---|---|
| **R1** | 🟠 | **Cross-chat state divergence → near work-loss.** Work done in one Claude surface (commits, deploys, decisions) is invisible to another that trusts a stale brief/memory; risk of re-doing, overwriting, or "losing" committed work. | 2026-05-30: a forensic read-only pass was needed to confirm `0c81363`/`1711035` were real and that origin was 98 commits behind production. | Rule **#57** (session-start ground-truth handshake): `curl /api/health` + `git HEAD/reflog/origin-diff` before routing work; live state outranks memory. Backup-push now part of the deploy ritual (Rule #43). |
| **R2** | ✅ | **A14 villa cold-503 — CLOSED 2026-05-30 (Sprint A14 lever 2, Heroku v146).** Heavy multi-QARS villa path warm ≈21 s vs the 30 s router wall → cold first-try 503 (ref 56/565/21). | Was 503@31s cold. **Post-deploy live H_lat: 56/565/21 cold first-try 200@14.4s + 200@15.0s (×2) · 56/647/6 cold 200@15.9s — all <30s, margin ~15s, zero 503.** | **FIXED:** parallelized `geometric_factors` internals (Round0 polygon → Round1 ∥{corner road-probes, hbu, landmarks}); byte-identical (H_det). Lever 1 (overlap, H_A-cleared) deferred + ready if the margin ever regresses. |
| **R3** | 🟠 | **Memory-vs-disk operational gap.** CLAUDE.md / briefs / chat memory drift from the live code, git, and `/api/health`; numbers get trusted instead of measured. | This governance pass found: MoJ "139d" (doc) vs **150d** (live); "VPS 4" (docs) vs VPGA 10 + VPS 6 + IVS 106 (code); "Current latest = 2.16.12" (4 sprints stale); a 49/49-vs-48/49 test delta. | Rule **#58** (assumed-vs-actual gap): when memory/brief diverges from measured live state, measured wins and the gap is logged HERE. Single-source critical numbers (DoD count). |
| **R4** | 🟡 | **MoJ data 150 days stale; refresh not self-serving.** `data.gov.qa` last published 2025-12-31; adopting any new drop is gated on a multi-factor sanity gate (schema/NBSP/volume) before it can replace the snapshot. | `/api/health` moj_freshness: days_old=150, tier=stale (2026-05-30). MUC clause active since 2026-02-28. | Transparent staleness banner (Sprint 2.7) + MUC. No silent adoption of a new drop without the multi-factor gate. Monitor. |
| **R5** | 🟠 | **QARS Heroku reachability fragility.** khazna `QARS_Point` returned an ArcGIS auth-error envelope from the Heroku AWS IP (Sprint 2.22.0a.1); a silent envelope-as-empty failure degraded address-tab classification. | 2026-05-27: `/api/health` qars_endpoint=degraded; every address `/api/evaluate` returned asset_type=unknown until the envelope-fallback hotfix (v132). | `_qars_query()` primary→legacy fallback on both exceptions AND ArcGIS envelopes (Rule #11 defensive design). Monitor `/api/health` qars_endpoint. |
| **R6** | ✅ | **Brittle EXACT-version-pin tests — RESOLVED 2026-05-30 (Sprint A14).** Test files that assert `ENGINE_VERSION == '<frozen tag>'` fail on every later sprint, producing a red regression unrelated to function. | `test_sprint_2p22p0a5_request_budget.py` (2 assertions) failed post-2.22.0a.6 bump — same anti-pattern Sprint 2.19.1 relaxed elsewhere. | **FIXED:** the 2 pins loosened to version-agnostic FORMAT checks against the live `ENGINE_VERSION`/`SPRINT_TAG` source → broad regression **50/50** green (was 48/49). Watch for the pattern recurring in new test files. (Recurred + fixed again in a8 `test_sprint_2p22p0a8_rics_citation.py` during the a9 deploy-prep.) |
| **R7** | 🟠 | **Built-type + condition blindness is BIDIRECTIONAL and affects BOTH comparison paths (widened AND bracket)** — generalised 2026-05-31. geo_v2 (`_categorize:105`) lumps basic / 2-story+annex / +penthouse / مسكن / مجلس into one `'villa'`, and condition (finish / maintenance / lease) is not an input — so the engine returns the comp **pool's central tendency, blind to where the subject sits in it**: it **over-anchors** below-average-condition subjects and **under-anchors** above-average-condition ones. The earlier "widened-only, over-anchor-only" framing was too narrow, and **"bracket path validated clean" holds ONLY for average-condition subjects.** (Widened mechanism — **refined by Phase-1b (measured)**: **window-recency + size-bracket selection** dominate; the built-type *lump* is secondary and the +penthouse reading is wrong [penthouse is **dilutive**, not inflative]. SAME area — NOT geographic widening; "comparison_widened" is a misnomer, decision was `primary_sufficient`/0 adjacent.) | **Over-anchor (widened):** 54/541/6 Marikh — PLAIN 2story+annex ~20yr ordinary finish → engine **681/ft² @24mo** (bracketed) vs unbracketed 509 (**+34%**). **Refined by Phase-1b (measured): the over-anchor is WINDOW-driven, NOT +penthouse** — STANDALONE_VILLA bracket[490-736] collapses **681 (24mo, n=29) → 554 (36mo) → 517 (FULL, n=43)** (recent high villa sales); penthouse rows are *cheaper* (517) than strict villas (790) so folding penthouse **DILUTES** the median (790→681), it does NOT inflate it. **Detector = the a10 dispersion gate, NOT an n-threshold** (Marikh n=29 passes T=20 yet **+32% over FULL**). Defensible ~512–517/ft² plain ceiling. **Under-anchor (bracket):** 56/565/21 Abu Hamour — excellent G+1, secure govt lease → engine **2.5M (~P68) under-anchors ~10%**; pool is TIGHT (dispersion **0.211**, correctly NOT gated by a10) but the subject sits at the UPPER end — market median ≈ income@5% ≈ 2.5M; P90 (567/ft²) ≈ income@4.5% ≈ 2.75M for this condition + secure income → **defensible RANGE ~2.5–2.8M (market + income basis, NOT replacement cost). Do NOT treat 56/565/21's 2.5M as a validated point.** | **OPEN. a10 = necessary, NOT sufficient:** the dispersion gate (Sprint 2.22.0a.10) catches dispersed pools (the over-anchor case) but does **NOT** catch the tight-pool-above-average case (56/565/21). **Gate-2 (c) — built-type/condition stratification — is the real fix, in BOTH directions across ALL areas.** Input = built-type + condition via **2.22.0b Stage-2 Q&A** (user-reported, broker-verified at Stage 4) — **NOT blocked on broker data sourcing** (QARS carries no built-type; the staged flow supplies it). **OPEN ↓ — over-anchor half TREATED:** b11 cost down-re-anchor (Marikh floor 1.9M→2.4M, v180) + b13 convergent-TRIM (user-age-gated, V001+25y→3.6M, v182; D-1 0.31 lux floor ✅). **Under-anchor half → B-2 (PARKED n≥20):** the Lever-2 cost UP-lift is FALSIFIED — **E25**: V002/V003 (new-premium) sold ABOVE replacement cost → a **market premium**, NOT cost-reachable → needs a calibrated `luxury_new` comparable stratum (corpus n≥20), not a cost lift. |
| **R8** | 🟡 | **Comparable-pool purity + thin-window volatility — PINNED by Phase-1b (measured 2026-05-31).** The two levers behind the R7 over-anchor, now quantified. (1) **Thin-window volatility:** geo_v2's 24mo default. (2) **Pool impurity:** geo_v2 pools by `نوع العقار` via `_categorize` (type-based, **no** residential-usage filter; lumps HOUSE + STANDALONE_VILLA + penthouse + فيلتان into one `'villa'`). | **Window (B1):** STANDALONE_VILLA is the volatile stratum — 24mo median **+8-11% above FULL** (genuine recent appreciation; e.g. 600-900: 480→459→432 @24/36/FULL); **LAND/HOUSE window-stable**. Cell volatility 24mo→FULL: thin cells (<10) move >15% in **17%** of cells vs **4%** at n≥20; 24mo→36mo is gentle (median ~1.2%); staleness drift on reliable cells **+3.3%**. Marikh STANDALONE_VILLA bkt[490-736] **681(24mo,n=29)→554(36mo)→517(FULL,n=43)**. **Purity (B2):** residential-usage filter removes **~22%** of `cat=villa` → pooled median **−5%** (in-bracket −2 to −4%); compound(فيلتان) removal **≤3%**. Penthouse fold-in negligible globally (Δ=1) but Marikh-material (20/74 of Qatar penthouses; **dilutive** in the R7 bracket). | **Phase-2 design inputs (PINNED).** Window: 24mo default; if cell **n<20** widen to **36mo** then FULL — **prefer 36mo** (cuts ~half the recency drift, gentle move). **CAVEAT:** n alone insufficient → **pair with the a10 dispersion gate** for dispersed-but-sufficient pools (Marikh n=29 passes T=20, still **+32% over FULL** — cross-ref **E23**). Purity: **ADOPT** the residential-usage filter (clean, −5% pooled) **+** compound removal (structurally correct, ≤3%); both **ORTHOGONAL** to Marikh's over-anchor (Marikh bracket cascade 517→517→517 FULL). Reliability: at 24mo only **83/787 (10.5%)** of cells reach n≥20 → shrinkage essential. Cross-ref **R7** / **E23**. Phase-2 = separate signed brief (not implemented). OPEN. |
| **R9** | ✅ | **A16 — bracket-path area-name under-match starves the comp pool.** `build_reference` matches `اسم المنطقة` EXACTLY (no alias/NBSP merge), so a sparse resolved name (e.g. `امريخ الجنوبي` n=0) can shadow the populated one (`مريخ`), forcing the dispersion-prone widened path even where bracket rows exist. | Marikh 54/541/6: offline exact `مريخ` = 400-600 n=23 + 600-900 n=13, yet live `bracket_n~1` → geo-routed. Sprint a13/M1: alias-merge yields only **+1** reliable cell globally → A16 is a **CORRECTNESS** bug, NOT a thin-cell lever. | ✅ CLOSED 2026-06-03 (Sprint 2.22.0a.18, Heroku v157) — resolved-as-pool-fix: area_match_key sibling-aggregation wired into build_reference + امريخ الجنوبي→مريخ override; live Marikh comparison_thin 5.4M n=15 same-district (§20.18). Condition residual = R7. Residual watch: unreachable names (فريج العسيري ~0.25%) + optional fast-follow live sub-zone demo. |
| **R10** | ✅ | **Bracket-SUCCESS path had NO dispersion gate — CLOSED 2026-06-01 (Sprint 2.22.0a.14 / (vi), Heroku v153).** A dispersed `comparison_bracket` "reliable" cell presented as a clean point WITHOUT the honest-range / tier-downgrade / MUC the a10 gate applies on the widened path (E23 / VPGA-10). **GENERALIZED beyond a13:** the gap was SYSTEMIC, not a13-specific. | Measured (ppm² 36mo (p75−p25)/median vs `STAGE1_DISPERSION_T=0.30`): **20 of 37** reliable villa bracket cells dispersed ≥0.30 = **7 a13-rescued + 13 PRE-EXISTING** (e.g. غرافة الريان 600-900 0.684, العب 600-900 0.632, الثمامة 46 0.446, الخريطيات 600-900 0.445, الغرافة 600-900 0.428). Anchors clean (Abu 0.208, Marikh 0.197). (55/296/13 dispersed 0.492 but `comparison_thin` → keeps its weak-sample caveat, not on the bracket-success path.) | **CLOSED by the a14 `comparison_bracket` branch in `_stage1_dispersion_gate`** (reuses the a10 application block — range-headline + 🟡 indicative + MUC high + AR/EN disclosure; presentation-only, NO value change) + (b) window disclosure («نافذة 36 شهراً» on `source_ar` + recent/total split in the Methodology brief). **BOUNDARY:** 3 cells within ±0.006 of T=0.30 (الثمامة 50 0.294 / نعيجة 44 0.302 / ام عبيرية 0.305) may flip gated↔clean on a MoJ refresh — expected, not a regression (hysteresis if it bites; dormant while MoJ frozen). **FAST-FOLLOW:** a direct live hit on a gated bracket cell (gate decision proven offline e2e + threading/application proven live). Cross-ref R7/R8/E23. |
| **R11** | 🟡 | **Beta instrumentation shipped DORMANT — must NOT be activated before counsel.** Sprint 2.22.0a.15 (Heroku v154) added prediction capture + `POST /api/feedback`; the captured record is personal / quasi-personal data under PDPPL, so activating before the data policy + cross-border ruling would create an unlawful-processing risk. | 2026-06-01: built + live but **inert** — capture no-ops unless BOTH `EVAL_CAPTURE_ENABLED=true` AND `DATABASE_URL` set; the Heroku Postgres add-on is NOT provisioned. Two-lane post-deploy smoke (CC browser-UA curl + Anas) BYTE-IDENTICAL on the 4 anchors + `/api/feedback` → {accepted, stored:false}; isolated 27/27, DoD 392/15/45/58. | **OPEN — dormant-pending-activation.** ACTIVATION gated on **§8.1** (PDPPL policy: fields / retention / consent / deletion) **+ §8.2** (cross-border ruling: Heroku US/EU vs a Qatar/GCC Postgres) — Anas + counsel. Do NOT set the flag or provision the add-on before both. UI feedback prompt = Sprint 2. **Pre-activation steps (a16, 2026-06-01):** (1) verify the Fernet encrypt/decrypt round-trip **on Heroku** before any real data (`cryptography` installs via requirements; `CAPTURE_ENC_KEY` set at activation); (2) set the Heroku PG backup retention **SHORT** so expired rows age out of backups within ~the window; (3) **backup-erasure runbook** for an erasure request landing mid-backup-retention. **a16 hardened the surface** (UUID-only key, no stored `valuation_id`, street/building Fernet-encrypted, `note` removed, 180d retention/aggregate/purge/erase — §20.16 / gate-11). |
| **R12** | 🟡 | **CC automated POST smoke to thammen.qa is Cloudflare-blocked (error 1010).** A bare `python-urllib` POST is rejected at the edge (bot signature) → a CC-side post-deploy POST check can be skipped or mis-read as a deploy failure. | 2026-06-01 (a15 post-deploy): urllib POST `/api/evaluate` + `/api/feedback` → **HTTP 403 "error code: 1010"** ×5; the same requests via **curl with a browser User-Agent passed**; GET `/api/health` was never blocked. | **MITIGATED** — Rule **#61** (CC post-deploy POST smoke = browser-UA curl, not urllib) + two-lane confirmation (CC + Anas). Updates the prior "POST only on Anas's side" note: CC can now self-smoke; fall back to Anas / Claude.ai side if Cloudflare tightens (e.g. JS challenge). |
| **R13** | 🟠 | **Regulatory self-clearance without external counsel (decision 2026-06-02).** Anas elected NOT to engage licensed counsel and to self-clear conservatively. التقييم العقاري is a *named regulated activity* under Amiri Decision 28/2023 Art 5(7) (Aqarat) + a broad catch-all, and the valuer-licensing regime is tightening — so operating even a FREE AVM on internal / AI-derived assumptions carries managed-but-real (a) unlicensed-regulated-activity and (b) PDPPL risk, neither *fully* curable by conservatism. | Web research 2026-06-02: Al Meezan confirms التقييم العقاري named among Aqarat-regulated professions + «وغيرها من الأنشطة العقارية»; Peninsula (May 2026) — "broker-as-valuer no longer acceptable"; gov precedent — free "المثمّن العقاري" approximate-estimate tool. AVM-specific binary NOT in public sources. Decision (Anas, 6/2): no counsel. | **OPEN — accepted-with-mitigations.** (1) Beta FREE, invite-only, accuracy-research, labelled «تقدير سوقي آلي، وليس تقييماً معتمداً»; NO paid access pre-licence. (2) Aqarat enquiry DRAFTED + HOLD in `docs/Aqarat_Enquiry_DRAFT_hold.md` — send post-design, pre-monetization. (3) PDPPL self-cleared conservatively (strict opt-in, all records personal data, 180d, full erasure, Qatar/GCC residency) — cross-ref R11. (4) MoJ open-data licence ✅ **CLOSED 2026-06-05** — verified **CC BY 4.0** (commercial + derivatives + redistribution permitted with attribution; Q13 → VERIFIED); verbatim attribution applied in Sprint 2.22.0a.25 / Heroku v164; no longer a paid gate. Checklist: `docs/COMPLIANCE_SELF_CLEARANCE_beta_v1.md`. Honesty #10. |
| **R14** | 🟠 | **Gate integrity — a push-gate report conflated REASONED / WORKED-AROUND with EXECUTED.** A brief-MANDATORY pre-deploy check was silently downgraded to post-deploy and assumed items were tagged as measured → the gate did not actually gate. (R13 was already taken by the regulatory-self-clearance risk, so this is R14.) | 2026-06-02 (Sprint a17): the push-gate report marked the brief-MANDATORY mobile-390×844 check "verified" via `.rn`-reuse REASONING + deferred the pixel-confirm to post-deploy ("a16 precedent"); reported DoD "59/59" when the broad suite was 58/59 (NEVER a clean pass) + 1/1 isolated. Push to v156 proceeded on that report. Outcome BENIGN — post-hoc REAL verification: headless render of the actual card at 390×844 → right-edge 374<390, note `scrollW==clientW`, no overflow; REAL JS parse of live `index.html` → `fmt`/`applyAssetToForm` defined, zero console errors. But the gate didn't gate. | **CONTROL ADOPTED.** (a) "verified" in any push-gate report = EXECUTED, not reasoned — tag each item **done / worked-around / not-done**. (b) a brief-mandatory check that cannot run at gate-time **BLOCKS** the push; downgrading it = Anas's explicit waiver, never a Fast-lane substitution. (c) briefs authored without the codebase mark code-level claims "CC verify in recon." a17 left live (verified-good post-hoc). Cross-ref the same-day geometric-determinism flake-split (Session_Log §20.17). |
| **R15** | ✅ | **`stock_strata.compute_land_median` is NOT a18-aware → its area land-median diverges from the floor's.** The land FLOOR (`value_decomposition.land`) comes from `moj_reference.build_reference` via **`area_match_key`** (a18 R9 sibling-aggregation + hamza-fold); the E4-stratum ratio reference (`stock_strata.land_reference`) comes from `stock_strata.compute_land_median` via plain **`_norm` EXACT-area** matching (a18 never wired in). Same district → two land medians → the stratum ratio can disagree with the floor near a ratio boundary. | Phase-0 Sprint-B recon (2026-06-04, **measured✓**): المعمورة land = **3,768/m² n=20** (moj_reference / floor) vs **4,032/m² n=13** (stock_strata) = **+7%** on the SAME area+24mo window; also explains the v139→a20 floor shift (2.63M→2.46M, the moj_reference path moved, strata did not). Boundary mis-stratification impact plausible but **not yet measured** (assumed~). | **✅ RESOLVED 2026-06-04 (Sprint 2.22.0a.23, Heroku v162, commit `ff483b0`).** `compute_land_median` now pools areas via `moj_reference.area_match_key` (a18) instead of `_norm` exact — the sibling-drop is removed. §5 audit `docs/PHASE0_R15_stock_strata_a18.md` proved blast radius = strata-card **DISPLAY only** (headline `valuation.amount` + the B-1 `value_floor` are UNTOUCHED — value_floor reads `moj_reference` directly). Live re-smoke v162: المعمورة strata land **4,032 → 3,754** (≈ floor 3,768); headline + value_floor **byte-identical** on all 4 anchors. **Refined finding (Rule #36):** the fix closes the genuine **sibling-drop** (area-wide cases); the **bracket-matched** anchors (بو هامور/مريخ/المعراض) are unchanged — their residual strata-vs-floor gap is **Rule E4 bracket-matching** (plot-bracket reference vs the area-level floor), **by-design**, not a bug. The a12 `compute_trend` categorizer-alignment remains the open sibling of this family. |
| **R16** | 🟡 | **Infra SPOF — single Eco dyno / single region / ephemeral FS.** Beta-acceptable; no HA/failover; any outage = clean unavailability (stateless-per-request, no data loss). | Eco dyno only; 30s router wall (R2-class margin ~15s); state lives in git + MoJ snapshot, not local FS. | **ACCEPTED (beta) — must reduce pre-monetization/scale** (redundant tier + managed PG if capture activates). KRI: cold latency vs 30s. |
| **R17** | 🟠 | **Key-person / bus-factor.** Single PO = sole gate authority + sole lane-router + all reserved decisions; no succession. Knowledge risk mitigated by docs; decision/routing continuity is NOT. | Founder unavailability halts all progress (no gate signer, no router). | **OPEN — partially reduced**: git+docs as durable truth (#57/#58 re-ground any session); consider narrowing/pre-delegating reserved decisions. Irreducible residual accepted as stage cost. |
| **R18** | 🟡 | **AI-lane dependency & governance drift.** Both lanes ride one vendor + an AI-mediated governance layer; long-horizon drift/framing-rot is real (proven: 2026-06-09 errata — a stale R9 entry infected derived docs same-day). | Model change/deprecation; stale priors recited un-measured; doc framing-rot. | **OPEN — managed**: #57/#58 handshakes, frozen rules #1–#65, memory hygiene, PO review of briefs, periodic doc cleanups, Rule #54 primary-source gating. Single-vendor residual accepted. |
| **R19** | 🟠 | **GT-corpus scarcity (n<20)** gates precision + any published accuracy claim (§0.4: gates precision, NOT shipping — b4/b11 shipped n=2 disclosed). 184/200 calibration cells = fallback (92%). | `calibration_freshness` fallback share; parcel-linked confirmed sales in single digits; luxury_new stratum n=0 in R7 areas. | **OPEN ↓** — ship-disclosed-as-indicative posture holds; growth = manual valuer reports + organic use (beta = parallel non-blocking track, ISS-G03 — no cohort gate). D-3. |
| **R20** | 🟠 | **No live accuracy measurement** — capture DORMANT, so no production error distribution; only 4 drift-anchors + a handful of confirmed sales. R7's true field error is unknown. | Capture table absent; gate #11 unrun; the ±37/40% comes from 3 sales, not a distribution. | **OPEN ↓** — activation gated on D-2 (§8.1 fields + §8.2 residency) + CC security pass; a16 hardening pre-built. Until then: anchored-and-disclosed, never measured-and-claimed. |
| **R21** | 🟠 | **MoJ single-source systemic dependency.** Sole truth source by design (E1/E12); discontinuation / schema change / licence change / registration-convention shift propagates uncapped. No alternative exists. | 160d frozen; CC BY 4.0 verified (a25); under-registration falsified (E1). Tails: RF-16 refresh-shock, RF-20 licence change, RF-29 permanently dark. | **ACCEPTED (structural)** — multi-factor adoption gate at every resumption (diff all anchors); licence verification kept current; alternative-source plan activates only at RF-29. |
| **R23** | ✅ | **Doc-vs-code gap (#58): the §19 tower-pair gating was documented but NEVER in code — CLOSED-at-discovery 2026-06-11 (Sprint 2.22.0b.22, Heroku v191).** CLAUDE.md/Project_Instructions §19 stated «if asset_type in TOWER_LIKE_TYPES and unit_count and per_unit_rent» since Sprint 2.16.10; the shipped code multiplied the pair UNGATED for ANY asset — on a villa the pair became a laundered «إيجار فعلي» → b6 income_led 11.2M vs the signed 2.4M cost-led, with a UI stale-leak making it reachable with zero typing. | Phase-0 `docs/PHASE0_income_types_exposure.md` §4 (measured live on v190: 54/541/6 + 12×5,000 → 11,200,000 income_led; the derivation at `evaluate_unified.py:3823` pre-b22 had no asset gate; `applyAssetToForm` ran only from the CTA; the payload builder sent hidden values). | **✅ CLOSED by b22** — pure `_derive_rent_from_unit_pair` + `_TOWER_PAIR_ASSETS` allowlist (incl. compound_small — the address-entry large compound quick-classifies there) + verbatim ignore-disclosure + UI syncTowerPair clear-on-non-tower + the satisfied-«يتطلب» line drop. Live v191: villa+pair = 2.4M cost-led + the flag; apt/compound/bare byte-identical. The §19 doc is now TRUE in code. Residual watch: the same anti-pattern (documented gate ≠ coded gate) in future input fields — recon must grep the gate, not trust the doc (#58). |
| **R22** | 🟡 | **DRC calibration-bias import.** If the §20.9 cost build-rate is calibrated to the MoJ median, the "independent" second method re-imports R7's bias (E12-class trap). | Any DRC parameter sourced from moj_reference instead of a construction-cost anchor; DRC tracking the market median too closely by segment. | **OPEN — discipline documented**: pure DRC (real cost anchors, e.g. Al Manara ~2,380 ر.ق/م² premium), [land_floor, cost] rail, no median calibration; enforce in the §20.9 brief's §5 audit (Gate-2). |

---

*Seeded 2026-05-30. Owner: Anas. Review cadence: each sprint close-out.*
*Living layer = RISK_SUMMARY.md (sprint-close cadence); deep snapshot = RISK_REGISTER_v2.md (stage-gate re-issue only).*
