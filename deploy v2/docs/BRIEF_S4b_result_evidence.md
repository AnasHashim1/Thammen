# BRIEF — Sprint 2.22.0b.125 (S4b): «أدلّة النتيجة» — result-screen evidence redesign

> **جاهز للتنفيذ.** الاستطلاع الكامل تمّ 2026-07-10 (جلسة ما بعد نشر v288/b124). هذا المستند = مخرَج ذلك الاستطلاع كي تُبنى S4b في جلسةٍ طازجة بلا إعادة عمل. اقرأه بعد مصافحة #57.

## 0) الحالة عند البدء (متوقَّع — تحقّق بـ#57)
- الحيّ: **b124 / Heroku v288** · `/api/health` = `thammen-sprint2p22p0b124-redesign-result-hero`.
- git anchor: `51162e7` · `master == origin` (0/0).
- بوّابة البايت الخماسيّة (الثابت الحاكم): 54/541/6=**2.4M** cost_led · 56/647/6=**3.8M** geo_full · 55/296/13=**2.6M** e25_capped · 56/565/21=**2.4M** matched · 52/903/90=**رفض**.
- المُنجَز من برنامج redesign-v2: **S0 (b120) · S1 (b121) · S4a (b124)**. **مُتخطّى:** S2 (نبض — يحتاج `/api/pulse`) · S3 (موافقة — محجوب على قرار المالك PDPPL).
- الخطة الأمّ: `C:/Users/ans_h/.claude/plans/temporal-honking-tiger.md`.

## 1) مصدر التصميم (الحقيقة التي نُعيد إنشاءها — لا نخترع)
- الحزمة: `deploy v2/# خريطة ملفات التصميم الاخير.zip` (الأحدث). استخرجها:
  ```bash
  export PYTHONIOENCODING=utf-8
  python -c "import zipfile,os; d=r'<scratch>/design'; os.makedirs(d,exist_ok=True); zipfile.ZipFile('# خريطة ملفات التصميم الاخير.zip').extractall(d)"
  ```
- **نموذج S4b = `design_handoff_thammen/ثمّن - شاشة النتيجة.dc.html`** (264 سطراً) + لقطة `screenshots/02-النتيجة.png`.
- أجوبة المصمّم: `ANSWERS_to_claude_code.md` · خريطة الملفّات: `NEW_DESIGN_FILE_MAP.md`.
- **نطاق S4b = كلّ ما بعد البطل** (البطل = S4a مُنجَز): من `<!-- EVIDENCE — COMPARABLES -->` (نموذج س94) حتى شريط الإجراء الثابت (س232).

## 2) خريطة البناء — قسمٌ بقسم (نموذج ← حقلٌ مبثوث ← بُنية قائمة في `index.html`)
البنية الحاليّة **مطويّة/أكورديون** (b15/b31/b34/b52) → النموذج **مسطّح ينكشف بالتمرير** (`data-reveal` + IntersectionObserver). أعِد بناء النصف السفليّ من `show()`.

| قسم النموذج (سطر) | الحقل المبثوث | البُنية/السطر القائم |
|---|---|---|
| **الأدلّة — صفقاتٌ مقارِنة** (h2 س97) + «37 معاملة · 28 خلال 24ش» (س98) | `v.comparables.window_*`/عدّ الصفوف | كَيستون b38 |
| **موقع تقديرنا بين الصفقات** — شريط كحليّ (س103-115): علامة التقدير 5,333 بين min 4,667 / max 5,849 | `v.comparables.rows[].price_per_m2` (min/max) + تقدير المتر = `amount/area` أو وسيط الصفوف | جديد (viz) من `_kc` س2660 |
| **جدول 6 صفوف** (التاريخ·المساحة·السعر·ر.ق/م²، س119-127) + `data-comp` تدرّج | `v.comparables`/`v.considered_comparables.rows` | `_repComparables` **س2031** — يُعاد تنسيقه للشبكة `1.1fr 1fr 1.2fr 1fr` |
| **تذييل الجدول** (س128): «عرض N من M · بيانات وزارة العدل (CC BY 4.0) — مجموعة عامّة بلا عناوين فرديّة» + «عرض الكل في التقرير ›» | ثابت امتثاليّ + `openReport` | — |
| **شرارة الاتّجاه** (س131-139): «اتجاه سعر المتر · 2020–2025» + شارة «مستقرّ» | `_repTrend` **س2064** | يُعاد تنسيقه SVG polyline |
| **كيف وصلنا للرقم** (h2 س144) + شارة «تقارب قويّ · فارق %» (س145) | `v.reconciliation.spread_pct` — **يظهر حين التقارب قويّاً فقط** (جواب ١٤؛ عتبة المحرّك)؛ حين يضعف → حالة تباعد لا الرقم | جديد |
| **مكدّس ثلاثيّ** (السوق «الأساس المعتمد» / التكلفة DRC / الدخل، س148-165) | `v.value_stack.market` + `v.value_stack.cost` (label/value/sub) + `v.income_triangulation`/`income_approach` | مبثوث b20؛ نمط `_repDef12`/`how` س3247 |
| **سرديّة القيادة** «الرقم يقوده السوق…» (س166) | `v.leadership.note_ar` عبر `pick()` | **س3244** |
| **ماذا لو؟ أثر الحالة والتشطيب** (h2 س171) + فقرة «لا تُغيّر التقدير المعتمد» (س173) | ثابت | — |
| **4 بطاقات سيناريو** (كما هي/ترميم/فاخر/هدم، س174-179) | `v.scenarios.items[]` (b23) | س2233 (تقرير) / س2714 (مختصر) — أعِد استخدام المنطق |
| **سطر الصدق تحت السيناريوهات** «تصنيفٌ استدلالاً بالسعر المسجَّل، لا معاينةً للتشطيب» | ثابت امتثاليّ (**جواب ٨ — إلزاميّ**) | b100/b113 |
| **حدود هذا التقدير** — `<details open>` (س183-214): «تحفّظ متوسط» chip + «ما لا نراه بعد» + «أسئلةٌ قبل الشراء» + سطر RICS | MUC (`_mucCardHtml` س1799) + known-unknowns + `accuracy` + «دون تسويةٍ زمنيّة» (جواب ٤) + `VPGA 10/VPS 6/IVS 106` | b52 MUC fold |
| **تذييل الإخلاء** (س216-220): «ثمّن تحليلٌ معلوماتيّ…» + `report_ref` | ثابت + `v.report_ref` | قائم |
| **شريط إجراء ثابت** (س224-232): «القيمة: {amount} ر.ق» + [حسّن التقدير] [التقرير المختصر] [التقرير الكامل] | حسّن→`go('refine')` · مختصر→`showShortReport`+`go('shortReport')` · كامل→`openReport()` | TIER-3 قائم |

## 3) الامتثال — يُحفَظ verbatim (كل سبرنت يُثبت: صفر تأكيدٍ أُضعف — R6/Lesson-2)
«ليس تقييماً معتمداً» · **CC BY 4.0** (تذييل الجدول) · «×0.90 ليست تصفية» · **صدق الكلفة b64 + e25 b72** (في البطل — لا تُمَسّ) · **«فوق ٥ مليون» شرطيّ** (جواب ١؛ `amount>5,000,000`) · **«دون تسويةٍ زمنيّة»** (جواب ٤، في الحدود) · **«استدلالاً بالسعر لا معاينةً»** (جواب ٨، تحت السيناريوهات) · MUC (`VPGA 10/VPS 6/IVS 106`) · «بانتظار مراجعة مُقيِّم مُرخّص» · هويّة b54 «التقييم السوقي» + القيمة «استرشاديّة» · «عدم اليقين الجوهري» (b105).

## 4) الثوابت الحاكمة
- **محايد القيمة**: المحرّك و`api.py` **لا يُلمسان** (S4b لا يحتاج `/api/pulse`). بوّابة البايت الخماسيّة مطابقة بايت. الحساب الوحيد المسموح للواجهة = `_srPayment` + ×0.90 + ×1.10/×1.30 — **لا مكان يُعدِّل `amount`**.
- **المبدأ الحاكم للمصمّم**: كل رقمٍ يظهر من حقلٍ حقيقيّ مبثوث أو يُحذف (لا 48/60% ولا ±8% مخترعة). مؤشّر الثقة من `accuracy.score` فقط (يُخفى إن غاب — S4a أنجزه).
- **البدائيّات جاهزة (S0)**: `_countUp` (س2813) · `_revealOnScroll` (س2830) — أعِد استخدامها؛ كلاهما يحترم `prefers-reduced-motion`.
- **الإنجليزيّة حيّة**: كل نصٍّ جديد `t('عربي','English')` (النبرة معتمدة من `Thammen - EN Reference.dc.html`)؛ تجاوزات LTR (`body.lang-en #resultsScreen …`) تُمدَّد للحاويات الجديدة.
- **`.src-credit`**: `showReport` يستنسخها من DOM النتيجة (س2289) — لا تحذفها من النتيجة (تكسر الإسناد القانونيّ في التقرير صامتةً).

## 5) مواضع `show()` الحاليّة (للجراحة الدقيقة)
- `function show(d)` = **س2850**؛ `t1` (البطل، مُعاد في S4a) ~2900-3220؛ سطر e25 س3203؛ سطر cost-basis س3195؛ عرض «9-note parade» → `how` س3223+؛ المكدّس/السرديّة س3244-3261؛ الكَيستون `_kc` س2660 + `considered` س3306+؛ السيناريوهات؛ MUC fold (b52)؛ تجميع `o.innerHTML` + خطّافات `_countUp`.
- بُناة يُعاد استخدامها: `_repComparables` س2031 · `_repTrend` س2064 · `_mucCardHtml` س1799 · `_decompHtml` س1810 · `_acc` س1401 · `_evOneRow` س1407 · `evidencePanelHtml` س1384.

## 6) إعادة توجيه المثبّتات (متوقَّعة — grep-تأكيد كلٍّ قبل التعديل)
النصف السفليّ للنتيجة مثبَّت في: **b15 · b31 · b34 · b38/b39/b40/b41 (الكَيستون) · b52 (طيّ MUC) · b19/b20 (value_stack/leadership) · b23 (scenarios) · b37 (آليّة الكلفة) · b64/b72 (الكلفة/e25) · b100/b113 (الشرائح/الحالة) · b124 (S4a)** + `calc_visual` في المجمّع. كل re-point بنيويّ/تجميليّ فقط — **يُمنع إضعاف امتثال/قيمة/منهجيّة**.

## 7) التحقّق (إلزاميّ)
```bash
python -m py_compile evaluate_unified.py api.py
node --check <inline-js>
PYTHONIOENCODING=utf-8 python test_sprint_2_22_0b125.py            # جديد (E14)
PYTHONIOENCODING=utf-8 python run_sprint_2p22p0a_suite.py          # 395/395 MATCH (calc_visual!)
PYTHONIOENCODING=utf-8 python test_sprint_2p16p17_security.py      # 16/16
PYTHONIOENCODING=utf-8 python test_sprint_2p22p0a3_surface_honesty.py  # 45/45
PYTHONIOENCODING=utf-8 python 2p22p0_pre/run_regression_2p22p0a.py     # ALL GREEN
```
- **R14 (Chromium 390×844 + سطح المكتب، 7 حالات قيادة)**: كلفة/سوق/جغرافي/دخل/e25/أرض/رفض — القيمة بايت · صفر console · لا فيضان · كل سطور الامتثال حاضرة · الكَيستون/الاتّجاه/السيناريوهات/الحدود تُكشف بالتمرير · الرفض بلا أدلّة مخترعة.
- **الدخان الحيّ (browser-UA #61)**: `/api/health`=b125 + بوّابة البايت الخماسيّة مطابقة.

## 8) النشر (#43 — origin أولاً)
```bash
git push origin master
git -C "C:/Thammen" subtree push --prefix "deploy v2" heroku master
```
كل نصٍّ جديد على المحامي واللغويّ (توجيه المالك الدائم).

## 9) بعد S4b
S5 (لحظة الكشف b126) · S6 (الإدخال b127) · S7 (المختصر b128) · S8 (الكامل b129) · S8ب (الشروط+الكلفة b130) · S9 (الموبايل b131) · S10 (الوصوليّة b132) · S11 (الإنجليزيّة b133). **المؤجَّلان بقرار المالك:** S2 (نبض `/api/pulse`) · S3 (موافقة PDPPL).
