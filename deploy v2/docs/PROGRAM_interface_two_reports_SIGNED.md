# برنامج «الواجهة والتقريران» — التوجيه الموقَّع (م0–م4) + حالة التنفيذ

> **مصدر النص:** رسالة أنس الموقَّعة 2026-06-12 (محفوظة بـ Rule #63 — هذا الملف = مرساة ما بعد أي compaction).
> **الخطة الحاكمة:** `docs/PLAN_short_report_rollout_v1.1.md` (نص أنس الأصلي حرفياً منذ صفر-أ) ·
> **المصفوفة:** `docs/MATRIX_short_report_copy_SIGNED.md` · **عقد الويب:** `docs/index_mockup_full_journey_v3.html`
> (الشاشات: ١-٥ الرحلة · ٦-٧ المختصر · ٨ العقد البنيوي للكامل v2). **عقد الطباعة وصل من الـPO وأُلزم:**
> `docs/ثمن_التقرير_المختصر_v2_امريخ.pdf` — **b28 طابقه** (CHANGELOG_v111، حية v199).

## كلمات الـPO الثلاث (مسجَّلة 2026-06-12 — تُغلق الأسئلة المعلَّقة)
1. **«أقرّ الترقيم»** — بقراءة CC المنفَّذة: **صفر استشهاد قديم كمعيار حالي؛ سطور خريطة 2025 (VPS 3/IVS 103 مناهج · VPS 5/IVS 105 نماذج · VPGA 10 · VPS 6/IVS 106 · VPS 2/IVS 102) التاريخية/الحية تبقى.** سؤال #54 المعلَّق في v107/v109 §8 → **مُقفل بإقرار الـPO**.
2. **«أبقِ بطل العقد»** — بطل المختصر = «قيمة بيتك التقديرية اليوم» (نسق الـPDF)؛ «مرتكز التكلفة (أرض + بناء مُهلَك)» يبقى في **سطر الأساس**. تجاوز بطل المصفوفة → **موقَّع**.
3. **«أقرّ القسط الدقيق 10,672»** — سطر التمويل يعرض الإطفاء الدقيق لا التدوير التحريري (≈10,700) — **موقَّع**.

## حالة D6 (قياس أمين، للجلسة القادمة)
**شاشة النتيجة b15 ما زالت الوجهة الأولى بعد التقييم** (المسار: تأكيد → تحسين → نتيجة)؛ «المختصر يفتح أولاً» مُنفَّذ **عند مدخل التقرير**: زر TIER-3 «📄 التقرير المختصر» يفتح المختصر أولاً والكامل على بُعد نقرة داخله. جعل المختصر **هبوطَ ما-بعد-التحسين الافتراضي** (قراءة D6 الكاملة: «الافتراضي بعد التقييم») = شريحة منتج مستقلة بقرار PO إن أرادها.

## حالة التنفيذ — **البرنامج البنائي مكتمل (BUILD-COMPLETE 2026-06-12)**
- **صفر-أ ✅** توفيق الخطة (`d7a7878`).
- **م0 ✅ = Sprint 2.22.0b.24 · Heroku v194** (§20.58، CHANGELOG_v107). بند الترقيم: محقَّق قياساً؛ القراءة الحرفية «صفر VPS 3/IVS 105» مرفوضة بـ#54 (v107 §8) — تحتاج كلمة PO صريحة إن قُصدت حرفياً.
- **م1 ✅ = b23/v192** (سلفاً). بندها المفتوح في سلة الدخان.
- **م2 ✅ = Sprint 2.22.0b.25 · Heroku v195+v196** (CHANGELOG_v108) + **جولة مطابقة عقد الطباعة b28** (CHANGELOG_v111 — العقد وصل من الـPO وأُلزمت نسخته في docs؛ المختصر أعيدت صياغته على صفحتيه بربط بثّي كامل).
- **م3 ✅ = Sprint 2.22.0b.26 · Heroku v197** (§20.60، CHANGELOG_v109 — جراحة D8 + D4 + إصلاحات تدقيق الـPDF الأربعة).
- **م4 ✅ = Sprint 2.22.0b.27 · Heroku v198** (§20.60، CHANGELOG_v110 — هوية الشاشات الخمس + المجموعات الثلاث الموسومة + معادلة الارتدادات).
- **سلة الدخان المؤجل ✅ مُصرَّفة بالكامل 2026-06-12 مساءً («أعد الدخان» نُفِّذت)** — khazna متعافية (health: primary_alive، 162,516)؛ الدفعة الخمسية = **14/14 PASS** + /verify ✓/✗ + جولات Chromium بالحمولات الحية (التأكيد الواعي بالقائد · المختصر على القادة الأربعة · الكامل v2 · تدفق البرج). **كل المراحل م0–م4+b28 مقفلة نهائياً.** السجل: Session_Log §20.61 + .basket/. البند الورقي الوحيد المتبقي = مسح QR من ورقة مطبوعة (خطوة Anas اليدوية؛ هدف المسح — عنوان الـQR نفسه — مُثبَت ✓ حيّاً عبر GET).

## م3 · C «الكامل v2» — التوجيه الموقَّع حرفياً
برومبت §6-C (من الخطة الأصلية) + خريطة D8 (§2-C الأصلية «كما هي مكتوبة») + **بند D4 الجديد: استبدال بند
«اضطراب 28-02-2026» بصياغة قِدم-بيانات عامة مربوطة بالـbanner** + امتصاص إصلاحات تدقيق الـPDF بنيوياً:
1. تسميتا «الوسيط» العمياوان على رقم كلفة-يقود (showReport: «الوسيط (التقدير المركزي)» + «القيمة السوقية (الوسيط)» في DEF-12 — تصيران واعيتين بالقائد).
2. تناقض لوحة MUC (متوسط ظاهراً / high مبثوثاً) — اللوحة تقرأ `material_uncertainty.level` المبثوث.
3. مقاييس الحوض المختلط (n=3 بجوار 0.165) — سطر الرسوب المزدوج «مطابق n / جغرافي n+تشتت».
4. IVS 105 → خريطة 2025 (⚠️ #54: التبنّي الحرفي يحتاج كلمة PO — راجع v107 §8؛ عقد الشاشة ٨ يقول «لا VPS 3 ولا IVS 105»).
**بوابته:** مطابقة عمود المختصر بصرياً. خريطة D8 (من الخطة §2-C): يُدمج تحفظ ص1+ص9 → كتلة واحدة بعد الرقم ·
المنهجية ص2 + معايير ص9 → ملحق واحد · تفكيكا ص3+ص4 → واحد (كلفة-تقود: أرقام DRC مباشرة لا متبقٍّ) ·
يُحذف/يُطوى: قسم الإعلان الفارغ · نثر «حالة أفضل/أدنى» (السيناريوهات تجيبه) · التدويرات الثلاث للكلفة → تدوير
واحد معلن · يبقى ملاحقَ مرقمة: التصنيف الطبقي · جودة الأدلة · ما لا نعرفه · أسئلة الشراء · مصدر الرسملة · الإسناد.
بنية الشاشة ٨ (ص1 الزبدة الموسّعة · ص2 الأدلة والحوض · ص3 التفكيك والمنهج · ص4 السيناريوهات والحساسية ·
ص5 الأساس القانوني والقيود · ص6 الملاحق والتحقق — ~6 صفحات على عمود المختصر، مكوّنات thm-report).

## م4 · هوية الشاشات الأولى — التوجيه الموقَّع حرفياً
إلباس الشاشات 1–5 مكوناتِ thm-report من B وفق عقد v3 + إعادة تجميع شاشة التحسين إلى المجموعات الثلاث
الموسومة (يحرّك التقدير / يدقّق مرتكز التكلفة / اختياري للإثراء) + معادلة الارتدادات تحت مساحة البناء
(أمامي 5 · جانبي 3 · خلفي 3 · سقف 60%).

## عقد التسلسل (غير قابل للتفاوض، من الرسالة)
كل مرحلة سبرنت كامل الحقوق: fixtures الـ22 بايت-مطابقة على amount/low/high/rule · اختبارات معزولة · DoD
كامل · R14 ‏390×844 · CHANGELOG + ENGINE_VERSION لكل مرحلة · deploy على أخضرها قبل التالية + git push origin.
توقف-وبلّغ فوراً عند: كسر حصر · سؤال Gate-2 خارج الموقَّع (مفاجآت خريطة D8 خاصة) · اقتراب compaction
(#64: أغلق وثائق المرحلة الجارية واكتب handoff ثم استأنف).

## استثناء البنية (khazna/R5)
المراحل عرضية/إضافية لا تلمس قيم المحرك ⇒ الضمان البنيوي يكفي للنشر على البوابات المحلية (نمط b23:
معزولة + DoD + R14 بحمولة محقونة). كل دخان حي يتطلب evaluate يُجمع في «سلة الدخان المؤجل» وتُشغَّل دفعةً
فور التعافي («أعد الدخان») — ولا تُعتبر أي مرحلة مقفلة نهائياً قبل سلتها.

## سلة الدخان المؤجل — القائمة الخمسية — ✅ مُصرَّفة 2026-06-12 (التفصيل: Session_Log §20.61)
> **نُفِّذت دفعةً واحدة فور التعافي: 14/14 PASS (بوابة البايت الرباعية مطابقة بالكامل — وأُعيد برهانها على v200 بعد نشر b29) + /verify ✓ على fp حقيقي و✗ على مزوّر + جولات Chromium الحية للبنود 2–5. البند الورقي (مسح QR من ورقة) = خطوة Anas؛ عنوان الـQR مُثبَت ✓.**
1. **م1/b23:** دخان القيمة على الـ4 fixtures (بايت-مطابقة amount/low/high/rule) + جولة `report_fp` لتقرير حقيقي عبر `/verify`.
2. **م0/b24:** جولة `/api/evaluate` كاملة بـ `audience=owner` + تسمية شاشة التأكيد الواعية بالقائد على حمولات حية (امريخ كلفة-تقود · أبو هامور مطابق).
3. **م2/b25+b28:** المختصر على القادة الأحياء الأربعة (كلفة/مطابق/دخل/أرض) + جولة مسح QR→/verify من ورقة مطبوعة.
4. **م3/b26:** الكامل v2 على حمولات حية (التسميات الواعية + كتلة MUC الواحدة بعد الرقم + سطر الأدلة المزدوج + الملاحق الستة المرقمة).
5. **م4/b27:** تدفق برج حي عبر مجموعات التحسين الثلاث (كشف towerRentSection + التركيز يفتح المجموعة المطوية).
