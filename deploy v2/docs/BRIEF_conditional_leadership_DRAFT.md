# BRIEF — Evidence-Conditional Leadership + three-value stack (🔴 Gate-2 VALUE-AFFECTING)

> **STATUS: DRAFT — PARTIAL SIGN (adjudication v2, 2026-06-11).** Anas signed the E25 rail (+ double-weak
> clause) and forks **F1 (amended unified rule) · F2=B · F3=(b) · F4=FLIP · F5=fail-safe COST**.
> **The BINDING F6 signature (the final flip number) is RESERVED** — the final recompute under the signed
> rule-set = **7/13 = 54% cost-led** (`PHASE0_conditional_leadership.md` §2.7). Build is HELD until F6 is
> signed. Live = b18/v187 untouched; b19 separate as signed.

## §1 Premise (signed direction, this brief = the encoding)

The engine's headline today is market-led by default; cost (DRC) leads/floors only on narrow gated slices
(b11/b13/b16). The PO direction: **the three-value stack renders everywhere; leadership is decided by
EVIDENCE QUALITY, not by path history** — the median is **TESTED per case, never trusted nor banned by
decree** (E23 + «الوسيط الخادع» operationalized). Legacy anchors' recorded expectations are retired as
methodology truth (re-baseline post-ship); **V001/TD-93317 = the sole calibration anchor** (±1% standing test).

**SCOPE (signed §C):** villas + houses + **land** only. Compounds/towers stay on the income path — outside
this sprint.

## §2 Three-value stack (display contract — every valued villa/house response)

1. **Market Comparison** — وسيط «حوض المقارنات» + n + window + **التشتت الرقمي (يُكشف لأول مرة — Phase-0 G2)**
   + الطبقة المسيطرة وحصتها. On cost-led surfaces the median + P25–P75 spread **remain displayed** (F4 —
   a failed median is shown, never hidden).
2. **Cost (DRC)** — label verbatim (signed B-d, unified with b19):
   **«قيمة التكلفة (أرض + بناء مُهلَك) — نهج DRC»** + **«استرشادي، مُعايَر على تقييم معتمد واحد (V001)»**.
   Full component breakdown (already computed by `_cost_approach_value`; today discarded on most paths —
   Phase-0 G1). Defaults per §6.
3. **Income** — unchanged (renders when it computes; leads when it fires — §3).

Paths: villa/house = full stack · **raw_land = market + سطر واحد (DRC ≡ قيمة الأرض)** · compounds/towers =
unchanged income/hybrid surfaces (no RCN basis — G3) · refusals stay refusals (G4).

## §3 Leadership — the SIGNED unified rule (F1-amended) + rails

Precedence: **income_led (unchanged, §20.40 circularity guard intact) → the conditional gate → refusal.**
For villa/house with a computed market value:

```
RULE 1 — matched-stratum pool:
   market LEADS iff [matched-stratum n >= 10] AND [disp36 < 0.30] AND [stratum match]
   (subject band = the E26 system-age band — the G5 proxy)
RULE 2 — UNMATCHED geo-full pool, reliable bar only:
   market LEADS iff [n_geo_full >= 20] AND [dispersion(geo_full) < 0.30]
   + disclosure «حوض جغرافي غير مطابق طبقياً» + MUC raised ONE notch + the cost line/floor (b19)
ELSE (thin OR dispersed OR untestable) — COST LEADS:
   range = F3(b): [cost … market-muted]  (b11 pattern — market median visible, no invented ceiling)
   MUC high · «قيادة كلفة استرشادية — حوض المقارنات لم يجتز اختبار الموثوقية (n={n}, تشتت={d})»
```

**F2 = B (signed):** re-surveyed stock (`vintage_capped` + sys-age < 2) → the band is untestable → RULE 1
cannot pass → adjudicated by RULE 2, else cost — + disclosure verbatim **«عمر مُعاد تسجيله — غير موثوق»**.

**F4 = FLIP (signed):** dispersed (a14) pools → cost-led per the unified doctrine — falls out of the gate
mechanically (disp36 ≥ 0.30 fails RULE 1; their geo-full pools also failed in measurement). The P25–P75
spread + median stay displayed in the stack. *Interaction: SUBSUMED by F1 — no separate mechanism.*

**F5 = fail-safe COST (signed):** strata-absent ⇒ untestable median ⇒ cost leads (the F2 precedent).
*Interaction: SUBSUMED by F1's else-branch («untestable») — no separate mechanism.*

**Hard rails (non-negotiable):**
- **E25 RAIL (LOCKED):** cost **NEVER leads upward** (المعراض = the proof: cost 3.74M > market 2.6M).
  When `cost >= market` the **market figure remains the anti-inflation cap**; **double-weak clause:** if the
  market pool itself FAILED the gates → market keeps the lead **BUT** with a mandatory divergence line +
  **MUC ≥ high**.
- **E26 rail:** every LEADING «معامل احتفاظ» uses the SYSTEM age; user age stays a sensitivity line (b18 §A1).
- **E24 rail:** vintage-capped ages are floors; the cliff-flag disclosure stays mandatory.
- **No invented central:** the cost-led figure is the computed DRC, never a blend point.

### §3.1 Precedence reconciliation — subsumption mapping (no double-fire)

| today's branch | under the signed rule-set | fires when |
|---|---|---|
| income_led (b6/b7/b8) | **UNCHANGED — first, above the gate** | grounded subject rent + calibrated cell |
| **the conditional gate (NEW)** | the single decision point: market(matched) / market(geo-full + disclosure + MUC+1 + cost floor) / **cost-led F3(b)** | every valued villa/house |
| cost_reanchor_down (b11) | **SUBSUMED** — its trigger set (OLD over-anchored thin/widened, undercut>30%) lands in the else-branch; same F3(b) shape, now with the COST as leader not just floor | retired as a separate branch |
| widen_down (b6) | **SUBSUMED** — no-rent over-anchored thin lands in the else-branch; the cost floor supersedes bare land | retired as a separate branch |
| old_stock_reanchor (b16) | **SUBSUMED** — its geo-full basis pool must now pass RULE 2's reliable bar; امريخ's pool (n=51, disp 0.62) FAILS → cost leads | retired as a separate branch |
| cost_trim → age_sensitivity (b13/b18) | **UNCHANGED** — disclosure after the chain (E26) | user age supplied |
| teardown / luxury-new (b4 opt-in) | **UNCHANGED** — explicit statements win, outside the gate | explicit input |
| E25 rail + double-weak | post-gate override on the else-branch | cost ≥ market |

One decision point; the elif chain (ct → osr → widen) retires. **No double-fire is structurally possible.**

## §4 Reconcile, not blend (+ signed addenda B-a/b/c)

The three values are never averaged. One leads (per §3), the others render as disclosed cross-checks with
their own n/confidence. `reconciliation` stays a status reporter.

- **(a) INVARIANT — hard test (Fannie-Mae-style):** the headline ALWAYS lies within
  **[cost-informed floor … market figure]** — never above the higher nor below the lower. Encoded as an
  isolated-test invariant over the full E2E cohort (build DoD §12).
- **(b) AGE-GRADED COST HONESTY:** when COST leads an OLD property, add the line verbatim
  **«تقدير الإهلاك يزداد ذاتيةً مع التقادم؛ معايرتنا على شيت مثمّن وأرضية السوق تخففه»** + MUC stays high —
  cost leads there **by elimination** (the pool failed), not by precision.
- **(c) DOCTRINE NOTE (deliberate, not oversight):** international practice permits an EXPLAINED weighted
  average across approaches; Thammen adopts the **STRICTER no-averaging posture** — recorded as a conscious
  methodology decision (VPS 3/IVS 103 reconciliation duty satisfied by disclosure, not blending).
- Divergence > 30% between displayed values ⇒ MUC ≥ high + an explicit divergence line.

## §5 Depreciation set (LOCKED — the calibrated numbers, no re-tuning in this brief)

| element | value | basis |
|---|---|---|
| age basis | **SYSTEM (CGIS) lead — E26**; user age = sensitivity only | b18 §A1, TD-93317 |
| curve | **50-year straight line**, «معامل الاحتفاظ» = clamp(1 − age/50, floor, 0.98) | METHODOLOGY_DRC §5 |
| condition ladder | excellent **−2** · renovated **−3** · new 0 · good +5 · **default avg +8** · fair +15 · poor/teardown +25 | b13 §4.2 |
| residual floors | **0.27** ordinary · **0.31** high/luxury (D-1) | b13 |
| RCN ladder (QAR/m² BUA) | shell **1200** · ordinary **2200** · good **2500** · high **3000** · luxury **3500** | PO web-validated §3 |
| BUA | confirmed footprint × floors, else max-buildable × **0.77** × floors (default floors 2) | b11/b10 |
| soil/geotech | **1.0 default**; sabkha/karst +5–15% = **v2 (GIS layer)** — NOT in this build. «معامل الإحلال» stays RESERVED for this soil factor (METHODOLOGY_DRC §4) — never used for retention | directive |

**Terminology (signed §C, corpus-wide):** «حوض المقارنات» / «عيّنة الصفقات» — never «مجمع» (collides with
residential compounds) · «معامل الاحتفاظ» = retention/depreciation · «معامل الإحلال» = soil only.

## §6 Universal DRC computation + defaults

Compute the DRC on **every valued villa/house** (it already computes — emission is the change, Phase-0 G1)
with the §5 defaults (ordinary + average + system age) when inputs are absent, and EMIT it in the stack with
its assumptions disclosed («افتراضات: تشطيب عادي · حالة متوسطة · عمر النظام {N}») + the refine CTA. When the
DRC is input-starved (no land floor / no system age / no footprint — Phase-0 §1.3) the stack shows
«قيمة التكلفة غير متاحة — {السبب}» rather than silence.

## §7 Calibration — V001 sole anchor (±1% standing test)

The TD-93317 reproduction stays the ONLY value-anchored test: land 2,456,345 (350/ft²) ≈ engine MoJ floor
2,456,736 (+0.016%) · building 602×1,900 ≡ RCN_high 3,000 × «معامل احتفاظ»(18,'high')=0.64 · MV 3,600,145
±1% (measured this pass: **+0.35% / +0.34% PASS**). ⚠ Basis = **RAW system age + finish=high + penalty 0**
(E26/b18) — NOT the §6 display defaults; the test pins the basis explicitly (`test_sprint_2_22_0b18.py`
§B carried verbatim). n=1 calibration disclosed on every cost-led surface; the GT kit (D-3) is the
tightening channel.

## §8 Honesty rails

cite-n everywhere · cost-led = «استرشادي» + MUC high, never «معتمد» · E1/E3 intact (no listing uplift; cost
never chases ASK — E25 as rewritten 2026-06-10) · «📌 تقدير سوقيّ آليّ — ليس تقييماً معتمداً» unchanged ·
a17/a19 condition caveat + b12 HBU + B-1 floor surfaces preserved · the dispersion numeric + gate verdict
EMITTED (auditability) · the geo-full lead carries its disclosure + MUC notch (RULE 2) · the re-survey
disclosure (F2) verbatim · the age-graded cost-honesty line (§4-b) on old cost-led surfaces · refusal
honesty unchanged.

## §9 Legacy-anchor retirement + re-baseline

Per Phase-0 §4: scratch live-expectation files RETIRED; offline input-fixtures survive; the precedence-chain
isolated tests (b6/b7/b8/b11/b13/b16/b18) re-pointed at build time per §3.1's subsumption map; post-ship the
SAME 22-case cohort is re-captured and fresh byte-identical guards are written, labeled **engineering
fixtures only**. V001 ±1% = the only surviving value anchor.

## §10 Out of scope

compounds/towers RCN basis (G3) · soil v2 GIS layer («معامل الإحلال») · the GT-collection track itself (D-3,
parallel) · b19 (separately signed; executes regardless) · any threshold re-tuning (the §3/§5 numbers ship
as-is) · frontend redesign beyond the stack rendering (screens 4/5 absorb the stack — copy-level edits only).

## §11 Fork register — adjudication v2 (2026-06-11)

| # | fork | **DECISION** | measured note |
|---|---|---|---|
| F1 | matched-only vs geo-full | **SIGNED — AMENDED UNIFIED RULE** (§3: RULE 1 + RULE 2 reliable bar + else cost) | امريخ geo-full **n=51, disp 0.62 → FAILS** → cost 2,378,094 (today OSR 3.4M). Geo-full RESCUED: V001 (n=22, 0.203) + V002/V003 (n=54, 0.212) |
| F2 | re-survey band | **SIGNED — B** (untestable → RULE 2 else cost + «عمر مُعاد تسجيله — غير موثوق») | V002/V003 → RULE 2 rescue (market, disclosed) |
| F3 | cost-led range shape | **SIGNED — (b)** b11 pattern `[cost … market-muted]` | kills the [0.96M…1.10M] false-precision shape on 54/788/10 |
| F4 | a14 dispersed pools | **SIGNED — FLIP** (cost-led; spread + median stay displayed) | subsumed by F1; 3 cases (51/825/22 · 51/833/37 · 53/736/4) — geo-full also failed (0.385/0.569/0.356) |
| F5 | strata-absent | **SIGNED — fail-safe COST** | subsumed by F1 else («untestable»); 3 cases, geo-full n=0/3/1 |
| E25 | upward inversion | **SIGNED — RAIL LOCKED** + double-weak clause (divergence + MUC ≥ high) | المعراض: market 2.6M keeps the lead vs cost 3.74M |
| **F6** | **the final flip number** | **⏳ RESERVED — awaiting Anas** | **FINAL = 7/13 = 54% cost-led** (2 matched-market · 3 geo-full-market · 1 E25-capped) · pure flips 4 + امريخ OSR→cost + 2 floor→lead. Full table: PHASE0 §2.7 |

## §12 Build-sprint DoD (when F6 signed)

Isolated tests for the gate fn (RULE 1 × RULE 2 × else × F2/F4/F5 fail-safes × the E25 rail incl. the
المعراض inversion + double-weak case) · **the §4-a INVARIANT as a hard test over the full E2E cohort**
(headline ∈ [cost floor … market]) · siblings re-pointed (§3.1 map) · the V001 ±1% test green · DoD
aggregator/security/surface/broad walk · local E2E on the 22-case cohort (expected-moves table = PHASE0
§2.7) · R14 Chromium 390×844 (stack + disclosures render, no overflow) · two-lane live smoke ·
post-ship re-snapshot per Phase-0 §4.2 · docs-close.

> **القرار المطلوب (المتبقي الوحيد):** التوقيع المُلزِم على **F6** — الرقم النهائي **7/13 = 54%** قيادة كلفة
> (وامريخ تتحرك 3.4M → 2,378,094 لأن حوضها الجغرافي n=51 بتشتت **0.62** يفشل اختبار الموثوقية الذي وقّعتَه).
> بدون توقيع F6 لا يُبنى شيء.
