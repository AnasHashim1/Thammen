# CHANGELOG v201 — Sprint 2.22.0b.121 «الهبوط» (S1, redesign v2 landing)

**Engine:** `thammen-sprint2p22p0b121-redesign-landing` · **api-health:** `3.1.0-sprint2.22.0b.121`
**Files:** `index.html` (hero + CSS) · `evaluate_unified.py` (version lines) · `test_sprint_2_22_0b121.py` (new)
**Date:** 2026-07-10

🟢 **FRONTEND-ONLY / VALUE-INVARIANT.** الـHTML/CSS للبطل فقط؛ المحرّك و`api.py` **لم يُلمسا**. بوّابة البايت الخماسيّة مطابقة بالبناء.

---

## 2) لماذا يهمّ

الشاشة الأولى (S1 من برنامج إعادة التصميم). نموذج المصمّم يرفع بطل الهبوط: شعارٌ شفّاف بلا الصندوق الكريميّ، kicker برونزيّ، سطر إقرارٍ مدمج تحت الزرّ، وشارات ثقة. الهبوط الحاليّ (b118/b119) كان قريباً بنيويّاً أصلاً — فالتعديل مركَّزٌ على `.lp-top` وحده. المبدأ الحاكم (توجيه المالك): **كل شيء لين** — بؤرةٌ واحدة، بلا صخب.

## 3) الجذر

- الهبوط الحاليّ يستخدم `logo.png` (الكريميّ) الذي يُظهر صندوقاً على الخلفيّة الورقيّة.
- ينقص البطل: الـkicker، سطر الإقرار المدمج، وشارات الثقة — كلّها في نموذج المصمّم.

## 4) ماذا يفعل هذا الـpatch (`.lp-top` فقط)

1. **`logo.png` → `logo_t.png`** (الشفّاف، من S0).
2. **kicker برونزيّ** «تقييم عقاريّ استرشاديّ · قطر» (فوق العنوان)، ثنائيّ اللغة.
3. **سطر إقرارٍ مدمج** تحت الزرّ: «بالمتابعة تُقرّ بأنه **تقديرٌ استرشاديّ** لدعم القرار لا تقييمٌ معتمد — [الشروط والخصوصية]» (رابط `openTerms()`). ثنائيّ اللغة.
4. **شارات ثقة** (`.hchips`): مجاني · بلا حساب · بلا تتبّع (أيقونة صحّ خضراء `--ok`).
- **العنوان والوصف محفوظان verbatim** (قفل b24). الأقسام السفليّة (مصادر/لماذا/تغطية/أسئلة/CTA/تذييل) **لم تُمَسّ**. **بوابة الموافقة UNCHANGED** — مصيرها يُحسَم في S3.

## 5) التحقّق

- **معزول** `test_sprint_2_22_0b121.py` = **24/24** (E14: الشعار الشفّاف · الـkicker · العنوان/الوصف verbatim · الإقرار + رابط الشروط · الشارات الثلاث · البوابة سليمة · الامتثال محفوظ · صفر رياضيّة قيمة).
- **DoD:** المجمّع **395/395 MATCH** · الأمن **16/16** · صدق السطح **45/45** · **المشية العريضة 175/175 ALL GREEN**.
- **تصادم اسمٍ عرضيّ عُولج (لا R6):** استخدمتُ ابتداءً `htrust` لشارات الثقة — لكن b118 يستعمل الاسم نفسه كعلامةٍ على «الشريط الثلاثيّ القديم المُزال». أُعيدت تسمية شاراتي إلى **`hchips`** (أوضح دلاليّاً) → **مُثبِّت b118 صحيحٌ بلا تعديل** (الشريط القديم لا يزال مُزالاً؛ `'نتيجتك فوراً' not in _home`).
- **py_compile + node --check** = PASS.
- **R14 (Chromium):** 390×844 + 1280×800 — البطل يُرسَم (شعار شفّاف محمَّل، kicker، إقرار داخل الحدود، 3 شارات خضراء) · **صفر أخطاء console · لا فيضان** (docW≤winW على المقاسين) · الامتثال محفوظ على الهبوط كلّه (CC BY 4.0 ×3، «غير منتسبة»، «لا تقييمٌ معتمد»).

## 6) النشر

```
git push origin master
git -C "C:/Thammen" subtree push --prefix "deploy v2" heroku master
```

## 7) تحقّق curl

```
curl -s -A "Mozilla/5.0" https://thammen.qa/api/health | findstr b121
```
+ بوّابة البايت الخماسيّة (2.4M/3.8M/2.6M/2.4M/رفض).

## 8) ما ليس في هذا الـpatch

- نبض السوق (`/api/pulse`) = S2 · بوابة الموافقة (خانة اختيار؟) = S3 (قرار المالك) · الشاشات الأخرى = S4→S11.
- لا تغيير على المحرّك ولا القيمة.
