# Thammen — Claude Code Workspace Configuration

> **Project:** thammen.qa — Qatar real-estate AVM (RICS Red Book Global Standards, effective 31 January 2025 — VPGA 10 + VPS 6 + IVS 106)
> **User:** Anas (Qatari, Windows, Heroku deploy)
> **Working directory:** `C:\Thammen\deploy v2`
> **Last update:** 2026-06-18 → **live = b58 / Heroku v230** (commit `f658f36`, master==origin). **✅ Sprint 2.22.0b.58 «إسقاط تأطير التجريبية» (drop the beta/trial framing) SHIPPED + live-verified** — 🟢 **FRONTEND-ONLY / VALUE-INVARIANT** (`index.html` copy only; `api.py`+engine UNTOUCHED; live 5-fixture value gate byte-identical to v229). **PO directive:** «لا اريد اطلاق نسخة تجريبية. الموقع يعمل بالفعل، فارجو ان تحذف من حساباتك اي ذكر لكلمة تجريبية. حدث الذاكرة.» — **ثمّن منتج حيّ يعمل، ليس «نسخة تجريبية».** Removed every **user-facing** «تجريبية / بالدعوة / beta / invite-only / before public launch / this beta» from the gate affirmation («المشاركة في النسخة التجريبية»→«الاستخدام») + Terms/Privacy AR+EN (header/§1/§3/§6 «نسخة تجريبية مجانية بالدعوة لقياس دقّة»→«خدمة مجانية … الاستخدام») + the English gate fold («free, invite-only accuracy beta … before public launch»→«A free automated market-estimate tool»). **PRESERVED (the real regulatory/honesty cover, separate from the word "beta"):** «ليس تقييماً معتمداً» (×10) · **free** («خدمة مجانية» — free-vs-paid is the Decision-28/2023 line) · the consent affirmation + the consent gate + the Terms modal · «مستقلّة غير منتسبة لوزارة العدل» · the **CC BY 4.0** MoJ attribution (×8, legally mandatory). The internal `betaGate` id / `thammen_beta_ack` key / `window._betaAck` are KEPT (code identifiers, not user-visible). **Memory updated** (`product-is-live-not-beta.md`, type=feedback + MEMORY.md index): thammen.qa is a LIVE product, never propose a "beta launch" — **the «binding constraint #1 = beta launch + GT collection (D-3)» framing across the older docs is RETIRED** (GT collection can continue as ordinary product improvement, NOT a "beta gate"). Verified: isolated `test_sprint_2_22_0b58.py` **27/27** + **1 R6 re-point** (b54 Terms-§1) + DoD aggregator **MATCH** / security **15/15** / surface **45/45** / broad walk **117/117 ALL GREEN** + **R14 390×844** (gate + Terms render with **0 «تجريبية»**, the affirmation + «ليس معتمداً» + free + «غير منتسبة» kept, 0 console errors, no overflow) + **live smoke v230** [`/api/health`=b58; served HTML «تجريبية»=0 · «بالدعوة»=0 · free-beta/invite-only=0 · «خدمة مجانية»=2 · «ليس تقييماً معتمداً»=10 · CC BY 4.0=8; **5-fixture value gate byte-identical to v229**]. commit `f658f36`, CHANGELOG_v139 + §20.87. **⏭️ NEXT (the PO «هل ممكن عملها كلها؟» engineering backlog, minus the retired beta): the deferred engine b11 range-inversion micro-fix (Gate-2) · OR A5 (`asset_type='unknown'` explanation) + the income_led/b13-trim decomposition-recompute gap · OR the engine-emitted Arabic-string polish — each its own verified sprint.** **Prior (b57 / Heroku v229): ✅ Sprint 2.22.0b.57 «تحصين الواجهة» (frontend hardening — esc/XSS insurance + gate fallback + null-guards) SHIPPED + live-verified** — 🟢 **FRONTEND-ONLY / VALUE-INVARIANT** (`index.html` only; `api.py`+engine UNTOUCHED; live 5-fixture value-invariance gate byte-identical to v228). **Born from a comprehensive code/bug AUDIT** (PO «اعمل فحص شامل للكود وفحص الـbugs المحتملة لعلاجها»): 3 parallel read-only agents (backend/api · engine · frontend), **every candidate verified against the actual code → most "critical" flags were FALSE ALARMS** (the identity helpers ARE defined `index.html:902-905`; `set_request_deadline` never returns None → no «LookupError»; `_income_triangulation` can't fire on a 0 rent — truthiness guard `:5679`; the leadership gate is guarded against a None cost `:6434`) + verified-CLEAN (rate-limiting · `extra='forbid'` · the `/verify` HMAC constant-time+`html.escape` · the dormant capture/mailer gating · `_scrub_personal`). **The genuine findings:** (1) no `esc()` on the ~19 plain-data `innerHTML` injections (the layout-review's "esc() insurance"; **live exploitability LOW** — zone/street/building/pin are Pydantic-int-validated → no markup; district = GIS gov data); (2) the **b11 `_cost_reanchor_down` low>high range inversion** (§20.50, rare 54/788/10-class); (3) the gate `window._betaAck` private-browsing fallback; (4) `||''` null-guards; (5) backlog A5 + the income_led/b13-trim decomposition-recompute gap. **The PO chose** the value-invariant frontend-hardening tranche. **b57 = (1)** an `esc()` helper applied to the plain-data fields (address/district/asset-label/keystone-area-names) — **the engine-authored `*_ar` HTML notes LEFT untouched** (intended markup, trusted our-engine output) · **(2)** `openMapPicker` coord coercion · **(3)** the gate `_betaAck` fallback · **(4)** the `value_stack.cost` `||''` guards. Verified: isolated `test_sprint_2_22_0b57.py` **29/29** + **1 R6 re-point** (b41 keystone `source_area`) + DoD aggregator **MATCH** / security **15/15** / surface **45/45** / broad **116/116 ALL GREEN** + **R14 390×844** (value **٢٬٤٠٠٬٠٠٠** byte-identical; MUC clause HTML intact; **XSS PROBE — injected `<img onerror>` NEUTRALIZED, no execution**; 0 console; no overflow) + **live smoke v229** [`/api/health`=b57; `function esc(`=1 · raw address sinks=0 · gate fallback present · CC BY 4.0=8 kept; **5-fixture value gate byte-identical to v228**]. commit `853367d`, CHANGELOG_v138 + §20.86. **⏭️ NEXT = the deferred engine b11 range-inversion micro-fix (Gate-2) · OR A5 + the income_led/b13-trim decomposition-recompute gap · OR the engine-emitted Arabic-string polish · OR the binding constraint #1 (beta launch + GT collection, D-3 — PO decision).** **Prior (b56 / Heroku v228): ✅ Sprint 2.22.0b.56 «تشذيب اللغة والواجهة» (language + interface polish) SHIPPED + live-verified** — 🟢 **FRONTEND-ONLY / VALUE-INVARIANT** (`index.html` copy/structure only; `api.py`+engine UNTOUCHED; live 5-fixture value-invariance gate byte-identical to v227). PO directive «نفذ الاصلاحات على التقرير المختصر والمفصل وعلى الواجهة ثم انشر» after the lawyer/أديب/لغوي + 10-persona language review: **GATE** — the beta sub-line «نسخة تجريبية مجّانيّة بالدعوة…» + the «اعرف المزيد» `<details class="bg-more">` fold (5 cards) removed; the **«غير منتسبة» + coverage/condition limits MOVED to Terms §2 (AR+EN), preserved**; consent note + affirmation + CTA (now above the fold) + Terms link kept. **HOME** — «العدل» **4→2** (the PO's «مرة أو مرتين بالكثير»): redundant `hsub` (→«تقييم سوقيّ آليّ للفلل والأراضي في قطر») + trust-step 2 (→«نحلّل الصفقات المسجّلة») dropped it; the `hcred` trust line + the recency line kept. **SHORT report** — formal register (الزبدة→**الخلاصة** · سعر عادل→«**التقدير المركزي** لبيتك اليوم» · «وش لو؟»→«**ماذا لو؟**» · شيت→**كشف تقييم** · «بناء أخذ نصيبه»→«**مُهلَكٌ** بحسب عمره» · «— لك وعليك» **deleted**). **DETAILED report** — DEF-12 forced-sale → guarded label «قيمة البيع الجبريّ الإرشاديّة (**×٠٫٩٠ — ليست تصفية معتمدة**)» + Arabic-Indic «×٠٫٩٠» (was Latin «×0.90»); «ليست تقييم تصفية معتمداً» kept. Verified: isolated `test_sprint_2_22_0b56.py` **30/30** + **6 R6/Lesson-2 re-points** (b17/b19/b25/b26/b50/b54 — **zero value/security/methodology assertion weakened**) + DoD aggregator **395** / security **15/15** / surface **45/45** / broad walk **115/115 ALL GREEN** + **R14 live Chromium 390×844** (GATE fold+sub-line gone, CTA bottom 565 ≤ 844 above the fold, card 495 ≤ 92vh; HOME «العدل»=2, no overflow; SHORT all 6 register fixes + «ليس تقييماً معتمداً» kept; DETAILED guarded label + «×٠٫٩٠» + the b55 clusters intact + CC BY 4.0 kept; value **٢٬٤٠٠٬٠٠٠** byte-identical; **0 console errors**) + **live two-lane smoke v228** [`/api/health`=b56; served HTML «اعرف المزيد»=0 · beta-sub=0 · «نحلّل الصفقات المسجّلة»=1 · old «صفقات العدل»=0 · «غير منتسبة لوزارة العدل»=1 (now in Terms) · forced-sale «البيع الجبريّ الإرشاديّة»=1 · CC BY 4.0=8 kept; **5-fixture value gate byte-identical to v227** — 54/541/6 2.4M cost_led · 56/647/6 3.8M geo_full · 55/296/13 2.6M e25 · 56/565/21 2.4M matched · 52/903/90 refusal]. commit `5875d08`, CHANGELOG_v137 + §20.85. **⏭️ NEXT = b57 (the ENGINE-emitted string polish, deferred per #38 — «مُخترَع» phrasing · broad Arabic-Indic number-unification of computed figures · grammar «غير معروفة»→«غير معلوم» · engine effective-date تعريب · the freshness `subtitle_ar` [brings home «العدل» to a literal 1]; own value-byte-gate)** · OR the binding constraint #1 (beta launch + GT collection, D-3 — PO decision). **Prior (b55 / Heroku v227): ✅ Sprint 2.22.0b.55 «رشاقة التقرير الكامل» (full-report note-clustering) SHIPPED + live-verified** — 🟢 FRONTEND-ONLY / VALUE-INVARIANT (`showReport` only + 2 CSS rules): the full report's flat ~12-note wall → **3 labeled bronze clusters** «حول الرقم» (leadership/OSR/cost-triangulation/value-floor) / «حول العقار» (condition/age-honesty/re-survey/age-sensitivity/HBU) / «حول البيانات» (dual-evidence/dispersion/moj-n) via the b31/b52 buffer-prefix-swap (every note verbatim, empty clusters auto-omit, DEF-12 still leads, one MUC clause after); isolated 41/41 + b18/b52 R6 + DoD 395/15/45/broad 114 + adversarial 4-lens verify 4/4 (weakened=false) + R14 (Marikh 3 clusters / V001 2 clusters, value byte-identical, 0 console) + live smoke v227 (byte-identical to v226). commit `2347523`, CHANGELOG_v136 + §20.84. **Prior (b54 / Heroku v226):** **✅ Sprint 2.22.0b.54 «قفل المصطلح: تقييم سوقيّ آليّ» (terminology lock تقدير→تقييم) SHIPPED + live-verified** — 🟢 **FRONTEND-ONLY / VALUE-INVARIANT** (`index.html` copy only; `api.py`+engine UNTOUCHED; live 5-fixture value-invariance gate byte-identical to v225). PO due-diligence (this session, web-researched) established «تقييم» is a **generic, non-reserved** word in Qatar — the reserved professional term is **تثمين/مُثمِّن** (the MoJ «المثمِّن العقاريّ» service; Aqarat / Emiri Resolution 28/2023; our brand ثمّن shares that root) — so the b50 lean toward «تقدير» was a clarity *preference* (not legal) and read weak to the credibility personas. **Term-lock (signed via the on-screen تصور + «نفّذ»):** the product **IDENTITY + PROCESS** «تقدير سوقي آلي»→**«تقييم سوقيّ آليّ»** (27 surgical copy edits — gate/home/CTA/result top-bar+hero/disclaimer/refine/report/short-report/Terms); the **VALUE/RANGE stays «تقديريّ»** (honestly an *estimated* value — «القيمة التقديريّة» · «النطاق التقديري السوقي» · «الوسيط (التقدير المركزي)» kept); **وزارة العدل** (CC BY 4.0 — a legal-attribution obligation + the #1 credibility asset) + **«ليس تقييماً معتمداً»** (the regulatory cover; `rics_compliant=false` by design) KEPT; **RICS tiered** (plain-language lead + the clause numbers VPGA 10/VPS 6/IVS 106 folded); **تثمين/مُثمِّن intentionally avoided for our output**. Built via a deterministic Workflow (the term-lock encoded as an explicit flip-list/keep-list) + an **adversarial 3-lens verify that CAUGHT 5 gate/Terms misses → all fixed**. Verified: isolated `test_sprint_2_22_0b54.py` **44/44** + **9 R6/Lesson-2 re-points** (b15/b17/b24/b25/b27/b30/b31/b50/b52 — intent preserved, **zero value/security/methodology assertion weakened**) + DoD aggregator **395/395** / security **15/15** / surface **45/45** / broad walk **113/113 ALL GREEN** + R14 (Chromium 390×844: 7 fns parse, **0 console errors**, gate renders «تقييم سوقيّ آليّ» + «وليست تقييماً معتمداً» visible, live villa result renders «التقييم السوقي» + value range stays «النطاق التقديري السوقي», no overflow 370<390) + **live two-lane smoke v226** [`/api/health`=b54; served HTML «تقييم سوقيّ آليّ»×10 / **وزارة العدل×16** / «ليس تقييماً معتمداً»×10 / old identity=0 / «النطاق التقديري السوقي» kept; **5-fixture value-invariance gate byte-identical to v225** — 54/541/6 2.4M cost_led · 56/647/6 3.8M geo_full · 55/296/13 2.6M e25_capped · 56/565/21 2.4M matched · 52/903/90 refusal]. commit `938c5ef`, CHANGELOG_v135 + §20.83. **⏭️ NEXT = b55 «رشاقة التقريرين» (report declutter — PO-requested + mockup-previewed this session, the agreed next):** the SHORT report → a single **«بطاقة»** (الزبدة; the income/evidence/full-legal annex folds to page 2) · the FULL report's ~12 fine-print notes → **3 grouped, labeled clusters** («حول الرقم / حول العقار / حول البيانات») instead of a flat wall · **consolidate** the legal/MUC block ONCE + one source attribution + «ليس معتمداً» once · move engine/timestamp/fingerprint **metadata to a thin footer** · **KEEP all compliance/honesty** (وزارة العدل + «ليس معتمداً» + the MUC clause — tier/consolidate, **never delete**). The reports are already partially-leaned (b51 dedup+reorder · b52 MUC fold · b15/b31 tiering); b55 finishes the previewed mockup. **Prior (b52 / Heroku v225):** **✅ Sprint 2.22.0b.52 «الواجهة الرشيقة: شاشة النتيجة» (result-screen lean) SHIPPED + live-verified** — 🟢 **FRONTEND-ONLY / VALUE-INVARIANT** (`index.html` `show()` only; `api.py`+engine UNTOUCHED; `evaluate_unified.py` = 2 version lines; live **5-fixture value-invariance gate identical to v224** — terminology note (PO-approved): the 4 legacy fixtures [54/541/6 · 56/647/6 · 55/296/13 · 56/565/21 · 52/903/90] are **engineering value-invariance fixtures, NOT value anchors**; **V001 56/647/6 is the SOLE calibration anchor**, SESSION_CLOSE §2.1). PO directive: «اجعل الموقع رشيقاً + منهجياً؛ والتقرير المفصّل يبقى مفصّلاً» (remove the redundant text that overwhelms the ordinary user). PO chose **«A + B: full lean»** (the safe folds + folding the full MUC legal clause behind its chip — a PO-signed compliance decision). b51 leaned the REPORT; **b52 leans the RESULT SCREEN** (`show()`): the appraiser fine-print (age-sensitivity · moj sample-size · the methodology bare line) moves from always-visible TIER-1 into the «🔍 كيف وصلنا» fold (`how`), and the **full MUC legal clause folds behind its chip** (`_mucFold` — still BUILT by `_mucCardHtml`, one click away; assembly `h=head+alerts+t1+_mucFold+t2+t3+foot`). **KEPT always-visible on TIER-1:** the figure + range-as-lead + the MUC level chip («تحفظ مادي: {level}») + «ليس تقييماً معتمداً» + the evidence one-row + the condition note. **The detailed report (`showReport`) is UNTOUCHED** — detailed stays detailed. **No compliance/honesty content deleted** (the MUC clause is FOLDED, not removed). Verified: DoD aggregator **ALL COUNTS MATCH** / security **15/15** / surface **45/45** / broad **112/112 ALL GREEN** (111→112, +the new `test_sprint_2_22_0b52.py` **17/17**; 3 R6/Lesson-2 re-points — b18 #23 «age_sensitivity TIER-1»→fold · b31 «STAYS t1»→fold + «valued assembly»→the `_mucFold` assembly · b15 «MVU NOT wrapped in _acc»→folds-behind-its-chip; **none weakens a value/security/methodology assertion** — placement pins only) + **R14 Chromium 390×844** (lean TIER-1; the full MUC clause inside a collapsed `<details>`; chip + «ليس معتمداً» stay; value **2,400,000** byte-identical; **0 console errors**; no overflow maxRight 370<390) + **live two-lane smoke v225** (browser-UA #61: `/api/health`=b52/qars healthy; served HTML carries `_mucFold` + age-sensitivity→`how` + the chip/«ليس معتمداً» in t1 + the full clause still built [7/7 markers PASS]; **5-fixture value-invariance gate identical to v224** — 54/541/6 2.4M cost_led · 56/647/6 3.8M geo_full · 55/296/13 2.6M e25 · 56/565/21 2.4M matched · 52/903/90 refusal). commit `bc6aaaa`, CHANGELOG_v134 + §20.82. **⏭️ NEXT:** finish the b50 copy residuals (Stage 5 · «قياس دقّة» Terms §1 · «التقييم» naming our output — value-invariant copy cleanup) · OR group the result-screen/report fine-print notes by class (further declutter, Gate-2 design) · OR a persona-panel **strategic** item (apartment/tower «بوابة بيانات الأنواع» · full EN localization DEF-UX5 — both Gate-2) · OR the binding constraint #1 (beta launch + GT collection, D-3 — PO decision). **Prior (b51 / Heroku v224):** **✅ Sprint 2.22.0b.51 «تنظيف ازدحام التقرير» (report declutter: dedup + reorder) SHIPPED + live-verified** — 🟢 **FRONTEND-ONLY / VALUE-INVARIANT** (`index.html` `showReport` only; `api.py`+engine UNTOUCHED; `evaluate_unified.py` = 2 version lines; live **5-anchor value byte-gate identical to v223**). The PO flagged the report «مزدحم بالبيانات» (crowded) + asked to cut filler. Recon (result screen `show()` + full report `showReport` + short report) found the surfaces **already heavily decluttered** (b15/b31/b32/b34) — genuine deletable filler is **small** (honest recon-correction: NOT the «40-50%» first pitched); the real win = **(1) de-dup** the cost-value triplicate (`value_stack.cost.value` rendered as a standalone `.rn` note + the DEF-12 row + «تفكيك المرتكز» → the b20 standalone note removed; cost now renders **once**, in DEF-12) + **(2) reorder** the DEF-12 three-value block (سوقية/كلفة/جبري) UP to right after the headline range, ahead of the ~12 fine-print appraiser notes → the reader meets the KEY numbers first; the fine-print follows. Compliance/honesty content (MUC clause · «ليس معتمداً» · CC BY 4.0 · «ما لا نعرفه» known-unknowns · legal block · the specific forced-sale qualifier) **ALL kept** — defensive repetition is the #1 trust lever (panel-confirmed), not bloat. Verified: DoD aggregator **ALL COUNTS MATCH** / security **15/15** / surface **45/45** / broad **111/111 ALL GREEN** (1 R6/Lesson-2 re-point: the b50 stale exact-version pin `ENGINE_VERSION==b50`/`SPRINT_TAG==2.22.0b.50` → version-agnostic format checks, b50 copy checks all green → 32/32; b19 25/25 · b20 69/69 green WITHOUT re-point) + **R14 Chromium 390×844 on BOTH leader paths** (Marikh cost-led **2.4M** `.b40_marikh.json` · V001 geo **3.8M** `.b41_v001.json` — DEF-12 **leads before** the notes [idx 1036/1025 < 2371/2259], `def12_firstRow` = the central value, cost once in DEF-12, **0 console errors**, no overflow maxRight 370<390, `showReport` parsed = no JS break) + **live two-lane smoke v224** (browser-UA #61: `/api/health`=b51/qars healthy; served HTML carries the b51 reorder marker «DEF-12 three-value block leads here» + the dedup marker + `rep-def12`; **5-anchor value byte-gate identical to v223** — 54/541/6 2.4M cost_led · 56/647/6 3.8M geo_full · 55/296/13 2.6M e25 · 56/565/21 2.4M matched · 52/903/90 refusal). commit `64523aa`, CHANGELOG_v133 + §20.81. **Also this session:** the **100-persona panel re-run on b50** (`docs/PERSONA_PANEL_100_b50_v223.md`, solo — the parallel Workflow was blocked 10/10 by the 1M-context credit gate, the handoff's predicted fallback) — rollup **5.7→5.8**, the b50 copy-honesty fixes verified (3 🔴 resolved on their primary surfaces · 2 🟡 partial: residual «يتطلّب تقييم Stage 5» in the uncertainty block · «قياس دقّة» in Terms §1 · «التقييم» naming our output in 2 spots), driven by the متشكّك/خصوصيّة segment (5.2→6.1, WhatsApp→email + «غير منتسبة»); structural blockers (apartments/English/condition/staleness) unchanged as expected. **⏭️ NEXT:** the reorder's bigger sibling = group the fine-print notes by class (further declutter, Gate-2 design) · OR finish the b50 copy residuals (Stage 5 · قياس دقّة · التقييم — a value-invariant copy cleanup) · OR a persona-panel **strategic** item (apartment/tower «بوابة بيانات الأنواع» · full EN localization DEF-UX5 — both Gate-2) · OR the binding constraint #1 (beta launch + GT collection, D-3 — PO decision). **Prior (b50 / Heroku v223):** **✅ Sprint 2.22.0b.50 «تصحيح النسخ: صدق المصدر + قناة التواصل» (copy honesty) SHIPPED + live-verified** — 🟢 **FRONTEND + small backend / VALUE-INVARIANT** (text/display only; live **5-anchor value byte-gate identical to v222**). From the 100-persona panel (`docs/PERSONA_PANEL_100_b49_v222.md`) + the per-phrase copy audit (`docs/COPY_AUDIT_persona_sweep_b49.md`): **(1) source honesty** — the MoJ lines that implied official/affiliation («العدل **الفعلية**» / api «**الرسمية**» / bare «العدل القطرية») → **open-data** framing + a «**ثمّن خدمة مستقلّة غير منتسبة لوزارة العدل**» clause (CC BY no-endorsement + regulatory prudence; the precise no-affiliation note had lived only in the footer); **(2) contradiction** — disc «هذا **التقييم** إرشادي» → «هذا **التقدير** إرشاديّ» (our output is a تقدير, never a تقييم); **(3) contact channel (PO decision)** — the **WhatsApp number dropped site-wide → `info@thammen.qa`** (2 GT hooks + 4 Terms AR/EN lines; personal name «أنس»/«Anas» removed from user copy → «فريق ثمّن / Thammen team»); **(4) de-jargon** — internal «(المرحلة الخامسة)»/«(Stage 5)» roadmap tag dropped from the rics-status + methodology note; **(5) gate framing** — «نتيجة بحثية»→«معلومة استرشاديّة» + «هدفها قياس دقّة»→«نطوّرها بملاحظاتك»; **(6) backend de-emoji** — 📅/⚠️ stripped from the freshness banner. **Self-correction (Rule #36):** the «والآراضي» typo flagged earlier was a screenshot misread — source (455) is correctly «والأراضي». Verified: isolated **32/32** + 4 sibling re-points (a20/a8/b17/b25) + DoD aggregator **395** / security **15** / surface **45** / broad **111/111 ALL GREEN** + R14 390×844 (new strings render, **0 console errors**, no overflow) + live two-lane smoke v223 (served HTML: no `70177761`, open-data framing, «غير منتسبة لوزارة العدل», «هذا التقدير إرشاديّ», no «المرحلة الخامسة», emails present [4 Cloudflare-obfuscated `__cf_email__` → render as `info@thammen.qa`]; **5-anchor value byte-gate identical to v222** — 54/541/6 2.4M cost-led · 56/647/6 3.8M geo_full · 55/296/13 2.6M e25 · 56/565/21 2.4M matched · 52/903/90 refusal). commit `bfc37ca`, CHANGELOG_v132 + §20.80. **⏭️ NEXT:** Sprint C remainder (form/confirm/refine bronze chrome + role segmented-control) · OR a persona-panel **strategic** item (apartment/tower support = «بوابة بيانات الأنواع» · full EN localization = DEF-UX5 — both Gate-2) · OR the binding constraint #1 (beta launch + GT collection, D-3 — PO decision). Deferred copy: «تحفظ مادي» plain-rename + standards-behind-ⓘ · full terminology lock (تقدير/تقييم/تثمين + شدّة سوقيّ/آليّ) · a full backend-emoji sweep. **Prior (b49 / Heroku v222):** **✅ Sprint 2.22.0b.49 «اللوقو + كروم النموذج» (logo placement + form-field chrome) SHIPPED + live-verified** — 🟢 **FRONTEND-ONLY / VALUE-INVARIANT** (`index.html` 6 surgical edits; `api.py`+engine UNTOUCHED; `evaluate_unified.py` = 2 version lines; live **5-anchor value byte-gate identical to v221**). Three PO observations on the live b48 site (screenshots): **(1) Logo → home only** — removed the gate-header logo chip (`.bgate-logo`; the `.bgate-head` keeps its navy band + title «ثمّن — تقدير سوقيّ آليّ…» which already carries the brand) + the 6 working-screen top-bar raster logos; the big home `.hlogo` (234px) is now the **only `logo.png` site-wide** («keep it on the front page»). **(2) Top-bar wordmark** — the 6 raster logos → a clean text wordmark `<span class="tbar-wm">ثمّن</span>` (navy `var(--primary)`, bold) inside the same `.tbar-logo` click target → **keeps go-home, not a squished raster**. **(3) Form overflow fix** — `.fr3 .fg,.fr2 .fg,.fr3 input,.fr2 input{min-width:0}`: the multi-column rows (منطقة/شارع/مبنى + the 2-col optional rows) had grid items at the browser default `min-width:auto` so the ~180px-intrinsic inputs refused to shrink to their `1fr` track inside the 580px-capped card → the «رقم المبنى» field overflowed (RTL → out the left). `min-width:0` lets each item shrink to its track. **The consent gate (#2, the PO «is it necessary?» question) is UNTOUCHED** — it stays the a24 session-only affirmative-consent layer (load-bearing for the beta / PDPPL self-clearance, already minimized in b46); lightening it («once per device» / inline banner) is a PO **compliance decision**, not done here. Verified: DoD aggregator **395/395** / security **15/15** / surface **45/45** / broad walk **110/110 — ZERO re-points** (grep-confirmed: no test pins `logo.png`/`bgate-logo`/`tbar-logo`/`bgate-head`/`hlogo`/`.fr3`) + live preview (gate no-logo navy band+title · top-bar wordmark «ثمّن» navy `onclick=go('home')` · home `.hlogo` kept 234px · overflow fixed measured at desktop 1280 [fields 155px, building-no inside the card], the 640px quirk width, and mobile 390 [stacks 1-col] · no doc-overflow on any width · 0 console errors) + **live two-lane smoke v222** [`/api/health`=b49/qars healthy; served HTML: `logo.png` count **1** (home only) · `.tbar-wm` ×6 · `bgate-logo` 0 · the `.fr3 .fg{min-width:0}` rule present · `class="rhero"`+`class="bgate-head"` intact · **0 emoji / 0 googleapis / 0 Tajawal**; **5-anchor value byte-gate identical to v221** — 54/541/6 2.4M cost_led · 56/647/6 3.8M geo_full · 55/296/13 2.6M e25 · 56/565/21 2.4M matched · 52/903/90 refusal]. commit `15e5f33`, CHANGELOG_v131 + §20.79. **⏭️ NEXT (Sprint C remainder, same نسق):** (1) the **form / confirm / refine** section-label bronze chrome + the role selector as a polished segmented-control · (2) a **backend-emoji sweep** (engine-emitted strings) · (3) the **logo** SVG + light/mono variant (designer track `docs/BRIEF_logo_v1.md`) · OR a **consent-gate lightening** if the PO decides (#2: «once per device» / inline banner — compliance call) · OR the binding constraint #1 (beta launch + GT collection, D-3 — PO decision). **Prior (b48 / Heroku v221): ✅ Sprint 2.22.0b.48 «الواجهة المرفوعة — نسق واحد» (interface elevation — one design system) SHIPPED + live-verified** — 🟢 **FRONTEND-ONLY / VALUE-INVARIANT** (`index.html` the bulk; `api.py`+engine UNTOUCHED; `evaluate_unified.py` = 2 version lines; live **5-anchor value byte-gate identical to v220**): applies the premium short-report design language to the **whole interface** (PO «اريد الموقع كاملا على نسق واحد … لا اريد ايموجيز … اريد الموقع فاخرا»). **(1) Result hero ★1** — the TIER-1 figure (a grey dashed worksheet box) → a navy `.rhero` band (gold «التقدير السوقي» label + the central figure in white + a slim low↔high `.rbar` range bar with a dot at the median); **evolves the signed b3 range-as-lead** (KEEPS the range + the RICS «ليس تقييماً معتمداً» clause), **supersedes the a8 calc-block-exactly-once contract** (the result no longer carries `calc-block`); MUC chip red→**amber** (`--bad`→`--warn`); scope card green→neutral. **(2) Color-system lock** — +`--gold:#E8C99A` (on-navy accent), dropped the orphan `--maroon`; swept the leftover old-palette literals app-wide (completes the b45 unify — old-navy `18,52,77`→`22,50,79`, old-bronze `166,130,82`→`164,129,74`, `#a68252`→`var(--bronze)`); `--sh` navy-tinted depth site-wide. **(3) Elevated home** — navy title + a bronze rule + a navy 3-step trust band (أدخل العنوان ← نحلّل صفقات العدل ← نتيجتك, gold step numbers) + «من صفقات وزارة العدل المسجّلة — لا أسعار إعلانات» (the b24 copy verbatim). **(4) Consent gate → navy `.bgate-head`** (white light-chip logo; the b46 fold + consent flow unchanged). **(5) De-emoji** — **151 emoji → 25 inline-SVG line icons** (a `<symbol>` sprite + `.ic` class, **no CDN** → preserves the b45 privacy win); **zero emoji site-wide**. Verified: DoD aggregator **395/395** (calc_visual 62→65 — the a8→hero re-point; manifest 392→395 per the documented contract) / security 15/15 / surface 45/45 / broad walk **110/110** (13 re-points — **every one dropped only the emoji from a pin / updated a hero pin to the new truth; zero value/security/methodology assertions weakened**, independently verified) + live preview 390×844 (hero amount byte-identical, navy band, amber MUC chip, range-bar dot positioned, 0 `calc-block`, **0 emoji in the DOM**, 0 console errors, no overflow) + **live two-lane smoke v221** [`/api/health`=b48/qars healthy; served HTML carries `class="rhero"`+`class="bgate-head"`+`class=ic`×151+`<symbol id="ic-`×25, **0 emoji / 0 googleapis / 0 Tajawal**; **5-anchor value byte-gate identical to v220** — 54/541/6 2.4M cost_led · 56/647/6 3.8M geo_full (warm 19s; the cold first-try H12 = the documented A6/R5 pattern, not a defect) · 55/296/13 2.6M e25 · 56/565/21 2.4M matched · 52/903/90 refusal]. commit `8ecab27`, CHANGELOG_v130 + §20.78. **⏭️ NEXT (Sprint C remainder, same نسق — next session):** (1) the **form / confirm / refine** section-label bronze chrome + the role selector as a polished segmented-control (icons + depth + color done; this is the remaining chrome) · (2) a **backend-emoji sweep** (engine-emitted strings — Marikh rendered 0 emoji in the DOM; spot-check other payloads) · (3) the **logo** stays the existing raster (designer brief `docs/BRIEF_logo_v1.md` sent — wire the SVG/light-on-navy variant the moment it lands) · OR the binding constraint #1 (beta launch + GT collection, D-3 — PO decision). **Prior (b46 / Heroku v220): ✅ Sprint 2.22.0b.46 «طبقات بوّابة الموافقة» (consent-gate layering) SHIPPED + live-verified** — 🟢 **FRONTEND-ONLY / VALUE-INVARIANT** (`index.html` only — the gate restructure + a scoped CSS block; `api.py`+engine UNTOUCHED; live **5-anchor value byte-gate identical to v219**): **Sprint C — slice 1 of the layout-review payoff (the review's ★4)**. The consent gate is every user's FIRST frame — a wall of **5 stacked detail cards** + the consent box, with the CTA «أوافق وأكمل» **below the fold** on 390×844. b46 folds the 5 cards into a collapsed `<details class="bg-more">` «اعرف المزيد عن النسخة التجريبية ↓» → the first frame is now tight: logo + title + sub + the fold + the consent note + the affirmation + **the CTA (now ABOVE the fold)** + Terms + the English fold. **Every word of the signed a24 text preserved** (layered, NOT removed — GDPR/ICO layered-notice best practice; `role=dialog` + affirmative consent unchanged). A **scoped** `.bg-more:not([open])>ul{display:none}` guarantees the fold collapses (the measured Chromium `<details>` content-hiding quirk) — other accordions untouched. Verified live 390×844: the gate card `scrollHeight` = **540px collapsed** (fits) vs 1013px open (**fold hides 473px**); `<ul>` display:none closed / block open; 5 cards preserved; **CTA bottom 587 ≤ 844 (above the fold)**; 0 console errors. DoD 392/15/45/broad **110/110 — ZERO re-points** (b27=23/23, the gate `class`/`id` unchanged) + live smoke v220 [`/api/health`=b46/qars healthy; served HTML carries `class="bg-more"`+«اعرف المزيد»+the 5 cards; **5-anchor value byte-gate identical to v219**]. commit `47e2ade`, CHANGELOG_v129 + §20.77. **⏭️ NEXT (Sprint C remainder):** the **result-screen hero (★1, highest value)** — its own focused slice (it evolves the signed b3 «range-as-lead» + supersedes the a8 calc-block contract + recolors the MUC chip red→amber; ~7-test re-point; the PO approved the *direction* via the now-vs-proposed mockup) · the **home trust strip** (additive) · the **logo** stays the existing raster (designer brief sent, may take ~a year; build on the current logo, a light-chip bridge on navy if needed). **Prior (b45 / Heroku v219): ✅ Sprint 2.22.0b.45 «توحيد العلامة: لون + خطّ + إغلاق CDN» SHIPPED + live-verified** — 🟢 **FRONTEND-ONLY / VALUE-INVARIANT** (CSS tokens + font-family + a `<link>` removal; `api.py`+engine UNTOUCHED; `evaluate_unified.py` = 2 version lines; live **5-anchor value byte-gate identical to v218**): **the keystone of the layout-review roadmap (Sprint B)** — unifies the app's TWO design systems into one. Recon-clean (#33: old brand hex only in `:root`, 0 Tajawal in JS, IBM Plex `@font-face` already global + already whitelisted-served → **zero new downloads, no api.py change**): (1) **tokens → canonical thmr values** (`--primary` `#12344D`→`#16324F`, `--bronze` `#A68252`→`#A4814A`, `--bg` `#FAFAF7`→`#FBF8F2`) with the legacy token NAMES **kept (aliased)** so ~40 sprints of `var(--primary)`/`var(--bronze)` class usage stay working; (2) **font → IBM Plex Sans Arabic app-wide** (`body` + all 13 `font-family:'Tajawal'`); (3) **dropped the Google-Fonts CDN `<link>`** → **closes the pre-consent third-party request (a PDPPL win)**. `.thmr` (the reports) is now a theme scope on the SAME tokens+font — the app shell finally matches the premium report. Verified: DoD 392/15/45/broad **110/110** (+ 1 b25 re-point: the م2/D7 «.thmr-only» scoping → the b45 global unify) + live preview 390×844 (body=IBM Plex, `--primary`=#16324F, CDN link gone, **0 overflow on all 5 screens**, 0 console errors) + live smoke v219 [`/api/health`=b45/qars healthy; served HTML carries `--primary:#16324F`+`body{…'IBM Plex Sans Arabic'…}`+**0 googleapis**; **5-anchor value byte-gate identical to v218** — Marikh 2.4M cost_led · V001 3.8M geo_full · المعراض 2.6M e25 · أبو هامور 2.4M matched · شقق refusal]. **Carried:** one inert `Tajawal` remains in the LIVE served HTML (a CSS *comment*, the calc-block explainer; source cleaned, goes live next deploy — no redeploy for a comment); the logo stays the existing raster (SVG/light/compact variants with the designer, brief sent — wire on arrival). commit `6e58fbd`, CHANGELOG_v128 + §20.76. **⏭️ NEXT = Sprint C (the payoff — result-screen `thmr` hero + consent-gate layering + home trust strip; now CHEAP)** · OR the binding constraint #1 (beta launch + GT collection, D-3 — PO decision). **Prior (b44 / Heroku v218): ✅ Sprint 2.22.0b.44 «تباين النصّ القانونيّ (وصوليّة AA)» SHIPPED + live-verified** — 🟢 **FRONTEND-ONLY / VALUE-INVARIANT** (CSS color tokens only; `api.py`+engine UNTOUCHED; `evaluate_unified.py` = 2 version lines; live **5-anchor value byte-gate identical to v217**): the **legal/disclaimer/MoJ-attribution/recency** text moves `--light` (`#9CA3AF` ≈ **2.3:1** — below WCAG AA) → `--muted` (`#6B7280` ≈ **4.5:1** — AA pass) on `.disc`/`.src-credit`(+.en)/`.hfoot`/`.cg-sub`/the Terms links; **decorative `--light` unchanged** (`.tbar-st`/`.trend-labels`/`.lprog`). **First slice of the LAYOUT-REVIEW roadmap** (a 4-lens brand+eng deep review rendered live at 390×844; diagnosis «the polish order is inverted»; two mismatched design systems): **Sprint A = a11y/asset hygiene** [recon RESHAPED it — the 727KB raster logo recompression is BLOCKED (no local image tooling) → deferred pending an optimized asset; self-hosting Tajawal = throwaway → the font/CDN-privacy + brand-color split move to **Sprint B (unify to the already-local IBM Plex, zero downloads)** → then **Sprint C (result-screen `thmr` hero + consent-gate layering + home trust strip)**]. Verified: DoD 392/15/45/broad **110/110** + live preview computed-colors (`.disc`/`.src-credit`/`.hfoot` = `rgb(107,114,128)`, decorative stayed) + 0 console errors + live smoke v218 [`/api/health`=b44/qars healthy; served HTML carries the `--muted` markers; **5-anchor value byte-gate identical to v217** — 54/541/6 2.4M cost_led · 56/647/6 3.8M geo_full · 55/296/13 2.6M e25 · 56/565/21 2.4M matched · 52/903/90 refusal]. **Tooling note:** a first spurious `None×5` smoke was a shell-harness bug (`${p%%:*}` truncated JSON-with-colons → 422) + a cold dyno — the heroku logs caught it (#33/#36/E14); CSS-only → value-invariant by construction regardless. commit `cf37fe0`, CHANGELOG_v127 + §20.75. **⏭️ NEXT = Sprint B (the keystone — token + font unify)** · OR the binding constraint #1 (beta launch + GT collection, D-3 — PO decision). **Prior (b43 / Heroku v217): ✅ Sprint 2.22.0b.43 «نسخة-المُشغِّل: العنوان بلا بيانات شخصية» SHIPPED + live-verified** — the operator report-copy now **KEEPS the property address** but **STRIPS the two Kahramaa utility ACCOUNT numbers** (`electricity_no`+`water_no` — the *only* person-identifying fields in the engine `result`) from **both** the email body **and** the JSON attachment (pure `_scrub_personal` deep-copy → never mutates `result`; PIN/district/GPS/valuation/age **kept** = property/parcel data). The PO's full address-**redaction** brief was first signed then **REJECTED** («كلا … اريد ان يصلني العنوان كذلك. لكن بدون بيانات شخصية») → reverted → **b43 = the precise middle**. 🟢 ADDITIVE / value-invariant (`api.py`+`index.html`+engine UNTOUCHED; live **5-anchor byte-identical to v216**; full-blob **PII scan clean**; the dormant capture never stored the utility numbers); isolated **48/48** + DoD 392/15/45/broad **110/110** + **email-send proof** (no send-failure logged after 5 live evals). commit `1c23904`, CHANGELOG_v126 + §20.74. **The a24 «stores nothing» notice update remains OPEN** (PO wording unsettled; the rejected heavy-redaction posture is **NOT** to be re-applied) — the gate for **beta-WIDE** use only (Rule #39), a non-issue for the operator's own testing. **⏭️ NEXT remains the binding constraint #1 (beta launch + GT collection, D-3 — PO decision).** **Prior (b42.2 / Heroku v216): ✅ email-send LIVE test PASSED — the report-copy memory is LIVE + ACTIVE + inbox-confirmed** (`RESEND_API_KEY`+`REPORT_COPY_EMAIL` set on Heroku; recipient `ans_hashim@hotmail.com`; Anas confirmed the email + the 41KB JSON attachment arrived). **b42.2 cleaned the email fields** (the age rendered the raw `building_age_estimate` dict → now «العمر ≥ N سنة (تقديري)»; the asset label `standalone_villa` → «فيلا منفردة» via `service_scope.label_ar`; the test fixture was matched to the production shape — E14; isolated 40→**42/42**, DoD 392/15/45/broad **110/110**, redeployed v216, clean-render verified on a real result). The first real send hit **Cloudflare `error code: 1010`** on `api.resend.com` (the bare `urllib` User-Agent — `api.resend.com` is Cloudflare-fronted too, the same #61 / R12 block as thammen.qa) → **fix b42.1**: a browser `User-Agent` on the Resend POST in `report_mailer._post` (isolated 36→**40/40**, +4 UA checks; DoD 392/15/45/broad **110/110**; redeployed v215; the re-run eval logged **no send-failure** → the report copy delivers; subject `[ثمن] TH-20260614-54541006-b052 — 54/541/6 — 2,400,000 ر.ق` + full JSON attached). **Beta-wide still gated** on the a24 notice update (Rule #39). commit `0efec63`, CHANGELOG_v125. **Prior: Sprint 2.22.0b.42 «نسخة-المُشغِّل بالبريد» (operator report-copy by email) SHIPPED** (🟢 **ADDITIVE BACKEND / DORMANT-by-default / VALUE-INVARIANT** — the operator (Anas) now receives an **email copy of every report**; the inbox IS the memory — durable, searchable by `report_ref`, no DB to run [Heroku FS is ephemeral]. New `report_mailer.py` [gated on `RESEND_API_KEY`+`REPORT_COPY_EMAIL`, lazy, NEVER raises, NEVER mutates `result`] + a guarded `BackgroundTasks` seam in BOTH `/api/evaluate*` handlers [runs AFTER the response → **zero latency**]; subject `[ثمن] {report_ref} — {address} — {value} ر.ق`, RTL summary + the **FULL result JSON attached** [complete archive]; default sender = Resend test `onboarding@resend.dev` [structural «my reports only» until a domain is verified]. **Decision (PO):** channel=**email (Resend)**, scope=**operator's-own-reports-now** [beta-wide DEFERRED behind the a24 notice update «العنوان لا يُخزَّن» → «نحتفظ بنسخة للسجلّات» + PDPPL nod — Rule #39 flag]. `index.html` UNTOUCHED, **amount/low/high/method/rule byte-identical**; isolated **36/36** + DoD 392/15/45/broad **110/110** [109→110] + `import api` OK [14 routes, `_MAIL_OK=True`, `mail_enabled()=False` → dormant] + live smoke **v213** [`/api/health`=b42; **5-anchor value byte-gate identical to v212**: 54/541/6 2.4M cost_led · 56/647/6 3.8M geo_full · 55/296/13 2.6M e25 · 56/565/21 2.4M matched · 52/903/90 refusal; `NO-capture_id` on all → capture+mail DORMANT, response byte-identical]; commit `45a9701` (fix `0efec63`), CHANGELOG_v125 + §20.73. **✅ email-send LIVE test PASSED (b42.1/v215)** — the mailer is active + delivering (no send-failure logged after the b42.1 redeploy). **⏭️ NEXT remains the binding constraint #1 (beta launch + GT collection, D-3 — PO decision); this report-copy is the operator-side memory that complements it.** **Prior: Sprint 2.22.0b.41 «الكَيستون: صفوف الجيران الجغرافيّة» (DEF-UX1.1b) SHIPPED** (🟢 engine-additive **display-only / VALUE-INVARIANT** — on a **geo-led** villa (V001 56/647/6) the keystone now surfaces the location-adjusted **neighbour** rows behind the median [from `geo_v2_result['accepted_areas']`, retained in-engine]: each row = source-area NAME + ×adjustment + the **DERIVED** adjusted ppm² [`round(displayed raw × displayed factor)` — **self-consistent** so the panel arithmetic closes (b14-coherence); never feeds the value → value-invariant], nested as `comparables.neighbours` via a new pure `_keystone_neighbours`; **geo-led ONLY** [matched_bracket b38 has no neighbours; cost-led `considered_comparables` b40 untouched]; rows anonymous [E12 — area name + ratio only] + CC BY 4.0; 2-line bidi-safe render [📍area RTL + ×factor LTR island / `date · م² · raw→adjusted ر.ق/م²` dir=ltr] + the honest «لم تُبَع بالرقم المُعدَّل»; the b39 «إجمالي {pool_n} صفقة» line coexists; `api.py` UNTOUCHED, **amount/low/high/method/rule/leadership byte-identical**; isolated **30/30** + siblings b38 25/25·b39 19/19·b40 18/18 green [no re-point] + DoD 392/15/45/broad **109/109** + local E2E [live GIS — V001 **neighbours PRESENT** areas=2/total=29/shown=8, derive-all self-consistent (بو هامور ×0.9517 · 3871→3684), E12 clean · Abu Hamour matched + comparables/no-neighbours · Marikh cost-led + considered/no-neighbours · **value byte-identical to v211**] + R14 390×844 [neighbour sub-table in the OPEN «كيف وصلنا», headline **٣٬٨٠٠٬٠٠٠ unchanged**, 0 console, no overflow (panel right 355 < 390, rows ≤ 338)] + live smoke **v212** [5-anchor value byte-gate identical to v211; V001 `comparables.neighbours.shown=8`; served HTML carries «الصفقات المجاورة المُعدَّلة الموقع» + `_kc.neighbours`]; commit `e51a505`, CHANGELOG_v124 + §20.72. **The keystone series (b38 matched → b39 geo primary → b40 cost-led considered → b41 geo neighbours) is COMPLETE.** **⏭️ NEXT = the binding constraint #1 (beta launch + GT collection, D-3 — PO decision)** · OR the lighter §4ب display items [UX4 freshness banner+slider · UX6 improvement-delta] · OR a signed Gate-2 [B-2 / §6 v2]; **Option B** [the exact cost-led n=51 geo-full pool rows, engine-additive `return_transactions`] deferred · **DEF-UX5** = Gate-2 EN-localization · **DEF-UX15** blocked (QARS data-drain). **Prior: Sprint 2.22.0b.40 «الكَيستون: حوض السوق المُعتبَر على مسار الكلفة» (DEF-UX1.2a) SHIPPED** (🟢 engine-additive **display-only / VALUE-INVARIANT** — extends the keystone to **cost-led** villas (e.g. Marikh 54/541/6): on a cost-led number the market pool was **considered but did NOT lead** [its geo-full pool failed reliability — n=51, dispersion 0.620 > 0.30] → a NEW `valuation.considered_comparables` (basis=`cost_considered`) surfaces the subject's **PRIMARY-area raw rows the engine examined** [`geo_v2_result['primary']['transactions']`, in scope; Marikh routes Case-4 thin so `primary['comparables']` is absent] under the honest «🔍 صفقات السوق في منطقتك — اطّلعنا عليها ولم تقُد الرقم» frame [**NEVER** «هي ما قرّر رقمك»] + the why-line «الحوض الجغرافيّ فشل حدّ الموثوقيّة (تشتّت 0.620 > 0.30، n=51) — قاد التقديرَ منهجُ الكلفة (DRC)»; **distinct key** from the b38/b39 `comparables` [mutually exclusive market vs cost_led]; reuses the UNCHANGED `_keystone_comparables` builder; rows anonymous [E12] + CC BY 4.0; muted border vs the bronze keystone; `api.py` UNTOUCHED, **amount/low/high/method/rule/leadership byte-identical**; isolated **18/18** + b38/b39 green [no re-point] + DoD 392/15/45/broad **108/108** + local E2E [live GIS — Marikh **considered PRESENT** n=29 shown=8 pool_n=51 disp 0.62 · أبو هامور matched · V001 geo · المعراض/شقق neither · value byte-identical to v210 · mutually-exclusive] + R14 390×844 [panel in the OPEN «كيف وصلنا», why-line + 8 dir=ltr rows + CC BY 4.0, **no overclaim**, headline **٢٬٤٠٠٬٠٠٠ unchanged**, 0 console, no overflow 355<390] + live smoke **v211** [5-anchor value byte-gate identical to v210; Marikh considered `cost_considered` pool_n=51; served HTML carries `considered_comparables`]; commit `2f957f5`, CHANGELOG_v123 + §20.71. **⏭️ NEXT = b41** (geo neighbour rows — the §20.70 «full geo pool» sibling: the accepted-neighbour rows from `geo_v2_result['accepted_areas']` with a source-area + ×adjustment column, the heavier 4-column bidi layout + fresh R14) · then **DEF-UX8** affordability/LTV guards [🟡 NET-NEW] · the lighter §4ب items [UX4/UX6]; **DEF-UX5** = Gate-2 EN-localization project · **DEF-UX15** blocked; the 🟢 deploy-on-green frontend backlog otherwise EXHAUSTED — remaining items need a signed brief / product decision / the precision track [B-2 / §6 v2 / GT D-3]). **Prior: Sprint 2.22.0b.39 «الكَيستون الجغرافيّ» (DEF-UX1.1) SHIPPED** (🟢 engine-additive **display-only / VALUE-INVARIANT** — extends the b38 keystone to the **geo-led** market villas (`comparison_widened`/`_widened_indicative`, e.g. V001): «🔑 صفقات في منطقتك ضمن حوض المقارنة الموسَّع جغرافياً» = the subject's **PRIMARY-area raw transactions** within the geo-widened pool + an honest disclosure that location-adjusted neighbour rows were pooled («إجمالي {pool_n} صفقة») — never the bracket «هي ما قرّر رقمك» overclaim; engine `_keystone_comparables` += `basis`/`pool_n`/`price_m2`-fallback/newest-first-sort, Cases 2-3 stash `geo_v2['primary']['transactions']`, the b4-region gate broadened to the widened methods [still `leader=='market'`]; rows anonymous [E12] + CC BY 4.0; **amount/low/high/method/rule byte-identical**; isolated **19/19** + b38 R6 re-point [25/25] + DoD 392/15/45/broad **107/107** + local E2E [V001 **geo_widened** n=5/pool 34 · أبو هامور **matched_bracket** · Marikh cost-led/apt absent · value byte-identical] + R14 V001 geo [geo header + widening disclosure + no overclaim, headline **٣٬٨٠٠٬٠٠٠ unchanged**, 0 console, no overflow] + live smoke **v210** [5-anchor value byte-identical to v209; V001 comparables **geo_widened pool_n=34**]; commit `65cfa35`, CHANGELOG_v122 + §20.70. **⏭️ NEXT = DEF-UX8** affordability/LTV guards on the b35 calculator [🟡 NET-NEW] · the **cost-led «considered-but-didn't-lead» pool** + the **full geo pool** [neighbour rows] — deferred from b39 · the lighter §4ب display items [UX4/UX6]; **DEF-UX5** = Gate-2 EN-localization project · **DEF-UX15** blocked; the 🟢 deploy-on-green frontend backlog is EXHAUSTED — remaining §4ب items need a signed brief or product decision). **Prior: Sprint 2.22.0b.38 «الكَيستون: كشف الصفقات المقارِنة للفيلا» (DEF-UX1) SHIPPED v209** (🟢 engine-additive **display-only / VALUE-INVARIANT** — the b31 «🔍 كيف وصلنا» accordion now shows the actual MoJ transactions that PRODUCED the median: «🔑 N صفقة في شريحتك ومنطقتك — هي ما قرّر رقمك» + a `date · م² · ر.ق` table + the CC BY 4.0 source line; **gated `leader=='market' && method=='comparison_bracket'`** → ONLY when the market led via the matched bracket [excludes cost-led/income-led/geo-led/thin/land/refusal — proven live]; rows **anonymous** [E12: no PIN/address; the PN-hash stripped from the export]; the median already drives the number → **amount/low/high/method/rule byte-identical** [engine threads `build_reference(return_transactions=True)` → `MoJValuation.bracket_transactions` → Case-1 stash → b4-region market-led attach; the new `_keystone_comparables` builder caps at 8, newest-first]; the §4ب «مبنيّ-مجاناً» claim was **FALSIFIED** [rows discarded on the live path] but surfacing them is **modest + privacy-safe** [recon `docs/PHASE0_DEF_UX1_keystone_comparables_recon.md`]; isolated **25/25** + b37 R6 version-pin re-point [22/22] + DoD 392/15/45/broad **106/106** + local E2E [live GIS — value byte-identical 4 anchors; comparables PRESENT only on أبو هامور matched **n=37 shown=8**, absent on Marikh cost-led / V001 geo-led / apt refusal] + R14 390×844 [keystone in the open «كيف وصلنا», 8 rows dir=ltr, headline **٢٬٤٠٠٬٠٠٠ unchanged**, 0 console, no overflow] + live smoke **v209** [5-anchor value byte-gate identical to v208 (2.4M/2.4M/2.6M/3.8M/refusal); comparables present 56/565/21 **n=37** anonymous + CC BY 4.0, absent Marikh/V001/apt]; commit `95248fb`, CHANGELOG_v121 + §20.69. **⏭️ NEXT = DEF-UX1.1** [geo-led keystone — the `geo_v2` pool rows for widened/geo-full-led villas, e.g. V001] **+ the cost-led «considered-but-didn't-lead» pool** [a more delicate copy slice, e.g. Marikh] — both deferred from b38; then **DEF-UX8** affordability/LTV guards [🟡] · the lighter §4ب display items [UX4/UX6]; **DEF-UX5 RE-CLASSIFIED** [recon `PHASE0_DEF_UX5_en_toggle_recon.md`: NOT a 🟢 slice — a Gate-2 EN-localization project; backend 32.6% covered / frontend consumes 0 `_en` / 740 client-AR lines] · **DEF-UX15 still BLOCKED** [QARS data-drain]). **Prior: Sprint 2.22.0b.37 «كشف آليّة الكلفة (BUA/RCN/الاحتفاظ)» (DEF-UX9) SHIPPED v208** (🟢 frontend/value-invariant — display-only, reads only already-broadcast `value_stack.cost.*`; `api.py` UNTOUCHED, value byte-identical across all 5 anchors: the result-screen «🔍 كيف وصلنا» accordion now shows the DRC cost MECHANICS «🔧 آليّة الكلفة: BUA × كلفة الإحلال × معامل الاحتفاظ ← البناء المُهلَك + الأرض» + the broadcast assumptions [for the المهندس/المثمّن] — previously visible ONLY on the full/short report; gated on the 3 fields present, numbers in dir=ltr islands [Rule #25], the report/short-report rows UNTOUCHED [DRY]; isolated 22/22 + b36/b31 R6 re-points [22/22 · 36/36] + DoD 392/15/45/broad **105/105** + R14 390×844 [VILLA cost-led: line in «كيف وصلنا» + BUA 479/2200/0.5 + assumptions + value 2.4M unchanged · V001 market-led: line present BUA 602 + value 3.8M · APT refusal: no line/no crash · 0 console · no overflow] + live smoke v208 [served HTML carries «آليّة الكلفة (نهج DRC)»; 5-anchor value byte-gate identical to v207]; CHANGELOG_v120 + §20.68). **Prior: Sprint 2.22.0b.36 «رفض الشقق فوريّ صادق» (DEF-UX3) SHIPPED v207** (🟢 frontend/value-invariant — apartment/tower refusals read «{الشقق|الأبراج} غير مدعومة بعد — للفلل والأراضي فقط» + 🚧 + the «why», income CTA suppressed; gated on `(apartment_building||tower)&&!hasValuation` → income path UNTOUCHED [compound_large keeps its E20 CTA]; §20.42 pre-built then CC-re-measured; isolated 22/22 + DoD 392/15/45/broad 104/104 + R14 + live smoke v207; CHANGELOG_v119 + §20.67). **Prior: Sprint 2.22.0b.35 «حاسبة التمويل للمشتري» (DEF-UX16) SHIPPED v206** (🟢 frontend/value-invariant — `api.py` UNTOUCHED, value byte-identical across all roles [b24]: a display-only **financing calculator under the figure, gated on `d.audience==='buyer'`** — DRY reuse of the b25/b28 short-report `_srPayment` amortization (the «القسط ١٠٬٦٧٢» math) via a result-screen `bcRecalc()` with separate `bc*` ids [no collision with the short-report `sr*`]; «🏦 حاسبة التمويل التقريبية: [20]% دفعة أولى · [25] سنة · [4.5]% فائدة → القسط الشهريّ ≈ {payment} — استشر بنكك» [three live `oninput` inputs; defaults match the signed b28 contract]; payment DERIVED from `v.amount` → owner/seller/investor/valuer render exactly as before; isolated 17/17 + b34 R6 format re-point [15/15] + DoD 392/15/45/broad **103/103** + R14 390×844 [buyer: calc القسط ١٠٬٦٧٢ = the short-report figure; owner/investor: no calc; value byte-identical 2.4M/2.4M/5.4M; interactivity proven; 0 console; no overflow] + live smoke v206 [served HTML carries `bcRecalc`; 5-anchor value byte-gate identical to v205]; CHANGELOG_v118 + §20.66). **Prior: Sprint 2.22.0b.34 «الكثافة المقودة بالدور» (DEF-UX12, the study's «المفصل») SHIPPED v205** (🟢 frontend/value-invariant — `show()` derives `_dense` from the **already-broadcast** `d.audience` (recon falsified the «server field»: `evaluate_unified.py` writes `'audience': audience` top-level on every path, live-confirmed; `_acc(title,inner,open)` already takes `open` → **api.py UNTOUCHED**) and passes it as the `open` arg to the b31 «🔍 كيف وصلنا لهذا الرقم؟» evidence accordion: **investor/valuer → OPEN** [«الأدلّة أولاً»], **owner/buyer/seller → FOLDED** [the b31 simple-owner default]; single-purpose [only that accordion is density-driven; per-role delta content = later slices]; `_dense` placed after `syncTowerPair` [b22 fence pin]; value byte-identical across ALL 5 roles [b24]; isolated 15/15 + **5 sibling R6 re-points** [b15/b2p2/b31/b32/b33 — the `_acc(...);` literal + the b33 version pin; b22 fence preserved by placement] + DoD 392/15/45/broad **102/102** + R14 390×844 [5 roles: owner/buyer/seller folded · investor/valuer open · value byte-identical 2.4M/2.4M/5.4M · 0 console · no overflow 390==390] + live smoke v205 [served HTML carries `_dense`; `audience` broadcast live; 5-anchor value byte-gate identical to v204]; CHANGELOG_v117 + §20.65). **Prior: Sprint 2.22.0b.33 «المدخل: تحسين الافتراضيّ» (DEF-UX14) SHIPPED v204** (🟢 frontend/value-invariant — study §4 option D: **(a) HELP LINE on the address input** «هذه الأرقام على لوحة عنوان المبنى أو فاتورة كهرماء (المنطقة، ثم الشارع، ثم رقم المبنى)» [`.br-note` parity with the PIN hint, after the 3 fields — the simple owner saw 3 bare number fields with no source hint] + **(b) REASONABLE DEFAULT = local identity memory** [`_saveIdentity` in `run()` after `bd` is built + `_restoreIdentity` on `DOMContentLoaded` + `clearIdentity` + a «مسح» link; persists `{tab,zone,street,building,pin}` — **NOT** audience [single-purpose «identity»; the b24 «مالك» default untouched]; **first-visit empty → zero clutter for the new owner**; localStorage + in-memory fallback, no cookie/no server write — a24/DPIA consistent]; **value-invariant by construction** — `run()` reads zone/street/building OR pin and builds `bd` UNCHANGED, the store only PRE-FILLS the fields; `api.py` UNTOUCHED, value byte-identical [b24]; isolated 33/33 + b32 R6 format re-point [29/29, the project's own «no exact version pins» rule] + DoD 392/15/45/broad **101/101** + R14 390×844 [help line within 390 (39→351), save/restore/clear cycle + land-tab restore, 0 console, no overflow 390==390] + live smoke v204 [served HTML carries the help line «لوحة عنوان المبنى» + `_IDENT_KEY` + `_restoreIdentity` + `clrIdent`; 5-anchor value byte-gate identical to v203: امريخ 2.4M cost-led · V001 3.8M market geo_full · المعراض 2.6M e25 · أبو هامور 2.4M matched · شقق refusal]; CHANGELOG_v116 + §20.64). **Prior: Sprint 2.22.0b.32 «تبسيط شاشة التأكيد» (DEF-UX13) SHIPPED v203** (🟢 frontend/value-invariant — `showConfirm` simplified: (18) the full evidence panel DROPPED [lives on the result, b31 «كيف وصلنا» accordion; no pill] + (17) the E15 setbacks equation inline→**tooltip** [`cg-tip` ⓘ; the max-buildable «≈ N م²» NUMBER + «عدّله» CTA stay; the formula (dims + 5/3/3 + 60% cap, LRM) on hover] + (19+20) `pbRows(pb, basisOnly)` prints PIN then early-returns [utilities + survey-vintage age leave the default view; the age message already lives at the refine «عمر البناء» field; other call-sites byte-identical]; KEEP for the owner = basis review + PIN + the muted preliminary range + the b20 dual-evidence line + «تابِع» + the full-report escape; value byte-identical [b24]; isolated 29/29 + 4 sibling re-points [b2p2/b2p3/b27/b31, R6/Lesson-2] + siblings green WITHOUT re-points + DoD 392/15/45/broad **100/100** + R14 390×844 on the live امريخ cost-led fixture [confirm = basis + الرقم المساحي 54360025 + «≈ ٣١١ م² ⓘ — عدّله»; evidence panel/utilities/age/inline-formula GONE; b20 dual-evidence line stays; 0 console errors; no overflow 350<390 · 390==390] + live smoke v203 [5-anchor value byte-gate identical to v202]; CHANGELOG_v115 + §20.63). **Prior: Sprint 2.22.0b.31 «طيّ TIER-1 للمالك» (DEF-UX11) SHIPPED v202** (🟢 frontend/value-invariant — the «9-note parade» [value-floor · HBU · old-stock · cost-triangulation · leadership · age-honesty · resurvey · cost-value-line · market-dispersion] + the full evidence panel fold into ONE collapsed «🔍 كيف وصلنا لهذا الرقم؟» accordion [the `how` buffer, first in t2]; TIER-1 = the 5-core [range + median + «ليس معتمداً» + evidence pill + the button]; condition/teardown/luxury/age-sensitivity/moj-n STAY t1 [Rule #38 — the NAMED 9 only]; value byte-identical [b24, مبدأ «الرقم واحد للجميع»]; isolated 36/36 + b15 re-pointed 50/50 [lines 72/98 — R6/Lesson-2 stale pins] + siblings green WITHOUT re-points + DoD 392/15/45/broad **99/99** + R14 390×844 on the live امريخ cost-led fixture [parade behind the collapsed accordion, 5-core in the figure, 0 console errors, no overflow] + live smoke v202 [5-anchor value byte-gate identical]; CHANGELOG_v114 + §20.62). **Prior: b30 «share-evidence copy» (v201, CHANGELOG_v113)** + docs-only [risk/issue ledgers rebuilt lean + the 10-persona LIVE review `ISSUES_LOG §4ب` DEF-UX1..10 + the persona-simplicity STUDY `docs/STUDY_persona_simplicity_and_entry_v1.md` + `ISSUES_LOG §4ب-2` DEF-UX11..18]. **⏭️ NEXT = DEF-UX3** (apartment refusal — honest «الشقق غير مدعومة بعد — للفلل والأراضي فقط» card, replacing the misleading «يتطلب: الإيجار» CTA; 8/10 personas, 🟢 frontend/copy, Gate-2-light; **DEF-UX15 BLOCKED** on a QARS data-drain [recon b35: khazna geo-restricted locally + 162k-point dump needs a probe deploy — ISSUES_LOG §4ب-2]; parallel §4ب track: UX9 BUA/RCN [broadcast-ready] · UX1 keystone comparables [Gate-2+recon] · UX8 affordability guards on the UX16 calculator). The per-role delta content + new roles (heirs/bank = UX17, Gate-2) branch from the UX12 hinge; the §4ب persona features (UX1 keystone comparables [Gate-2+recon] · UX3 apartment refusal · UX9 BUA/RCN · UX8 affordability guards on the UX16 calculator) are the parallel track. **NO open engineering gate.** Authoritative forward state = the «⚡ LIVE NOW» bullet below + Session_Log §20.61 + `ISSUES_LOG §4ب`/§4ب-2 + the study; everything older in THIS line is HISTORICAL. ⟦historical stamp 2026-06-01⟧ (Sprint 2.22.0a.16 deployed — pre-activation capture privacy-hardening: UUID-only key [address-embedding valuation_id NOT stored], street/building Fernet-encrypted [gated on `CAPTURE_ENC_KEY`], 180-day retention + dormant aggregate/purge/erase, free-text `note` removed, output label «التقدير السوقي» [provisional]. Capture STILL DORMANT; additive, NO valuation-logic change; ACTIVATION counsel-gated [§8.1 PDPPL + §8.2 cross-border + gate-11 security pass]. CHANGELOG_v68 + Session_Log §20.16). **LIVE: engine `thammen-sprint2p22p0a16-precapture-privacy-hardening` · api/health 3.1.0-sprint2.22.0a.16 · Heroku v155 · commit `94075f2` · CHANGELOG_v68** (DORMANT, privacy-hardened — UUID-only key [valuation_id NOT stored; active mode returns it as capture_id; feedback FK via prediction_id], street/building Fernet-encrypted [gated on `CAPTURE_ENC_KEY`], 180d retention + dormant aggregate/purge/erase, `note` removed, label «التقدير السوقي» [provisional]. SHA-of-enumerable REJECTED → Operational #62. Two-lane smoke v155 BYTE-IDENTICAL on the 4 anchors [2.4M/4.5M/2.6M/refusal; NO capture_id → dormant]; `/api/feedback {prediction_id}` → {accepted, stored:false}; `note`/`valuation_id` → 422. ACTIVATION counsel-gated [§8.1 PDPPL + §8.2 cross-border + gate-11 security pass: verify Fernet round-trip on Heroku + short PG backup retention + backup-erasure runbook]. Isolated 26/26 + DoD 392/15/45/58; origin in sync `94075f2`. **Prior: Sprint 2.22.0a.15 — beta instrumentation (DORMANT)** (DORMANT — capture writes NOTHING until BOTH `EVAL_CAPTURE_ENABLED=true` AND a counsel-cleared Postgres exist; two-lane post-deploy smoke [CC browser-UA curl + Anas] BYTE-IDENTICAL on the 4 anchors: 56/565/21 = 2,400,000, 54/541/6 = 4,500,000, 55/296/13 = 2,600,000 comparison_thin n=8, 52/903/90 = refusal; `/api/feedback` dormant → {accepted, stored:false}; extra field → 422. ADDITIVE BACKEND — NO valuation-logic change. ACTIVATION counsel-gated [§8.1 PDPPL + §8.2 cross-border]; add-on NOT provisioned [RISK_REGISTER R11]. Tooling: CC POST smoke = browser-UA curl, Cloudflare 1010 blocks urllib [Operational #61 / R12]. Isolated 27/27 + DoD 392/15/45/58; origin in sync `8d6f304`. **Prior: Sprint 2.22.0a.14 (vi) — bracket honest-range + window disclosure** (USER-VISIBLE: 20 of 37 reliable villa bracket cells [7 a13-rescued + 13 pre-existing] were dispersed yet showed as clean reliable points — they now get the honest-range/🟡 indicative/MUC treatment; anchors stay clean [Abu Hamour disp 0.208, Marikh 0.197 < 0.30]. Live smoke v153 4/4: Abu Hamour 56/565/21 = 2,400,000 [value IDENTICAL, NOW «(نافذة 36 شهراً)» + window_used «37 معاملة، منها 28 خلال 24 شهراً» → CHECK-3 closed on the anchor], Marikh 54/541/6 = 4,500,000 [unchanged, widened range-headline], 55/296/13 = 2,600,000 comparison_thin n=8 [unchanged, not gated], 52/903/90 = refusal. **R10 generalized → CLOSED-by-a14** [bracket path had no dispersion gate; 20 dispersed reliable cells = 7+13]. Boundary: 3 cells within ±0.006 of T=0.30 may flip on a MoJ refresh [expected, not a regression — hysteresis if it bites]. Known-minor: window suffix fires on `comparison_bracket` only, not `comparison_thin` [thin already caveated]. Fast-follow: a DIRECT live hit on a gated bracket cell [الغرافة/العب 600-900] — gate DECISION proven offline e2e + threading/application proven live. Isolated 19/19 + DoD 392/15/45/57; origin in sync `78ffd9b`. **Prior: Sprint 2.22.0a.13** — thin-cell credibility (Heroku v152, `c366d66`, CHANGELOG_v65; USER-VISIBLE: thin villa bracket cells now borrow their OWN 36mo comps via credibility shrinkage instead of the dispersion-prone widened path — +10 cells upgrade thin→reliable, reliable cells move ~0% [max 2.2%], <5 floor preserved, ppm² untouched. Live smoke v152 4/4: Abu Hamour 56/565/21 = 2,400,000 [IDENTICAL to a12; comp count 28→37 via n36 tiering], Marikh 54/541/6 = 4,500,000 [byte-identical — A16-starved bracket → geo path, NOT shrunk], 55/296/13 = 2,600,000 comparison_thin n=8 [no upgrade, gentle ~−4%], 52/903/90 = refusal; api/health a13/v152. **OPEN — R10 temporary honesty gap:** 7/10 rescued cells are dispersed [ppm² ≥0.30] yet present as clean `comparison_bracket` reliable points with no honest-range; AND CHECK-3-live: the bracket SUCCESS `source_ar` discloses NO window for ANY villa cell [Abu Hamour «وسيط 37 معاملة» spans up to 36mo, undisclosed]. **NEXT = (vi) URGENT** [bracket-success surface only, presentation/copy, NO value change: (a) extend the a10 dispersion honest-range to the bracket path → closes R10; (b) disclose 24-vs-36mo window basis in source_ar when n is a 36mo count → closes CHECK 3, Gate-2 copy sub-decision]. Isolated 16/16 + DoD 392/15/45/56; origin in sync `2bfec00`. A16 = still the only Marikh lever [R9, own sprint after a live trace]; A7 still open; LAND bracket path unchanged [villa-only]. **Prior: Sprint 2.22.0a.12** — A2 built-type stratification (Heroku v151, `9fa375c`, CHANGELOG_v64; USER-VISIBLE: villa comparable pool is now PURE-villa — house [بيت/مسكن], فيلتان [−6 to −10% discount product], and compound rows removed → pooled villa ppm2 median ~+9.7%; net A1+A2 ~+4.5% above the original contaminated median. HEADLINE effect VARIABLE: reference anchors STABLE [Abu Hamour 56/565/21 = 2.4M, Marikh 54/541/6 = 4.5M] because the bracket headline uses the robust TOTAL-PRICE median [removed house rows weren't at the median position]; anchors' under/over-valuation = CONDITION → Sprint B, not stratification. **Subject side CANNOT distinguish HOUSE from VILLA** [QARS subtype 1 = "Villa/House", no HOUSE AssetType] → house subjects pool as standalone_villa [live-confirmed 55/296/13]; house-subject fix DEFERS to B. DRY `built_type.py` at both comp sites, composes with A1 usage filter; `categorize`/`_categorize` KEPT for the out-of-scope `compute_trend` + geo non-villa categories [Rule #39]. Isolated 28/28 + DoD 392/15/45/55; live smoke 4/4 [incl. `comparison_thin n=8` proving graceful thinning]; origin in sync `9fa375c`. **NEXT = window-fallback 36mo-cap + light shrinkage** [recon F: 36mo captures ~half the +8-13% staleness drift]. Prior: Sprint 2.22.0a.11 — A1 residential-usage filter on the villa comparable pool (Heroku v150, commit `ec0d1b9`, CHANGELOG_v63; villa headlines shed non-residential-usage comps [~+101%] → villa median ~−4.75%; 56/565/21 → 2.4M [contamination removal, condition-blind, ~2.5–2.8M with-condition pending B]; isolated 13/13 + DoD 392/15/45/54; §20.11). Prior: Sprint 2.22.0a.10 — Stage-1 honest range (USER-VISIBLE: widened villas with a dispersed comp pool now show an indicative P25–P75 range + lowered tier + widened MVU + AR/EN disclosure, the median retained as the central estimate; `_stage1_dispersion_gate` T=0.30 on the geo_value widened paths — live Marikh 54/541/6 → 3.3–5.4M indicative, Maamoura 56/647/6 → 2.9–4.4M indicative, Abu Hamour 56/565/21 bracket = 2.5M unchanged; backend-only, no index.html; isolated 16/16 + DoD 392/15/45/53; origin in sync `21c5fe3`. **Built-type/condition blindness [RISK_REGISTER R7] is BIDIRECTIONAL & affects BOTH paths** — over-anchors below-avg-condition [54/541/6 widened, RE-OPENED], under-anchors above-avg-condition [56/565/21 bracket → defensible ~2.5–2.8M, NOT the 2.5M point]; a10 = necessary-NOT-sufficient, real fix = Gate-2 (c) built-type stratification via 2.22.0b Stage-2 Q&A). Prior: Sprint 2.22.0a.9 — widened-path age/quality elasticity, facet a (Heroku v148, CHANGELOG_v61; the geo_value widened headline applies the age/quality slice clamped ±0.10; the a9 ship-time 681≈682/ft² "match" was **later OVERTURNED** — coincidence, 54/541/6 RE-OPENED, §20.10.1 / R7). Prior: Sprint 2.22.0a.8 — RICS/IVS 2025 citation correctness (Heroku v147, commit `1e07a2a`, CHANGELOG_v60): added the AVM models standard VPS 5 / IVS 105 + AVM-not-standalone disclosure on a secondary collapsible surface [the 2.22.0a.4-deferred surface]; remapped ALL stale citations — approaches VPS 4→VPS 3 / IVS 103, HBU→VPS 2 / IVS 102 [genus, triple-confirmed], scope→VPS 1, VPN 13→VPGA 10 [D5 widened]; bare `methodology_ar` untouched; copy-only — valuations unchanged [villa 56/565/21 = 2.5M = v101]; regression 392/15/45/51 + 43/43; origin in sync `b560920..1e07a2a`). Prior: Sprint A14 lever 2 — geometric_factors parallelized, **A14 CLOSED** (Heroku v146, CHANGELOG_v59, cold villa 200 @~15s, was 503@31s; lever 1 deferred + H_A-cleared/ready). The production-state snapshot block below + Session_Log §20 are authoritative. Prior: 2026-05-29 Sprint 2.22.0a.4 — Disclosure & Framing Honesty (Heroku **v140**, commit `f7870a3`, CHANGELOG_v55; engine `thammen-sprint2p22p0a4-disclosure-framing-honesty`). `methodology_ar` → universal bare line «أساس التقدير هو منهج المقارنة بالمبيعات.» (dropped «توفيق ثلاثي الطرق» + Latin); main-path Layer A fold (6→5, 5 genuine caveats preserved); D/C4 canonical from 2.22.0a.2 untouched. Multi-AI Rule #54 (GPT-5+Gemini) Path A bare-line. Live smoke villa 56/565/21 (200 on A6 retry @22s) + apt 52/903/90 PASS. **Arabic-Surface arc since 2.21.4:** 2.22.0a (v50) → a.1 QARS fallback (v51/Heroku v132) → a.2 content fixes (v52) → 2.16.17 security (v53) → a.3 honesty (v54/v139) → a.4 framing (v55/v140). Full bridge + deferred items in Session_Log §18. **NOTE:** the production-state snapshot block below predates the 2.22.0a arc — trust the four updated lines there + Session_Log §20.x (current: §20.47) over the older body until a full snapshot rewrite.)

## Quick orientation

Read these files in order before any technical work:

@./docs/Project_Instructions.md
@./docs/Session_Log.md
@./docs/Empirical_Findings.md
@./docs/Custom_Instructions.md
@./docs/Session_Update_2026-05-19.md
@./docs/Operational_Rules.md

**Most recent state: see the «🧭 CURRENT STATE» block (top) + Session_Log §20.45–§20.47 — current = b13 / Heroku v182; the production-snapshot block + the 2.21.x notes below are HISTORICAL. Full hybrid arc complete:** Sprint 2.21.2
(foundation, v107) → Sprint 2.21.3 (T2 PF Lusail apartments, v124) →
Sprint 2.21.4 (T3 Aryan/City Avenues + status-aware hybrid, v125). Live
H_WALK PASS for H1 + H11 + H2 (kill switch). H10 UI rendering deferred
to Sprint 2.21.5. Session_Log §15 still holds the most recent NARRATIVE
(Sprints 2.18.1 + 2.18.1.1); §16 (the hybrid arc 2.21.2 → 2.21.3 →
2.21.4) added 2026-05-25. Pre-Sprint artifacts since:

- `2p21p1_pre/CHANGELOG_pre_2p21p1.md` — MME smoke (anonymous Directus
  token, kpi29 schema discovered, rent paths verified dead). Sprint 2.21.1
  deferred pending DevTools capture.
- `2p22p0_pre/CHANGELOG_pre_2p22p0.md` — 3-stage architecture exploration
  audit. H5 FALSE: apartments are a data problem, not latency. Sprint
  2.22.0 deferred. H1+H3+H4 evidence preserved for future UX-refactor Sprint.
- `2p21p2_pre/` — Sprint 2.21.2 §5 audit probes (MoJ Lusail apt count = 0,
  PropertyFinder reachable, arady root only). Sprint 2.21.2 then shipped.
- `2p21p3_pre/CHANGELOG_pre_2p21p3.md` — T2 connector smoke from Heroku.
  4 of 5 TRUE. arady canonical search = `/listings`. PropertyFinder DOM
  duplicates listing nodes ~6× (raw 142 → 24 unique on Lusail page 1) —
  connector MUST deduplicate by canonical URL or listing ID.

§14 covered Sprint 2.18.0 earlier that morning; §13 covered Sprint 2.21.0.9
Stage 1; §11-12 covered the Land Arc through Sprint 2.21.0.7.1. The
`Session_Update_2026-05-19.md` file is an older delta (Bug A11 era) kept
for history. Newest operational rules: `Operational_Rules.md` #43–#61
(latest written = #61 "CC post-deploy POST smoke = browser-UA curl, not urllib"; #55/#56 reserved-pending).
Newest empirical rules: `Empirical_Findings.md` E13–E23 (Rule E3 itself
expanded to 8 numbered constraints by Sprint 2.21.2 — listings now allowed
tier-weighted entry via `hybrid_valuation_v1()`).

-----

## Current production state (snapshot)

> **🧭 CURRENT STATE + NEXT STEP (Rule #65a — CC maintains; read first at the #57 handshake):**
> - **⚡ LIVE NOW (2026-06-17) — live = b52 / Heroku v225 (Sprint 2.22.0b.52 «الواجهة الرشيقة: شاشة النتيجة / result-screen lean» — 🟢 FRONTEND-ONLY / VALUE-INVARIANT: the result-screen appraiser fine-print → the «كيف وصلنا» fold + the full MUC legal clause folds behind its chip (`_mucFold`); the chip + «ليس معتمداً» stay always-visible; the detailed report stays detailed; commit `bc6aaaa`, CHANGELOG_v134, §20.82; live 5-fixture value-invariance gate identical to v224 — the 4 legacy fixtures are engineering value-invariance fixtures, NOT value anchors; V001 = the sole calibration anchor). Prior: live = b51 / Heroku v224 (Sprint 2.22.0b.51 «تنظيف ازدحام التقرير / report declutter» — 🟢 FRONTEND-ONLY / VALUE-INVARIANT: cost-value de-dup + DEF-12 three-value reorder so the key numbers lead the report; commit `64523aa`, CHANGELOG_v133, §20.81; live 5-anchor byte-gate identical to v223). ⚠️ **The detailed «⚡ LIVE NOW» body that follows is FROZEN at the b46-era and is HISTORICAL** — the authoritative current state = the top «Last update» line + Session_Log §20.x (current §20.81) + RISK_SUMMARY (b47→b51 were tracked there, not in this bullet). ⟦historical from here⟧ — live = b46 / Heroku v220 (Sprint 2.22.0b.46 «طبقات بوّابة الموافقة / consent-gate layering»)** — 🟢 FRONTEND-ONLY / VALUE-INVARIANT (`index.html` only — the gate restructure + a scoped CSS block; `api.py`+engine UNTOUCHED; `evaluate_unified.py` = 2 version lines; live **5-anchor value byte-gate identical to v219**): **Sprint C — slice 1 of the layout-review payoff (the review's ★4).** The consent gate is every user's literal FIRST frame — a wall of **5 stacked detail cards** (ما هذا / ما ليس هذا / ماذا يغطّي / حدود نعرضها / دورك) + the consent box, with the CTA «أوافق وأكمل» **below the fold** on 390×844 (the highest cognitive load before any value — a bounce point). b46 folds the 5 cards into a collapsed `<details class="bg-more">` «اعرف المزيد عن النسخة التجريبية ↓» → the first frame is now tight: logo + the title (which already states what ثمّن is) + the beta sub-line + the «اعرف المزيد» fold + the consent note «… وليست تقييماً معتمداً» + the affirmation «أُقرّ بأنني فهمت …» + **the CTA (now ABOVE the fold)** + the Terms link + the existing English fold. **Every word of the signed a24 text is preserved** (the 5 cards are LAYERED, not removed — GDPR/ICO layered-notice best practice; `role="dialog" aria-modal` + the affirmative-consent requirement unchanged). A **scoped** `.bg-more:not([open])>ul{display:none}` guarantees the fold collapses (the measured Chromium `<details>` content-hiding quirk — author CSS computes the `<ul>` as `block` when closed) — scoped to `.bg-more>ul` so the t2acc / thmr-grp / bg-en accordions are untouched. **Single-purpose (#38):** Sprint C = 3 distinct UI changes → CC split it; the result-screen hero (★1) is the most-delicate slice (it would REVERSE the signed b3 «range-as-lead» + supersede the a8 «calc-block exactly-once» contract + re-point ~7 tests [`الوسيط` is pinned by b3/b15/b17/b19/b24/b26/a2]) → its own slice; the gate (near-zero test surface — gate content has 0 test pins, only b27 checks `class`/`id`) went first. Verified: py_compile OK + DoD aggregator **392 ALL COUNTS MATCH** / security **15/15** / surface **45/45** / broad walk **110/110 ALL GREEN** (127.7s) — **ZERO test re-points** (b27 = 23/23, the gate `class`/`id` unchanged) + **live preview 390×844** (measured: the gate card `scrollHeight` = **540px collapsed** [fits the 92vh card → no internal scroll] vs **1013px open** → the fold hides **473px**; `<ul>` `display:none` closed / `block` open; `li` count = **5** [all preserved]; open-by-default = false; **the CTA bottom = 587 ≤ 844 → above the fold** [was below]; **0 console errors**; the screenshot tool timed out — §20.34 hiccup) + **live two-lane smoke v220** [browser-UA #61: `/api/health`=b46 / qars healthy; served `index.html` carries `class="bg-more"` + «اعرف المزيد» + the 5 cards (markers ما هذا؟/حدود نعرضها/دورك all present); **5-anchor value byte-gate identical to v219** — 54/541/6 2.4M cost_led · 56/647/6 3.8M geo_full · 55/296/13 2.6M e25 · 56/565/21 2.4M matched · 52/903/90 refusal]. commit `47e2ade` (origin in sync), CHANGELOG_v129, §20.77. **⏭️ NEXT (Sprint C remainder):** (1) **the result-screen hero (★1, the highest-value slice)** — its own focused sprint: it **evolves the signed b3 range-as-lead hierarchy** (lead with a confident central figure + a slim range bar, KEEPING the range + the RICS clause) + **supersedes the a8 calc-block contract** + recolors the MUC chip red→amber + demotes the green scope card; ~7-test re-point surface → careful + a rendered review (the PO already approved the *direction* via the now-vs-proposed mockup) · (2) **the home trust strip** (a 3-step «أدخل العنوان ← نحلّل بيانات العدل ← نتيجتك» + a readable «مبني على صفقات وزارة العدل» line) — additive · the **logo** stays the existing raster (designer brief sent — may take ~a year; we build on the current logo, a light-chip bridge behind it on navy surfaces if needed) · OR the binding constraint #1 (beta launch + GT collection, D-3 — PO decision). **⚡ Prior — b45 / Heroku v219 (Sprint 2.22.0b.45 «توحيد العلامة / brand unify — tokens + font + CDN»)** — 🟢 FRONTEND-ONLY / VALUE-INVARIANT (CSS tokens + font-family + a `<link>` removal; `api.py`+engine UNTOUCHED; `evaluate_unified.py` = 2 version lines; live **5-anchor value byte-gate identical to v218**): **the KEYSTONE of the layout-review roadmap (Sprint B)** — unifies the app's TWO design systems into one. The review's top brand finding: the app-shell (home/form/confirm/result) ran **Tajawal (Google-Fonts CDN) + navy `#12344D` + bronze `#A68252`** while the reports ran **IBM Plex (local) + navy `#16324F` + bronze `#A4814A`** → the brand visibly shifted mid-session. **Recon-clean (#33):** the old brand hex lives ONLY in the `:root` token defs, `'Tajawal'` is 13 CSS `font-family` decls + the 2 CDN lines, **0 Tajawal in the JS**, and the IBM Plex `@font-face` is already global + already served live (the `_THMR_FONT_WHITELIST` route) → so the unify is **zero new downloads, no api.py change**: (1) **tokens → canonical thmr values** (`--primary`→`#16324F`, `--bronze`→`#A4814A`, `--bg`→`#FBF8F2`, `--bronze-h`/`--bronze-g` aligned) with the legacy token NAMES **kept (aliased)** → ~40 sprints of `var(--primary)`/`var(--bronze)` class usage keep working untouched; (2) **font → IBM Plex Sans Arabic app-wide** (`body` + all 13 `font-family:'Tajawal'` → IBM Plex; the app shell now matches the premium report typography; IBM Plex ships 400/500/600/700 → `font-weight:800` maps to 700, same as the report already does); (3) **dropped the Google-Fonts CDN `<link>`** (the `preconnect` + the Tajawal stylesheet) → **closes the render-blocking, pre-consent third-party request (a PDPPL win)**; no Tajawal is fetched anywhere anymore. `.thmr` (the reports) is now a layout/theme scope on the SAME tokens + font — the two systems are ONE. Verified: py_compile OK + DoD aggregator **392 ALL COUNTS MATCH** / security **15/15** / surface **45/45** / broad walk **110/110 ALL GREEN** (133.6s) — with **one re-point (R6/Lesson-2):** `test_sprint_2_22_0b25.py`'s «font INSIDE .thmr only (no global swap)» pinned the م2/D7 scoping (body NOT IBM Plex) that b45 INTENTIONALLY inverts → re-pointed to «IBM Plex is the unified app font — .thmr + global body (b45)» (b25 = 77/77; no other test pinned Tajawal/CDN/old-hex — the only other match was a comment) + **live preview 390×844** (computed: `body` font = IBM Plex · `--primary` = `#16324F` · `--bronze` = `#A4814A` · `--bg` = `#FBF8F2` · `.hbtn` bg = `rgb(164,129,74)` · `.rt` = `rgb(22,50,79)` · IBM Plex loaded · **no `link[href*=googleapis]`**; **NO horizontal overflow on ALL 5 screens** [home/form/confirm/result/short-report — `scrollW==clientW==390`]; **0 console errors**; the screenshot tool timed out — the §20.34 hiccup) + **live two-lane smoke v219** [browser-UA #61: `/api/health`=b45 / qars healthy; served `index.html` carries `--primary:#16324F` + `body{…'IBM Plex Sans Arabic'…}` + **0 `googleapis`**; **5-anchor value byte-gate identical to v218** — 54/541/6 2.4M cost_led · 56/647/6 3.8M geo_full · 55/296/13 2.6M e25 · 56/565/21 2.4M matched · 52/903/90 refusal]. **Carried (Rule #42):** one inert `Tajawal` remains in the LIVE served HTML — a CSS **comment** (the calc-block explainer; no element uses Tajawal, the font isn't loaded); the source is now cleaned, it goes live with the next deploy (no redeploy for a comment) · the **logo** stays the existing raster (the SVG/light/compact variants are with the designer — brief sent; wire on arrival; a light-chip bridge behind the logo on the navy report header can land separately) · the desktop **form-band overflow** is the pre-existing fixed-580px-column quirk (mobile-first) — unchanged by b45, the 390 target is clean. commit `6e58fbd` (origin in sync), CHANGELOG_v128, §20.76. **⏭️ NEXT = Sprint C (the payoff — the result-screen `thmr` hero [confident central figure + slim range bar + amber-not-red reservation chip] + consent-gate layering + home trust strip; now CHEAP because the tokens + font are unified)** · OR the binding constraint #1 (beta launch + GT collection, D-3 — PO decision). **⚡ Prior — b44 / Heroku v218 (Sprint 2.22.0b.44 «تباين النصّ القانونيّ / a11y AA contrast»)** — 🟢 FRONTEND-ONLY / VALUE-INVARIANT (CSS color tokens only; `api.py`+engine UNTOUCHED; `evaluate_unified.py` = 2 version lines; live **5-anchor value byte-gate identical to v217**): the **legal/disclaimer/MoJ-attribution/recency** text moves `--light` (`#9CA3AF` ≈ 2.3:1 — below WCAG AA) → `--muted` (`#6B7280` ≈ 4.5:1 — AA pass) on `.disc`/`.src-credit`(+`.en`)/`.hfoot`/`.cg-sub`/the Terms links (8 surgical swaps); **decorative `--light` UNCHANGED** (`.tbar-st`/`.trend-labels`/`.lprog`/`.cg-unit`). **The FIRST slice of the LAYOUT-REVIEW roadmap** — the PO asked for a brand-designer + software-engineer review; CC rendered the LIVE site at 390×844 across all 5 screens + read the full CSS + ran a 4-lens deep-review workflow (33 findings). **Diagnosis: «the polish order is inverted»** (heaviest load = the consent wall first; the most premium surface = the short-report navy hero last/optional; the screen everyone judges = the result, least finished) + **TWO mismatched design systems** (app shell: Tajawal CDN + navy `#12344D` + bronze `#A68252`; thm-report: IBM Plex LOCAL + navy `#16324F` + bronze `#A4814A`). **Recon RESHAPED Sprint A** (the §20.26/§20.29 pattern): the **727KB raster `logo.png` recompression is BLOCKED** (no local image tooling — the `convert` on PATH is Windows' FAT→NTFS util; no Pillow; can't recreate the SVG) → deferred pending an operator-supplied optimized asset; **self-hosting Tajawal = throwaway** (≈12 subset files + a `_THMR_FONT_WHITELIST` change, undone when Sprint B unifies the font) → so Sprint A = the **one unanimous zero-risk AA contrast win**, and the **font/CDN-privacy** (Tajawal loads from Google **before the consent gate** — a PDPPL point) + **brand-color split** move to **Sprint B (unify to the already-local IBM Plex + alias the legacy tokens, ZERO downloads)** → then **Sprint C (result-screen `thmr` hero + consent-gate layering + home trust strip)**. Verified: py_compile OK + DoD aggregator **392 ALL COUNTS MATCH** / security **15/15** / surface **45/45** / broad walk **110/110 ALL GREEN** (132.1s) + **live preview 390×844** (computed-colors the channel: `.disc`/`.src-credit`/`.hfoot` = `rgb(107,114,128)` = `--muted`, decorative `.tbar-st` = `rgb(156,163,175)` = `--light` unchanged; 0 console errors; the screenshot tool timed out — §20.34 hiccup) + **live smoke v218** [browser-UA #61: `/api/health`=b44 / qars healthy 162,517; served `index.html` carries `.disc{…--muted…}`+`.src-credit{…--muted…}`+`.hfoot{…--muted…}`; **5-anchor value byte-gate identical to v217** — 54/541/6 2.4M cost_led · 56/647/6 3.8M geo_full · 55/296/13 2.6M e25 · 56/565/21 2.4M matched · 52/903/90 refusal]. **Tooling note:** a first spurious `None×5` smoke = a shell-harness bug (`${p%%:*}` split name↔body on the FIRST colon, but the JSON payload contains colons → truncated `{"zone"` → 422-in-2ms) + a cold dyno post-restart; the **heroku logs caught it** (#33/#36/E14 — a broken harness tests nothing); CSS-only → value-invariant by construction. commit `cf37fe0` (origin in sync), CHANGELOG_v127, §20.75. **⏭️ NEXT = Sprint B (the keystone — token + font unify; captures the CDN/privacy win via local IBM Plex)** · OR the binding constraint #1 (beta launch + GT collection, D-3 — PO decision). **⚡ Prior — b43 / Heroku v217 (Sprint 2.22.0b.43 «نسخة-المُشغِّل: العنوان بلا بيانات شخصية» / operator copy: keep the address, strip personal data)** — 🟢 ADDITIVE / DORMANT-by-default / VALUE-INVARIANT (`api.py` + `index.html` + the valuation engine UNTOUCHED; `evaluate_unified.py` = the 2 version lines only; live **5-anchor value byte-gate identical to v216**): the operator report-copy now **KEEPS the property ADDRESS** but **STRIPS the two Kahramaa utility ACCOUNT numbers** (`property_basis.electricity_no` + `water_no` — the *only* person-identifying fields in the engine `result`, confirmed by a full-blob PII scan: **0** phone/email/national-id/contact) from **BOTH** the email body **AND** the JSON attachment, via a new pure `_scrub_personal(result)` (deep-copy → **NEVER mutates `result`**, the isolation invariant). **KEPT = property/parcel data:** address, cadastral **PIN**, district, GPS, valuation/range/method/MUC, age, fingerprint, comparables (the copy stays a useful searchable memory + regenerable on thammen.qa from `address + report_ref`). **The arc:** the PO's full address-**redaction** brief was first SIGNED, then **REJECTED** («كلا … اريد ان يصلني العنوان كذلك. لكن بدون بيانات شخصية») → CC reverted to the b42.2 rich email (PO «ممتاز») → **b43 = the precise middle.** `report_mailer.py`: `_PERSONAL_PB_FIELDS=("electricity_no","water_no")` + `_scrub_personal` (used by `build_email` for the attachment); `_summary_fields`/`_html` drop the «كهرباء» bit. Verified: isolated `test_sprint_2_22_0b42.py` **48/48** (42→48; SAMPLE += `water_no` per E14; +6 b43 checks incl. **ISOLATION — `build_email` did not mutate the caller's result**) + DoD aggregator **392 ALL COUNTS MATCH** / security **15/15** / surface **45/45** / broad walk **110/110 ALL GREEN** (68.9s) + `import api` OK [14 routes, `_MAIL_OK=True`, dormant] + **real-result render proof** (Marikh: address+PIN+age kept; electricity `161418`+«كهرباء»+water `131980` gone from body+attachment; original `result` untouched) + **exfil-paths clean** (the dormant a15/a16 capture never stored the utility numbers — only zone-plaintext + Fernet-encrypted street/building + value; `report_mailer` is the ONLY path carrying them) + **live two-lane smoke v217** [browser-UA, #61: `/api/health`=b43; **5-anchor value byte-gate identical to v216** — 54/541/6 2.4M cost_led · 56/647/6 3.8M geo_full · 55/296/13 2.6M e25 · 56/565/21 2.4M matched · 52/903/90 refusal; all `cap_id=None` → capture dormant] + **email-send proof** [the 5 evals each fired an operator copy → **no `report-copy email failed` / Resend-1010 logged** = the copies delivered with the scrub]. commit `1c23904` (origin in sync), CHANGELOG_v126, §20.74. **The a24 «stores nothing» notice update remains OPEN** (PO wording unsettled; the rejected heavy-redaction posture is **NOT** to be re-applied) — the gate for **beta-WIDE** use only (Rule #39), a non-issue for the operator's own testing. **⏭️ NEXT remains the binding constraint #1 (beta launch + GT collection, D-3 — PO decision); this operator-side memory complements it.** **⚡ Prior — b42.2 / Heroku v216 (Sprint 2.22.0b.42.2 «نسخة-المُشغِّل بالبريد» / operator report-copy email + UA-fix + clean-fields)** — ✅ **email-send LIVE test PASSED — the report-copy memory is LIVE + ACTIVE + inbox-confirmed** (`RESEND_API_KEY`+`REPORT_COPY_EMAIL` set on Heroku; recipient `ans_hashim@hotmail.com`; Anas confirmed the email + 41KB JSON attachment arrived). **b42.2** then cleaned two email-body fields (the age was dumping the raw `building_age_estimate` dict → «العمر ≥ N سنة (تقديري)»; the asset slug `standalone_villa` → «فيلا منفردة» via `service_scope.label_ar`; the test fixture matched to the real production shape — E14; isolated **42/42**, DoD 392/15/45/broad **110/110**, redeployed v216, clean render verified on a real result). 🟢 ADDITIVE BACKEND / **DORMANT-by-default** / **VALUE-INVARIANT** (`index.html` UNTOUCHED; amount/low/high/method/rule byte-identical; the email is a post-response side-effect that NEVER mutates `result`): **the operator now receives an email copy of EVERY report — the inbox IS the memory** (durable, searchable by `report_ref`; NO database to run/back up, since Heroku's FS is ephemeral). New pure `report_mailer.py` [gated on `RESEND_API_KEY`+`REPORT_COPY_EMAIL`, lazy import, NEVER raises, NEVER mutates `result`]: `build_email(result,inputs)` [PURE — subject `[ثمن] {report_ref} — {address} — {value} ر.ق`, RTL HTML summary + the **FULL result JSON base64-attached** (complete archive); default sender = Resend test `onboarding@resend.dev`, which delivers ONLY to the account owner → a structural «my reports only» until a domain is verified] + `send_report_copy` [dormant→False/no network; active→one stdlib-urllib POST to `api.resend.com/emails`; failures swallowed]. `api.py` wires it via a guarded `BackgroundTasks` seam (`if _MAIL_OK:`) in BOTH `/api/evaluate*` handlers, right after the dormant a15 capture — runs AFTER the response → **ZERO latency**. **PO decision:** channel=**email (Resend)**, scope=**operator's-own-reports-now** [beta-wide DEFERRED behind the a24 notice update — Rule #39: «العنوان لا يُخزَّن» must become «نحتفظ بنسخة للسجلّات» + a PDPPL nod before real users get copied]. Verified: isolated `test_sprint_2_22_0b42.py` **36/36** [E14: dormant-gate matrix · pure build_email shape/recipient/from/subject-ref/JSON-attachment/slug-safe-filename · refusal + address fallbacks · `send_report_copy` dormant→no-network / active→exactly-one-POST / failure-swallowed · **value-invariance** (`result` unchanged) · api.py wiring] + DoD aggregator **392/392 MATCH** / security **15/15** / surface **45/45** / broad walk **110/110 ALL GREEN** [109→110, no flake, 187.5s] + `import api` OK [14 routes, `_MAIL_OK=True`, `mail_enabled()=False` → dormant] + **live smoke v213** [browser-UA, #61: `/api/health`=b42; **5-anchor value byte-gate identical to v212** — 54/541/6 **2,400,000** cost_led [2.4M–5.4M] · 56/647/6 **3,800,000** geo_full [3.1M–3.8M] · 55/296/13 **2,600,000** e25_capped · 56/565/21 **2,400,000** matched · 52/903/90 **refusal**; **`NO-capture_id` on all 5** → capture+mail DORMANT, response byte-identical]. heroku auth held (`ans_hashim@hotmail.com` — no hand-off); commit `45a9701` (origin `5652cc2`), CHANGELOG_v125, §20.73. **✅ email-send LIVE test PASSED (b42.1 / v215).** The first real send 403'd with **Cloudflare `error code: 1010`** on `api.resend.com` (the bare `urllib` User-Agent — Resend's API is Cloudflare-fronted too, the same #61/R12 block as thammen.qa; `curl` passed, `urllib` didn't). **Fix b42.1:** a browser `User-Agent` on the Resend POST in `report_mailer._post` (isolated **40/40** [+4 UA checks: the real `_post` sets `Mozilla/5.0` + Authorization + the /emails URL], DoD 392/15/45/broad **110/110**); redeployed v215; the re-run eval logged **no send-failure** → the report copy delivers (subject `[ثمن] TH-20260614-54541006-b052 — 54/541/6 — 2,400,000 ر.ق` + full JSON attached). commit `0efec63`. **Lesson:** #61/R12 (Cloudflare 1010 blocks bare urllib) extends to ANY Cloudflare-fronted API the app POSTs to via urllib — set a browser UA. **Still gated for beta-wide:** the a24 notice update (Rule #39). **⏭️ NEXT = the binding constraint #1 (beta launch + GT collection, D-3 — PO decision); this report-copy is the operator-side MEMORY that complements it** · the lighter §4ب display items [UX4 freshness banner+slider · UX6 improvement-delta] remain the only 🟢 frontend slices; precision (B-2 / §6 v2 / Option B) = a signed Gate-2. **⚡ Prior — b41 / Heroku v212 (Sprint 2.22.0b.41 «الكَيستون: صفوف الجيران الجغرافيّة» / DEF-UX1.1b)** — 🟢 ENGINE-ADDITIVE / DISPLAY-ONLY / VALUE-INVARIANT (`api.py` UNTOUCHED; amount/low/high/method/rule/leadership byte-identical; rows ANONYMOUS — E12): **geo neighbour rows** — on a GEO-LED villa (V001 56/647/6, rule==`geo_full` / method `comparison_widened`) the keystone now surfaces the location-adjusted NEIGHBOUR rows behind the median — the b39 sibling (b39 disclosed only the pool SIZE «إجمالي {pool_n} صفقة»). A new pure `_keystone_neighbours(accepted_areas, cap=8)` flattens `geo_v2_result['accepted_areas']` (retained in-engine; the Step-5 `all_adjusted_prices` local is NOT read) → each row = source-area NAME + ×adjustment + the DERIVED adjusted ppm² (`round(displayed raw × displayed factor)` — **SELF-CONSISTENT** so a reader can verify raw × ×factor = adjusted; b14 display-coherence; never feeds the value → value-invariant), E12-anonymized (area name + ratio only — no PIN/coords/raw-keys). Nested as **`comparables.neighbours`** ONLY when `_kc_basis=='geo_widened'` (matched_bracket b38 has no neighbours; cost-led `considered_comparables` b40 untouched). Frontend (`index.html`, the `how`/«كيف وصلنا» accordion, b34 density-open): a 2-line bidi-safe neighbour sub-table under the b39 pool disclosure — line 1 «📍{area}» (RTL) + «×{factor}» (LTR island); line 2 `date · م² · {raw}→{adjusted} ر.ق/م²` (dir=ltr) showing BOTH the raw sale ppm² and the adjusted (rates, never a «sold-for» price) + «عرض X من N صفقة مجاورة» + the honest «السعر المعروض هو سعر البيع الفعليّ في منطقة الجار؛ ×التعديل يُحوّله إلى مستوى موقعك (لم تُبَع بالرقم المُعدَّل)». The b39 «إجمالي {pool_n} صفقة» line coexists (summary → detail). Gate-2 SIGNED BY DELEGATION (the §20.70/§20.71 «full geo pool» deferral; recon §3 Slice B); Gate-1 explicit «GO». Verified: isolated `test_sprint_2_22_0b41.py` **30/30** (incl. A12 self-consistency on a fractional ppm² — the E2E-caught b14-class bug) + siblings b38 25/25 · b39 19/19 · b40 18/18 green WITHOUT re-point + DoD aggregator **392** / security **15** / surface **45** / broad walk **109/109 ALL GREEN** (108→109) + **local E2E (live GIS)**: V001 → geo_widened 3,800,000, **neighbours PRESENT areas_n=2 total_n=29 shown=8** (بو هامور ×0.9517 · 3871→3684 derive-all self-consistent, E12 clean) · أبو هامور matched 2.4M + comparables, neighbours absent · Marikh cost-led 2.4M + considered, neighbours absent · المعراض 2.6M / شقق refusal → neither · **value byte-identical to v211** + R14 real-Chromium 390×844 (V001, audience=investor): the neighbour sub-table in the OPEN «كيف وصلنا» — header + 5 primary rows + «إجمالي 34 صفقة» + «📍 بو هامور ×0.9517 · ٣٬٨٧١ → ٣٬٦٨٤ ر.ق/م²» (arithmetic closes) + «📍 المعمورة 43 ×1» + «لم تُبَع بالرقم المُعدَّل» + CC BY 4.0; headline **٣٬٨٠٠٬٠٠٠ unchanged**; no overflow (docScrollW 390 == clientW 390, panel right 355 < 390, rows ≤ 338); **0 console errors** (screenshot timed out — the §20.34 capture hiccup; DOM measurements the channel) + live smoke **v212** (browser-UA, Rule #61): `/api/health` = b41; **5-anchor value byte-gate identical to v211** (V001 3.8M geo + `comparables.neighbours.shown=8` areas=2/total=29 · أبو هامور 2.4M matched + comparables, no neighbours · Marikh 2.4M cost-led + considered, no neighbours · المعراض 2.6M · شقق refusal); served HTML carries «الصفقات المجاورة المُعدَّلة الموقع» + `_kc.neighbours` + `price_per_m2_adjusted`. commit `e51a505`, CHANGELOG_v124, §20.72. **The keystone series (b38 matched → b39 geo primary → b40 cost-led considered → b41 geo neighbours) is COMPLETE.** **⏭️ NEXT = the binding constraint #1 (beta launch + GT collection, D-3 — PO decision)** · OR the lighter §4ب display items (UX4 freshness banner+slider · UX6 improvement-delta) · OR a signed Gate-2 (B-2 / §6 v2); **Option B** (the exact cost-led n=51 geo-full pool rows, engine-additive `return_transactions` on `subject_geo_full_ppm2`) deferred · **DEF-UX5** = Gate-2 EN-localization · **DEF-UX15** blocked (QARS data-drain); the 🟢 deploy-on-green frontend backlog otherwise EXHAUSTED. **Deploy note:** Gate-1 needed the explicit «GO» (the safety classifier blocks the production push on generic «الأصوب/اكمل», the b38/b39 lesson); git auth held. **Prior: ⚡ b40 / Heroku v211 (Sprint 2.22.0b.40 «الكَيستون: حوض السوق المُعتبَر على مسار الكلفة» / DEF-UX1.2a)** — 🟢 ENGINE-ADDITIVE / DISPLAY-ONLY / VALUE-INVARIANT (the cost (DRC) already drives the cost-led number → `amount/low/high/method/rule/leadership` byte-identical; rows ANONYMOUS — E12): **cost-led «considered» comparables** — on a cost-led villa (rule==`cost_led`, e.g. Marikh 54/541/6) the market pool was CONSIDERED but did NOT lead (the geo-full pool failed its reliability bar: n=51, dispersion 0.620 > 0.30). The engine attaches a NEW `valuation.considered_comparables` (basis=`cost_considered`) = the subject's PRIMARY-area raw rows it examined (read directly from `geo_v2_result['primary']['transactions']` — in scope at the b4-region attach; Marikh routes Case-4 thin so `primary['comparables']` is absent), via the UNCHANGED `_keystone_comparables` builder (basis passthrough + the geo `price_m2` ppm² fallback + newest-first + E12 anonymization to `{date,area_m2,total_price,price_per_m2}`). DISTINCT key from b38/b39 `comparables` (mutually exclusive) → the frontend renders the honest «🔍 صفقات السوق في منطقتك — اطّلعنا عليها ولم تقُد الرقم» frame, NEVER «هي ما قرّر رقمك»; the «why» scalars (geo_full_n + geo_full_dispersion) ride the block → the why-line «الحوض الجغرافيّ فشل حدّ الموثوقيّة (تشتّت 0.620 > 0.30، n=51) — قاد التقديرَ منهجُ الكلفة (DRC)». Frontend (`index.html`, display-only → the `how`/«كيف وصلنا» accordion, b34 density-open): a new render block with a muted border (vs the bronze keystone) signalling «considered», not «decided». Excludes market-led (keystone) / income / thin-without-cost / land / refusal. Gate-2 SIGNED BY DELEGATION (the §20.70 «إثراء الكَيستون» deferral; the proposed cost-led copy surfaced for review). Verified: isolated `test_sprint_2_22_0b40.py` **18/18** + siblings b38 25/25 · b39 19/19 green WITHOUT re-point + DoD aggregator **392** / security **15** / surface **45** / broad walk **108/108 ALL GREEN** (107→108) + **local E2E (live GIS)**: Marikh 54/541/6 → cost_led 2,400,000 + **considered PRESENT** (n=29 shown=8 pool_n=51 disp 0.62, real امريخ الجنوبي rows, anonymous) · أبو هامور 56/565/21 matched + comparables · V001 56/647/6 geo + comparables · المعراض 55/296/13 e25 / شقق 52/903/90 refusal → neither · **value byte-identical to v210, mutually-exclusive** + R14 real-Chromium 390×844 (Marikh, audience=investor): the considered panel in the OPEN «كيف وصلنا» — header + the why-line (0.620 > 0.30, n=51, DRC) + 8 dir=ltr rows + «عرض 8 من 29» + CC BY 4.0; **«هي ما قرّر رقمك» ABSENT** (no overclaim); headline **٢٬٤٠٠٬٠٠٠ unchanged**; no overflow (panel right 355 < 390, docScrollW 390 == clientW 390); **0 console errors** + live smoke **v211** (browser-UA, Rule #61): `/api/health` = b40; **5-anchor value byte-gate identical to v210** (Marikh 2.4M cost-led **+ considered cost_considered pool_n=51** · أبو هامور 2.4M matched + comparables · V001 3.8M geo + comparables · المعراض 2.6M e25 · شقق refusal); served HTML carries `considered_comparables` ×5 + «اطّلعنا عليها ولم تقُد الرقم». commit `2f957f5`, CHANGELOG_v123, §20.71. **⏭️ NEXT = b41** (geo neighbour rows — the §20.70 «full geo pool» sibling: the `geo_v2_result['accepted_areas']` accepted-neighbour rows with a source-area + ×adjustment column [adjusted ppm² derived display-side = raw × location_adjustment, value-invariant], the heavier 4-column bidi layout + a fresh R14) · then **DEF-UX8** affordability/LTV guards [🟡 NET-NEW, needs an income input] · the lighter §4ب display items [UX4 freshness banner+slider · UX6 improvement-delta]; **DEF-UX5** = Gate-2 EN-localization project · **DEF-UX15** blocked (QARS data-drain); the 🟢 deploy-on-green frontend backlog otherwise EXHAUSTED — remaining items need a signed brief / product decision / the precision track (B-2 / §6 v2 / GT D-3). **Prior: ⚡ b39 / Heroku v210 (Sprint 2.22.0b.39 «الكَيستون الجغرافيّ» / DEF-UX1.1)** — 🟢 ENGINE-ADDITIVE / DISPLAY-ONLY / VALUE-INVARIANT (the median already drives the number → byte-identical; rows ANONYMOUS — E12): **geo-led keystone** — b38 surfaced the matched-bracket comparables; b39 extends the keystone to the **geo-led market** path (`comparison_widened` / `_widened_indicative`, e.g. V001). The geo-led value is a weighted median of **primary (unadjusted) + accepted-neighbour (location-adjusted) transactions** (geo_reference_v2.py:663-696); the panel surfaces ONLY the subject's **PRIMARY-area RAW rows** (`geo_v2['primary']['transactions']` — every shown number a real, unadjusted, same-area transaction) and the frontend **discloses** that location-adjusted neighbour rows were also pooled («وُسِّع الحوض لمناطق مجاورة (مُعدَّلة الموقع) … إجمالي {pool_n} صفقة») → the geo header is «🔑 صفقات في منطقتك ضمن حوض المقارنة الموسَّع جغرافياً», NOT the bracket «هي ما قرّر رقمك» (no overclaim). Engine: `_keystone_comparables(...basis, pool_n)` += a `price_m2` fallback (geo rows key ppm² as `price_m2`) + a newest-first sort (geo pool is row-order); `_select_primary_comparison` Cases 2-3 stash the geo primary rows; the b4-region gate broadened to `leader=='market' AND method in (comparison_bracket, comparison_widened, comparison_widened_indicative)`, `basis` derived from the method. Still excludes cost-led / income-led / thin / preliminary / land / refusal. Verified: isolated `test_sprint_2_22_0b39.py` **19/19** (geo `price_m2` fallback + pool_n + newest-first + E12 anonymity + value-invariance + the b38 matched_bracket default unchanged + the structural geo stash/gate + the index.html per-basis disclosure) + **R6 re-point** (`test_sprint_2_22_0b38.py` — D2 gate now a method-set, D3 comparables now in Cases 1-3, E3 window = 25/25) + DoD aggregator **392** / security **15** / surface **45** / broad walk **107/107 ALL GREEN** (106→107) + **local E2E (live GIS)**: أبو هامور **matched_bracket** n=37 (b38 unchanged) · V001 56/647/6 → **geo_widened, 5 primary rows, pool_n=34** (ppm² via the price_m2 fallback) · Marikh cost-led / apt refusal → absent (**value byte-identical**) + R14 real-Chromium 390×844 (captured V001 geo, audience=investor): the geo keystone in the OPEN «كيف وصلنا» — geo header + the widening disclosure + «إجمالي 34 صفقة» + first row `2025-06-15 · ٦٤٠ م² · ٣٬٣٥٠٬٠٠٠ ر.ق` (newest-first, dir=ltr) + CC BY 4.0; **no bracket-style overclaim**; headline **٣٬٨٠٠٬٠٠٠ unchanged**; no overflow (35→355 within 390); **0 console errors** + live smoke **v210** (browser-UA, Rule #61): `/api/health` = b39; **5-anchor value byte-gate identical to v209** (أبو هامور 2.4M matched_bracket · V001 3.8M **geo_widened pool_n=34** · Marikh 2.4M cost-led absent · المعراض 2.6M e25 absent · شقق refusal). commit `65cfa35`, CHANGELOG_v122, §20.70. **⏭️ NEXT = DEF-UX8** affordability/LTV guards on the b35 calculator [🟡 NET-NEW] · the **cost-led «considered-but-didn't-lead» pool** (the dispersed market pool on a cost-led villa, e.g. Marikh) + the **full geo pool** (the neighbour rows with source-area + adjustment) — both deferred from b39 (#42) · the lighter §4ب display items (UX4 freshness banner+slider · UX6 improvement-delta). **DEF-UX5** = Gate-2 EN-localization project (recon `PHASE0_DEF_UX5_*`) · **DEF-UX15** blocked (QARS data-drain). The 🟢 deploy-on-green frontend backlog is EXHAUSTED — remaining §4ب items need a signed brief or product decision. **Prior: ⚡ b38 / Heroku v209 (Sprint 2.22.0b.38 «الكَيستون: كشف الصفقات المقارِنة للفيلا» / DEF-UX1)** — 🟢 ENGINE-ADDITIVE / DISPLAY-ONLY / VALUE-INVARIANT (the median already drives the number → `amount/low/high/method/rule/leadership` byte-identical; rows ANONYMOUS — E12 — no PIN/address): **keystone comparables** — the §4ب persona LIVE review ranked DEF-UX1 the highest-value item (**7/10 personas** — البنك·المثمّن·السمسار·المستثمر·المالك·الصحفي·المشترية): the user is shown a number but not the actual MoJ transactions that produced it. **The «مبنيّ-مجاناً» claim was FALSIFIED in recon** (`docs/PHASE0_DEF_UX1_keystone_comparables_recon.md`): the live villa path (`evaluate_property.py:1576`) called `build_reference` **without `return_transactions=True`** → the subject-bracket rows were built then DISCARDED; only the LAND `comparable_grid` was broadcast. b38 surfaces them — **modest, value-invariant, privacy-safe.** Engine: `MoJValuation += bracket_transactions` (mirrors the a13/a14 additive channel) + `build_reference(...return_transactions=True)` [ADDITIVE — aggregate outputs byte-identical] + `apply_moj_strategy` captures the subject-bracket rows; a pure `_keystone_comparables(rows,n,window,cap=8)` builder (anonymizes to `{date,area_m2,total_price,price_per_m2}`, newest-first); `_select_primary_comparison` **Case 1** stashes them on `primary['comparables']`; the **b4-region attaches `valuation.comparables` ONLY when `leader=='market' AND method=='comparison_bracket'`** (the displayed median IS the subject-bracket median) → **excludes cost-led** (number is DRC), income-led, **geo-led** (RULE 2 → deferred UX1.1), thin/preliminary, **land** (its own grid; the gate sits in the villa/house-only `if _gate:` block → land never reaches it), refusals. Frontend (`index.html`, display-only → appends to `how`): a keystone panel in the b31 «🔍 كيف وصلنا» accordion (**b34 density-open for investor/valuer**) — «🔑 N صفقة في شريحتك ومنطقتك — هي ما قرّر رقمك» + a `direction:ltr` table of `date · {size} م² · {total} ر.ق` (Rule #25) + «عرض X من N (الأحدث)» + the CC BY 4.0 source line. Placed right after the b20 «حوض المقارنات» dispersion line (pool→rows story). Gate-2 **SIGNED BY DELEGATION** («اكمل وافعل الأصوب»). Verified: isolated `test_sprint_2_22_0b38.py` **25/25** (E14: value-invariance [A2 `build_reference` aggregates byte-identical with/without the flag; C4 `apply_moj_strategy` value identical] + E12 anonymity [A4/A5/B4/F1 row keys == `{date,area_m2,total_price,price_per_m2}`, no PIN/ref/address] + cap-8 + newest-first + the structural gates [Case-1-only stash, `leader=='market' && comparison_bracket` attach] + the index.html render) + **R6 re-point** (`test_sprint_2_22_0b37.py` exact-version pins → format checks = 22/22) + DoD aggregator **392** / security **15** / surface **45** / broad walk **106/106 ALL GREEN** (105→106; siblings b31 36/36·b34 15/15·b35 17/17·b36 22/22 green WITHOUT re-points) + **local E2E (live GIS)**: أبو هامور 56/565/21 → 2,400,000 comparison_bracket·market·matched → **comparables PRESENT n=37 shown=8** (real anonymous rows); Marikh 54/541/6 → 2,400,000 **cost-led** → absent; V001 56/647/6 → 3,800,000 geo_full → absent; apt 52/903/90 → refusal → absent (**value byte-identical to v208 on all 4**) + R14 real-Chromium 390×844 (captured Abu Hamour, audience=investor): keystone in the OPEN «كيف وصلنا» — 8 rows, first = `2025-12-17 · ٤٤٤ م² · ٢٬٣٠٠٬٠٠٠ ر.ق` (correct dir=ltr) + CC BY 4.0 source; headline **٢٬٤٠٠٬٠٠٠ unchanged**; no overflow (docScrollW 390 == clientW 390, block 35→355); **0 console errors** + live smoke **v209** (browser-UA, Rule #61): `/api/health` = b38; **5-anchor value byte-gate identical to v208** (امريخ 2.4M cost-led · أبو هامور 2.4M matched **+ comparables n=37 anonymous + CC BY 4.0** · المعراض 2.6M e25 · V001 3.8M geo_full · شقق refusal); comparables present ONLY on 56/565/21, absent on Marikh/V001/apt. commit `95248fb`, CHANGELOG_v121, §20.69. **⏭️ NEXT = DEF-UX1.1** (geo-led keystone — the `geo_v2` pool rows for widened/geo-full-led villas, e.g. V001) **+ the cost-led «considered-but-didn't-lead» pool** (a more delicate copy slice, e.g. Marikh) — both deferred from b38 (#42); then **DEF-UX8** affordability/LTV guards on the b35 calculator [🟡 NET-NEW] · the lighter §4ب display items [UX4 freshness banner+slider · UX6 improvement-delta]. **DEF-UX5 RE-CLASSIFIED** (recon `docs/PHASE0_DEF_UX5_en_toggle_recon.md`: NOT a 🟢 slice — a **Gate-2 EN-localization project**; backend 32.6% `_en`-covered / frontend consumes 0 `_en` / 740 client-AR lines + Terms ~800w + bidi/LTR flip). **DEF-UX15 still BLOCKED** (QARS data-drain, recon b35). The 🟢 deploy-on-green frontend backlog is otherwise EXHAUSTED — remaining §4ب items need a signed brief or product decision. **Prior: ⚡ b37 / Heroku v208 (Sprint 2.22.0b.37 «كشف آليّة الكلفة (BUA/RCN/الاحتفاظ)» / DEF-UX9)** — 🟢 FRONTEND-ONLY / VALUE-INVARIANT (display-only; reads only already-broadcast `value_stack.cost.*`; engine = 2 version lines; `api.py` UNTOUCHED; value byte-identical across all 5 anchors): **cost-mechanics on the result screen** — the §4ب المهندس/المثمّن personas want the cost-approach (DRC) mechanics on the result screen without opening the full report. The engine has BROADCAST `value_stack.cost.{bua_m2, rcn_qar_per_m2, retention, building_value, land_floor, assumptions_ar}` since b11/b18/b20, but on the **result screen** only the cost *value* line showed — the `(BUA × RCN × retention)` breakdown lived ONLY in the full/short report (`showReport`/`showShortReport`). b37 adds ONE appraiser-detail line inside the «🔍 كيف وصلنا لهذا الرقم؟» accordion (the `how` buffer — b34 density-open for investor/valuer, one click for everyone else; the b31 «طيّ TIER-1» discipline preserved): «🔧 آليّة الكلفة (نهج DRC): مساحة البناء BUA ≈ {bua} م² · كلفة الإحلال {rcn} ر.ق/م² · معامل الاحتفاظ {retention} ← البناء المُهلَك ≈ {building_value} ر.ق + الأرض {land_floor} ر.ق» + the **broadcast** `assumptions_ar` line (no authored copy). Gated on the 3 fields present (`_vc.bua_m2 && _vc.rcn_qar_per_m2 && _vc.retention!=null`; the cost-unavailable else-branch untouched); reads the block once via `const _vc=v.value_stack.cost;` (DRY); the Latin/decimal tokens (bua, retention 0.5) in `dir=ltr` islands (Rule #25); the report/short-report sibling rows UNTOUCHED. `evaluate_unified.py` = 2 version lines; `api.py` UNTOUCHED. Verified: isolated `test_sprint_2_22_0b37.py` **22/22** (E14: the gated block + the three mechanics + depreciated-building + land from broadcast fields + the broadcast assumptions + the dir=ltr islands + placement in `how` not t1 + the cost-value line preserved + the else-branch chains + no value mutation + the report/short-report siblings untouched) + **2 R6/Lesson-2 re-points** (`test_sprint_2_22_0b36.py` exact-version pin → format = 22/22; `test_sprint_2_22_0b31.py` the cost-value `…cost.value){how+=` literal → `🏗️ '+_vc.label_ar` + `const _vc=…` markers, because b37 moved the line into a `{const _vc=…; how+=…}` block — the «cost-value note in `how`, not t1» intent held = 36/36; no other test pinned the stale literal) + DoD aggregator **392** / security **15** / surface **45** / broad walk **105/105 ALL GREEN** (104→105) + R14 real-Chromium 390×844 on 3 LIVE payloads: **VILLA 54/541/6 [cost-led]** → the «🔧 آليّة الكلفة (نهج DRC)» line renders INSIDE the «كيف وصلنا» accordion with BUA 479 · ٢٬٢٠٠ · 0.5 · البناء المُهلَك ٥٢٦٬٨٣٤ · الأرض ١٬٨٥١٬٢٦٠ + the assumptions; value **٢٬٤٠٠٬٠٠٠ unchanged**; dir=ltr islands correct (479, 0.5 not reversed); no overflow (docSW 390) · **V001 56/647/6 [market-led]** → the line present (BUA 602), value **٣٬٨٠٠٬٠٠٠ unchanged** (NOT gated on cost-led) · **APT 52/903/90 [refusal]** → no line/no crash · **0 console errors/warnings** + live smoke v208 (browser-UA, served HTML carries «آليّة الكلفة (نهج DRC)»; **5-anchor value byte-gate identical to v207**: امريخ 2.4M cost-led [2.4M–5.4M] · V001 3.8M market [3.1M–3.8M] · المعراض 2.6M · أبو هامور 2.4M · شقق refusal). commit `190a9e9` split `1404a98`, CHANGELOG_v120, §20.68. **⏭️ NEXT = the §4ب parallel persona track** (DEF-UX15 autocomplete **BLOCKED** on a QARS data-drain [recon b35, `ISSUES_LOG §4ب-2`]: **DEF-UX1** keystone comparables [🔴 Gate-2 + recon — needs a signed brief, the «built-free» claim falsified] · **DEF-UX8** affordability/LTV guards on the b35 UX16 calculator [🟡 NET-NEW, needs an income input] · the lighter §4ب display items [UX4 freshness banner + market-adj slider · UX5 AR|EN toggle (backend `_en` broadcast-ready) · UX6 improvement-delta]; the persona-simplicity §5 sequence + the broadcast-ready §4ب items [UX3✓ + UX9✓] are now complete). **Prior: ⚡ b36 / Heroku v207 (Sprint 2.22.0b.36 «رفض الشقق فوريّ صادق» / DEF-UX3)** — 🟢 FRONTEND-ONLY / VALUE-INVARIANT (engine = 2 version lines; `api.py` UNTOUCHED; value byte-identical across all 5 anchors): **honest apartment/tower refusal** — the §4ب persona LIVE review (`ISSUES_LOG §4ب`, 8/10 personas) flagged the apartment refusal screen as MISLEADING: live (52/903/90 → apartment_building, amount None) it read «⚠️ تقييم مشروط · منهج الدخل · **يتطلب: الإيجار السنوي الإجمالي**» + a big income deep-link button «**→ أدخل: الإيجار السنوي الإجمالي**» (`goForm('rentalIncome')`) — both implying *adding rent yields a real valuation*, but the apartment income product is the DEFERRED «بوابة بيانات الأنواع» (أ) (no MoJ per-unit comparables, hardcoded cap rate). In `show()`, a single predicate `var _ux3NotReady=(d.asset_type==='apartment_building'||d.asset_type==='tower')&&!hasValuation;` (+ `_ux3Noun` = الأبراج/الشقق) reframes the two refusal surfaces: (1) scope badge → 🚧 «غير مدعوم بعد» + «ثمّن يدعم **الفلل والأراضي** فقط حالياً» (the «منهج الدخل»/«يتطلب» lines DROPPED); (2) insufficient-data card → 🚧 + «{الشقق|الأبراج} غير مدعومة بعد — للفلل والأراضي فقط» + the honest «why» («وزارة العدل لا تسجّل وحدات … فردياً … نعمل على دعم هذا النوع لاحقاً»); (3) the income CTA **SUPPRESSED**. Both gate on **`!hasValuation`** → **the income path is UNTOUCHED** (apartment-with-rent renders the income valuation byte-identical, §20.56); scope = apartment+tower ONLY → **compound_large keeps its CTA** «→ أدخل: الإيجار السنوي الإجمالي للمجمع» (the methodologically-correct Income path per E20), palace = Cost. **§20.42 pattern:** the sprint was found PRE-BUILT in the working tree by a parallel session (uncommitted/undeployed — `git log -S "_ux3NotReady"` = ∅; live carried none); CC INDEPENDENTLY reviewed it (sound + «الأصوب») + **RE-MEASURED all DoD/R14 itself** before push (Rule #58). Recon-reshaped the §4ب-2 spec (the «client-side pre-API detect» + «types-tab disable» parts infeasible — E17 1-field, no asset-type tab → the deferred «بوابة الأنواع» ج). Verified: isolated `test_sprint_2_22_0b36.py` **22/22** (E14: the predicate + both surfaces reframed + «يتطلب»/methodology gated off + CTA suppressed + value-invariance + the engine scope contract `apartment_building` STILL `tier='limited'` UNTOUCHED) + sibling `test_sprint_2_22_0b35.py` **17/17** (R6 re-point) + DoD aggregator **392** / security **15** / surface **45** / broad walk **104/104 ALL GREEN** (103→104) + R14 real-Chromium 390×844 on the 3 LIVE payloads: **APT 52/903/90** → 🚧 «الشقق غير مدعومة بعد — للفلل والأراضي فقط» + the why + NO «يتطلب» + NO «منهج الدخل» + **CTA count = 0** · **CMP 51/835/17 [compound_large]** → KEEPS «يتطلب» + the CTA «→ أدخل: الإيجار السنوي الإجمالي للمجمع» (E20 untouched) · **VILLA 54/541/6** → 2,400,000 «٢٬٤٠٠٬٠٠٠» unchanged · **APT-with-value** (amount injected → hasValuation) → the honest stance does NOT fire (income preserved) · **0 console errors/warnings** · **no overflow 390==390** + live smoke v207 (browser-UA, served HTML carries `_ux3NotReady` ×8 + the honest copy; **5-anchor value byte-gate identical to v206**: امريخ 2.4M cost-led [2.4M–5.4M] · V001 3.8M market [3.1M–3.8M] · المعراض 2.6M · أبو هامور 2.4M matched · شقق refusal). commit `2998548` split `85c3f77`, CHANGELOG_v119, §20.67. **⏭️ NEXT = the §4ب parallel persona track** (DEF-UX15 autocomplete **BLOCKED** on a QARS data-drain [recon b35, `ISSUES_LOG §4ب-2`]: UX9 BUA/RCN [broadcast-ready] · UX1 keystone comparables [Gate-2+recon] · UX8 affordability guards on the b35 UX16 calculator; the persona-simplicity §5 sequence UX11✓→UX13✓→UX14✓→UX12✓→UX16✓→**UX3✓** is essentially complete — UX17/UX18 = heavier Gate-2). **Prior: ⚡ b35 / Heroku v206 (Sprint 2.22.0b.35 «حاسبة التمويل للمشتري» / DEF-UX16)** — 🟢 FRONTEND-ONLY / VALUE-INVARIANT (engine = 2 version lines; `api.py` UNTOUCHED; value byte-identical across all roles — مبدأ b24): **buyer financing calculator** — the study §2 «المشترية» persona (أم خالد) gets an illustrative financing calculator directly **under the figure, gated on `d.audience==='buyer'`**. **DRY recon:** the amortization math ALREADY exists (the b25/b28 short report's `_srPayment(P, downPct, years, ratePct)` — the D3 «ONE allowed value-math», live-proven as «القسط ١٠٬٦٧٢»), so UX16 reuses it via a result-screen `bcRecalc()` with **separate `bc*` ids** (no collision with the short-report `sr*` calculator on the other screen). In `show()` TIER-1, after the range/median + condition/teardown/luxury notes, before the «كيف وصلنا» accordion: «🏦 حاسبة التمويل التقريبية: [20]% دفعة أولى · [25] سنة · [4.5]% فائدة → القسط الشهريّ ≈ {payment} — تقديريّ، استشر بنكك» (three live `oninput="bcRecalc()"` inputs; defaults match the signed b28 short-report contract; «استشر بنكك» keeps it illustrative). **Only `audience=buyer` sees it** (owner/seller/investor/valuer render exactly as before); the payment is DERIVED from `v.amount` (display-only) → value byte-identical. Verified: isolated `test_sprint_2_22_0b35.py` **17/17** (E14: `bcRecalc` reuses `_srPayment` [no duplicate math] + reads `bc*` not `sr*`; gated on `audience==buyer`; defaults 20/25/4.5; placed under the figure before the how-accordion `_acc`; «استشر بنكك» disclosure; no value mutation) + **1 sibling R6 re-point** (b34 `ENGINE_VERSION == b34` literal → format check, b34 = 15/15) + DoD aggregator **392** / security **15** / surface **45** / broad walk **103/103 ALL GREEN** (102→103) + R14 real-Chromium 390×844 on the live امريخ fixture (amount 2,400,000): **buyer → calculator present, القسط ١٠٬٦٧٢ ر.ق/شهر** (= the short-report figure, DRY confirmed) · **owner/investor → NO calculator** (buyer-gated) · **value byte-identical across roles** (2.4M/2.4M/5.4M) · **interactivity** proven (20%/25y→10,672 · 50%/25y→6,670 · 50%/15y→9,180 — correct amortization) · **no overflow** (390==390, maxRight 370<390) · 0 console errors/warnings + live smoke v206 (browser-UA, served HTML carries `bcRecalc` ×2; **5-anchor value byte-gate identical to v205**: امريخ 2.4M cost-led [2.4M–5.4M] · V001 3.8M market · المعراض 2.6M · أبو هامور 2.4M matched · شقق refusal). commit `b775381` split `959481c`, CHANGELOG_v118, §20.66. **⏭️ NEXT = DEF-UX3** (apartment refusal — honest «الشقق غير مدعومة بعد — للفلل والأراضي فقط» card, replacing the misleading «يتطلب: الإيجار» CTA; 8/10 personas, 🟢 frontend/copy, Gate-2-light; **DEF-UX15 BLOCKED** on a QARS data-drain [recon b35: khazna geo-restricted locally + 162k-point dump needs a probe deploy — ISSUES_LOG §4ب-2]; parallel §4ب track: UX9 BUA/RCN [broadcast-ready] · UX1 keystone comparables [Gate-2+recon] · UX8 affordability guards on the UX16 calculator). The affordability guards (UX8: LTV caps · payment>30%-income warning) layer on top of this calculator; the §4ب persona features (UX1 keystone comparables [Gate-2+recon] · UX3 apartment refusal · UX9 BUA/RCN) = the parallel track. **Prior: ⚡ b34 / Heroku v205 (Sprint 2.22.0b.34 «الكثافة المقودة بالدور» / DEF-UX12, the study's «المفصل»)** — 🟢 FRONTEND-ONLY / VALUE-INVARIANT (engine = 2 version lines; `api.py` UNTOUCHED; value byte-identical across ALL roles — مبدأ b24): **role-driven density** — the study §1 hinge «من أنت؟» now drives the result screen's DEFAULT fold-state. **Recon falsified the «server field»** (`ISSUES_LOG §4ب-2` called UX12 «🟡 frontend + بثّ audience»): `evaluate_unified.py` ALREADY writes `'audience': audience` top-level on the main path + every fast path (live-confirmed v204: `POST {audience:investor}` → response `audience: investor`), and `_acc(title,inner,open)` (b15) already takes an `open` arg → **UX12 is FRONTEND-ONLY, `api.py` UNTOUCHED.** In `show()`: `const _dense=(a=>a==='investor'||a==='valuer')(d.audience||'owner')` passed as the `open` arg to the b31 «🔍 كيف وصلنا لهذا الرقم؟» evidence accordion → **investor/valuer generate with the evidence OPEN** («الأدلّة أولاً», study §1.3); **owner/buyer/seller keep it FOLDED** (the b31 simple-owner default). **Single-purpose** (Rule #38): only the «كيف وصلنا» accordion is density-driven this sprint; the others (basic-info / report / full-details) stay folded for everyone (one click away); the per-role *delta content* (yield badge / per-component n / financing calc — study §2) = later slices (UX9/UX16/UX17). `_dense` is placed AFTER `syncTowerPair` to preserve the b22 fence-position pin. Verified: isolated `test_sprint_2_22_0b34.py` **15/15** + **5 sibling R6 re-points** (b15/b2p2/b31/b32 pinned the `_acc(...)` call ending `);` → re-pointed to drop the trailing `);` after b34 added the 3rd `open` arg; b33 `ENGINE_VERSION == b33` literal → format check; the b22 fence pin preserved by code placement, NOT a re-point) + siblings green (b34 15/15 · b33 33/33 · b32 29/29 · b31 36/36 · b15 50/50 · b2p2 26/26 · b22 63/63) + DoD aggregator **392** / security **15** / surface **45** / broad walk **102/102 ALL GREEN** (101→102) + R14 real-Chromium 390×844 on the live امريخ cost-led fixture (`.basket/f_marikh.json`): the 5 roles rendered — **owner/buyer/seller → «كيف وصلنا» FOLDED · investor/valuer → OPEN** · **value byte-identical across ALL 5 roles** (amount 2,400,000 / low 2,400,000 / high 5,400,000) · investor (open) **no overflow** (docScrollW 390 == clientW 390, maxRight 370<390) · howBody visible · 0 console errors/warnings + live smoke v205 (browser-UA, served HTML carries `_dense` ×2; `audience` broadcast live; **5-anchor value byte-gate identical to v204**: امريخ 2.4M cost-led [2.4M–5.4M] · V001 3.8M market [3.1M–3.8M] · المعراض 2.6M · أبو هامور 2.4M matched · شقق refusal). commit `703a988` split `c4b3d78`, CHANGELOG_v117, §20.65. **Prior: ⚡ b33 / Heroku v204 (Sprint 2.22.0b.33 «المدخل: تحسين الافتراضيّ» / DEF-UX14)** — 🟢 FRONTEND-ONLY / VALUE-INVARIANT (engine = 2 version lines; `api.py` UNTOUCHED; value byte-identical — مبدأ b24 «الرقم واحد للجميع»): **identity-input default** per the study §4 «إعادة تصميم مدخل البيانات» (option D = «افتراضيّ + سطر مساعدة»). **(a) HELP LINE on the address input** — a new `.br-note` (visual parity with the PIN field's existing hint), placed AFTER the three fields inside `grpAddr`: «هذه الأرقام على لوحة عنوان المبنى أو فاتورة كهرماء (المنطقة، ثم الشارع، ثم رقم المبنى).» The simple owner saw three bare number fields with no source hint (only the PIN field had one); this names the two places a Qatari finds the national address, in the engine's field order. The PIN hint is untouched. **(b) REASONABLE DEFAULT = local identity memory** — pure `_identGet/_identPut/_identDel` (localStorage + **in-memory fallback** `_identMem`, the a24 gate pattern) + `_saveIdentity` (called in `run()` **after `bd` is built**, persists `{tab,zone,street,building,pin}` — **NOT** `audience`: single-purpose «identity», the b24 «مالك» default + the role selector untouched) + `_restoreIdentity` (wired to `DOMContentLoaded`: pre-fills the fields, re-selects the land tab when saved, reveals a «مسح ✕» link; **first visit = empty** `if(!o)return;` → zero first-time clutter for the new owner) + `clearIdentity` (empties + removes the store + hides the link). **Privacy:** local-only (no cookie, no server write, the address never leaves the device) — consistent with the a24 sessionStorage gate + the DPIA «no server-side address». **Value-invariant by construction:** `run()` reads zone/street/building OR pin and builds `bd` EXACTLY as before; the store only PRE-FILLS the fields. Verified: isolated `test_sprint_2_22_0b33.py` **33/33** (reads the REAL index.html — E14: verbatim help line inside grpAddr after the building field + `.br-note` parity + PIN hint untouched · the store persists tab+4 fields NOT audience · in-memory fallback · first-visit early-return · land-tab re-select · «مسح» reveal-on-value · DOMContentLoaded + run() wiring · the value-invariance guard that `bd` is built unchanged and the store never feeds `bd`) + **1 sibling re-point** (`test_sprint_2_22_0b32.py` pinned `ENGINE_VERSION == b32` literally → re-pointed to a format check, the b19 precedent for the project's own «no exact version pins» rule; b32 = **29/29**) + siblings green WITHOUT re-points + DoD aggregator **392** / security **15** / surface **45** / broad walk **101/101 ALL GREEN** (100→101) + R14 real-Chromium 390×844 (node absent → Chromium is the JS gate): first visit → help line verbatim AFTER the building field, within 390 (left 39 / right 351), «مسح» hidden, fields empty · fill 54/541/6 → `_saveIdentity` stores `{tab:address, zone:54, street:541, building:6}` (no audience) · reload → `_restoreIdentity` pre-fills + «مسح ✕» shown · land flow: PIN 74328443 → reload → inputTab=land, grpLand visible / grpAddr hidden, PIN restored · `clearIdentity` empties + removes + hides · **no horizontal overflow (docScrollW 390 == clientW 390)** · **0 console errors/warnings** + live smoke v204 (browser-UA, served HTML carries the help line «لوحة عنوان المبنى» + `_IDENT_KEY` + `_restoreIdentity` + `clrIdent`; **5-anchor value byte-gate identical to v203**: امريخ 2.4M cost-led [2.4M–5.4M] · V001 3.8M market geo_full [3.1M–3.8M] · المعراض 2.6M e25 · أبو هامور 2.4M matched · شقق refusal). commit `2214ad3` split `a8f9270`, CHANGELOG_v116, §20.64. **Prior: ⚡ b32 / Heroku v203 (Sprint 2.22.0b.32 «تبسيط شاشة التأكيد» / DEF-UX13)** — 🟢 FRONTEND-ONLY / VALUE-INVARIANT (engine = 2 version lines; `api.py` UNTOUCHED; value byte-identical — b24): **`showConfirm` (Screen 2) simplified** per the study §3 kill-list 17-20. **(18)** the full evidence panel is DROPPED from the confirm screen — it lives only on the result, inside the b31 «🔍 كيف وصلنا لهذا الرقم؟» accordion (NO pill substitute, study §3 «لا لوحة أدلّة»). **(17)** the E15 setbacks equation moves from an inline parenthetical (م4/b27) to a hover **tooltip** (`<span class="cg-tip" title="…">ⓘ</span>`): the owner keeps the max-buildable NUMBER «≈ {N} م²» + the «عدّله في خطوة التحسين» CTA; the formula (dims + 5/3/3 + 60% cap, LRM per Rule #25) is one hover away; the shared-parcel detail is tooltip-only too. **(19)+(20)** `pbRows(pb, basisOnly)` gains a flag — on the confirm screen it prints ONLY the cadastral id (PIN) then early-returns, so the utility numbers + the survey-vintage age leave the default view (the age message already lives at the refine «عمر البناء» field → a MOVE, not a loss; the other call-sites — results/report — pass NO flag → full panel, **byte-identical**). **KEEP on confirm (study §3 «يبقى»):** basis review (address / asset / district / R1 / area) + PIN + the muted preliminary range + the b20 cost-led dual-evidence cg-mid line (part of the range, not the panel) + the «تابِع بهذه البيانات» button + the «التقرير الكامل الآن» escape. **PIN stays** — NOT in the §3 kill-list (Rule #38, the NAMED items only). Verified: isolated `test_sprint_2_22_0b32.py` **29/29** + **4 sibling re-points** (b2p2/b2p3/b27/b31 — stale structural pins DEF-UX13 invalidates, R6/Lesson-2: «panel rendered»→folded-on-result · «5.4 reuses panel»→DROPPED-from-gate · «confirm carries the equation»→now-in-cg-tip · «still in showConfirm»→standalone-h+=-render-gone) + siblings green WITHOUT re-points (b9/b3/b15/b17/b24/b26/b29) + DoD aggregator **392** / security **15** / surface **45** / broad walk **100/100 ALL GREEN** + R14 real-Chromium 390×844 on the live امريخ cost-led fixture (`.basket/f_marikh.json`: confirm = العنوان · فيلا منفردة · امريخ الجنوبي · R1 · مساحة القسيمة ٦١٣ م² · **الرقم المساحي 54360025** · «مساحة البناء الأرضي (تقدير أقصى) ≈ ٣١١ م² ⓘ — عدّله» [the ⓘ title carries the dims + 5/3/3 + 60% cap]; evidence panel + electricity/water + age-estimate + inline formula ALL GONE; the b20 dual-evidence line stays; 0 console errors; no overflow 350<390 · doc 390==390) + live smoke v203 (browser-UA, **5-anchor value byte-gate identical to v202**: امريخ 2.4M cost-led [2.4M–5.4M] · V001 3.8M geo_full · المعراض 2.6M e25_capped · أبو هامور 2.4M matched · شقق refusal; served HTML carries `class="cg-tip"` + `pbRows(d.property_basis,true)`). commit `e81ed4f` split `f2ae3ed`, CHANGELOG_v115, §20.63. **Prior: ⚡ b31 / Heroku v202 (Sprint 2.22.0b.31 «طيّ TIER-1 للمالك» / DEF-UX11)** — 🟢 FRONTEND-ONLY / VALUE-INVARIANT (engine = 2 version lines; `api.py` UNTOUCHED; value byte-identical — b24): the **«9-note parade»** (value-floor · HBU · old-stock · cost-triangulation · leadership · age-honesty · resurvey · cost-value-line · market-dispersion) + the full evidence panel fold into **ONE collapsed «🔍 كيف وصلنا لهذا الرقم؟» accordion** (the `how` buffer — a buffer-prefix swap `t1+=`→`how+=`, every string verbatim — built FIRST in t2 after the figure + MVU clause); **TIER-1 = the 5-core** (range-as-lead + median marker + «ليس تقييماً معتمداً» + the evidence pill + the accordion button). Scope (Rule #38 — the NAMED 9 only): condition/teardown/luxury (decision-relevant) + age-sensitivity (b18 §A1, pinned) + moj-n + not-certified + methodology + tier-badge + MUC-chip STAY t1. Refusal path byte-identical (fold inside `if(hasValuation)`). Verified: isolated `test_sprint_2_22_0b31.py` **36/36** + **b15 re-pointed 50/50** (lines 72/98 — R6/Lesson-2 stale structural pins DEF-UX11 invalidates) + siblings green WITHOUT re-points (b16/b18/b20/b2.2/b26/b29) + DoD aggregator **392** / security **15** / surface **45** / broad walk **99/99 ALL GREEN** + R14 real-Chromium 390×844 on the live امريخ cost-led fixture (`.basket/f_marikh.json`: the figure = the 5-core, the parade GONE from it; «كيف وصلنا» FIRST + COLLAPSED, body 1081 chars holds the full parade + the «جودة الأدلّة» panel; 0 console errors; no overflow scrollW 390==clientW 390, maxRight 370<390 collapsed AND open) + live smoke v202 (browser-UA, **5-anchor value byte-gate identical to v201**: امريخ 2.4M cost_led [2.4M–5.4M] cost 2,378,094 · V001 3.8M [3.1M–3.8M] geo_full · المعراض 2.6M e25_capped · أبو هامور 2.4M matched · شقق refusal; served HTML carries «كيف وصلنا» + `let how=''`). commit `a5cdce9` split `fb4b4d4`, CHANGELOG_v114, §20.62. **⏭️ NEXT = DEF-UX13** (تبسيط شاشة التأكيد — drop the confirm evidence panel + setbacks-equation→tooltip + move survey-window/utilities; 🟢 frontend/value-invariant; study §5: UX11✓→**UX13**→UX14→**UX12** [the hinge: broadcast `audience` → role-driven fold-state]). Deferred micro: the full study §3 «21→5» (tier-badge→header / MUC-word-conditional / moj-n fold — touches b15 pins 42/99). **Prior: ⚡ b29 / Heroku v200 (2026-06-12 مساءً) («هبوط المختصر», D6 completion) · برنامج «الواجهة والتقريران» BUILD-COMPLETE + LIVE-VERIFIED (م0–م4 + b28, v194→v199; the deferred-smoke basket FIRED 14/14 — §20.61):** **b29 = Sprint 2.22.0b.29** (🟢 FRONTEND-ONLY / VALUE-INVARIANT, commit `b17b48a`, CHANGELOG_v112: the post-refine landing → the SHORT report for valued NON-VALUER villa/house/raw_land [show(data) still pre-renders b15 → «التفاصيل الكاملة» instant; the thmr-btns row gains the signed «التفاصيل الكاملة»→b15 button beside «التقرير الكامل»→openReport, 2×2 on 390; the wrapper relabeled «→ التفاصيل الكاملة»]; valuer [v4 two-path] + refusals + **BUILDINGS** keep the b15 landing — the buildings carve-out = a MEASURED Soft-Gate-3 flag [the b25 buildings short-report copy reads «رسملة معايَرة 6.5%» on the HARDCODED rate + «بيتك» — pre-existing CTA surface, NOT amplified; → «بوابة بيانات الأنواع» (ج)]; isolated 32/32 + siblings WITHOUT re-points + DoD 392/15/45/broad **97/97** + R14 on the LIVE basket captures + live smoke v200 [4-fixture byte gate re-proven LIVE + served-HTML markers]). **The basket (السلة الخمسية) = FIRED 2026-06-12 14/14 PASS** (م1 byte-gate 4/4 + /verify ✓/✗ on a real fp · م0 owner round-trips + the leader-aware confirm label live [امريخ «مرتكز التكلفة»+dual-evidence «مطابق n=3 · جغرافي 51/0.62» / أبو هامور «الوسيط»] · م2 the 4-leader short-report walk [القسط ١٠٬٦٧٢ · جبري ٢٬١٦٠٬٠٠٠ · §١ live 52%/5.4M/17] + the QR URL itself → «✓ تقرير أصليّ» · م3 the full-report v2 walk [ONE MUC after the number · ملحق 1–6 · dual-evidence · no «اضطراب» anchoring] · م4 the live tower flow [pair → 8,529,231 byte + the three tagged groups + «يتطلب» drops]); **the one remaining manual step = Anas's PAPER QR scan** (the encoded URL already proven ✓). The program-era state below:  the signed one-paste program SHIPPED in one session — **صفر-أ** (the plan reconciled to Anas's original verbatim; the b23 D1–D8 record → `docs/DECISIONS_b23_short_report_implementation.md`) → **م0 = b24/v194** (the R13 first-screens text bundle: تقدير↔تقييم · the signed recency line · «مالك» default + «من أنت؟ — الرقم واحد للجميع» · the leader-aware confirm label + the dual evidence line · the scope-window alias dedup + «فيلا منفردة»; §20.58/CHANGELOG_v107) → **م2 = b25/v195-196** (the SHORT report «الزبدة + ملحق المختصين» + the **thm-report design system** [IBM Plex Sans Arabic LOCAL ×4 + qrcode LOCAL — no CDN; the whitelisted `/fonts/{f}` + `/qrcode.local.js` routes were the live-smoke catch] + D6 short-first + the shared `_verifyUrl` + the D3 financing line EDITABLE inline; §20.59/CHANGELOG_v108) → **م3 = b26/v197** (the FULL report v2 on the **D8 surgery** + **D4** [the MUC clause/basis → banner-tied recency, the «اضطراب 28-02-2026» anchoring GONE] + the four PDF-audit fixes [the blind «الوسيط» labels → leader-aware · the MU level reads the BROADCAST · the mixed-pool line → the dual-evidence line · ONE MUC block AFTER the number + ONE methodology annex + the numbered six]; §20.60/CHANGELOG_v109) → **م4 = b27/v198** (the five-screens thmr identity + the refine THREE tagged groups [يحرّك التقدير / يدقّق مرتكز التكلفة / اختياري للإثراء؛ towerRentSection ungrouped — the b22 flow holds؛ the focus helper opens closed `<details>`] + the E15 setbacks equation; CHANGELOG_v110) → **b28** (the م2 print-contract alignment — the PO-delivered **`docs/ثمن_التقرير_المختصر_v2_امريخ.pdf`** now governs the short report's two pages, every figure broadcast-bound; the §٣ advice bars = the SIGNED hard ceilings ×1.10/×1.30 as disclosed conventions; CHANGELOG_v111). **سلة الدخان المؤجل → ✅ FIRED 2026-06-12 (14/14 PASS, §20.61; the anchor `docs/PROGRAM_interface_two_reports_SIGNED.md` stamped) — NO open program gate remains; the paper QR scan = Anas's manual step.** Deferred (Rule #42): the buildings short-report copy+landing («بيتك» + «معايَرة» on the hardcoded 6.5%) = «بوابة بيانات الأنواع» (ج). **#54 CLOSED by the PO word «أقرّ الترقيم» (2026-06-12):** zero OLD citations as the current standard; the 2025-map lines STAY (the CC reading executed — v107/v109 §8 + the program anchor). Two more PO words recorded there: «أبقِ بطل العقد» (المرتكز in the basis line) + «أقرّ القسط الدقيق 10,672». **The b18→b23 detail below = the prior arc (historical).**
> - **Live (historical, pre-program):** Heroku **v187** · engine `thammen-sprint2p22p0b18-age-basis-finish-delta` (**b18 = AGE-BASIS directive + LUXURY-EXIT finish-delta + TD-93317 recalibration** [🔴 Gate-2 VALUE-AFFECTING — SIGNED in-session (§A–§E); Gate-1 deploy-on-green INSIDE the §D HALT bands (all passed): **A1/E26 — every LEADING cost/retention computation uses the SYSTEM (CGIS) age; a user-claimed age NEVER moves a headline** — the b13 trim DEMOTED lead→«حساسية العمر: لو كان العمر الفعلي {N} سنة ≈ {value}» (the `elif _ct_trim` lead branch removed; `_ct_trim` = the sensitivity calculator) + **the a9 widened `building_age` slice excluded on `age_source=='user'`** (the §D E2E CAUGHT the residual user-age move 3.8M→3.7M once the trim demoted; gis_imagery/system keep the elasticity); b11 system floor + E24 cliff-flag + b4's explicit levers UNTOUCHED · **A2/§C(ii) — LUXURY-EXIT closed:** `is_luxury` no longer abstains the OSR (the verified jump Marikh+lux 3.4M→5.4M raw-median revert) — **FINISH-DELTA = (RCN_finish−RCN_ord) × retention(RAW sys age) × BUA** on the plain base, hard rail plain ≤ lux ≤ thin median; bake-off REJECTED (i) pure-lux-DRC (2.96M < plain = monotonicity violation) → (ii) shipped; Case-A + 10-Year CTAs re-worded to the delta promise · **§B — TD-93317 = the calibration GT:** the certified sheet reproduced **+0.35%** (RAW system age 18 + finish=high, 3,000×0.64; land = the engine MoJ floor, +0.016% vs the bank's 350/ft²) → **V001 re-tiers LUXURY→HIGH** + **DOCS ERRATA** (the «V001 actual ~25» attribution was WRONG — the valuer USED system age 18; deed 2002 = أرض فضاء; the b13 «exact match» = compensating parameters 3,500×0.50 ≈ 3,000×0.64; errata in PHASE0_age_gap_recon + E24 + §20.52 = the record) + **Rule E26 recorded** (VALUER-VALIDATED); isolated **26/26** + siblings b13 37/37·b16 38/38·b14 34/34 (pin→signed reword)·a9 28/28·b11 52/52 + DoD **392/15/45/broad 87** + **local E2E = the §D table 15/15** [Marikh plain 3.4M byte-identical · **Marikh+lux 3,800,000** (delta 410,982, OSR-led, monotonic) ∈ [3.4M,4.2M] · V001 bare 3.8M · **V001+25 headline UNCHANGED + sensitivity 3,600,000** · 3 controls byte-identical] + R14 390×844 [sensitivity TIER-1 + report, 0 errors, no overflow] + live smoke v187 = the same table live; commit `0e43e86`; §20.52 / CHANGELOG_v101] · **b17 = screen-5 FULL REPORT + DEF-12 two-values — the v4 five-screen owner journey COMPLETE** [🟢 FRONTEND-ONLY VALUE-INVARIANT — Gate-2 signed by delegation (the screens-4/5 brief §3, renumbered b16→b17), Gate-1 deploy-on-green: NEW `reportScreen` + `openReport()`/`showReport(d)` from the SAME response (b2.3 pattern); §3 structure cover → FULL MVU/RICS (all OPEN, no accordions in a report) → headline range + the figure's honesty notes (condition·b16-OSR·b11/b13-cost·B-1·b12·cite-n) → **DEF-12: MV + Forced-Sale indication = central×0.90, verbatim label «قيمة بيع جبري إرشادية (عُرف سوقي ×0.90) — ليست تقييم تصفية معتمداً», REPORT-ONLY display math** (Marikh 3,400,000→3,060,000) → evidence → decomposition+10-Year+strata via builders **EXTRACTED VERBATIM from show()** (`_mucFields`/`_mucCardHtml`/`_decompHtml`/`_substHtml`/`_strataHtml` — screens 4+5 = ONE b14-coherent voice; the report MV card = plain `.rc`, the a8 calc-block exactly-once contract held) → basis(b9)+footprint(b10) → the a4 line + **the a25 attribution CLONED at runtime from `.src-credit`** → audience brief sections → footer (not-certified + a20 status + version + timestamp + **the GT hook: «هل لديك سعر بيع فعليّ… شاركه لتحسين الدقّة — واتساب +974 70177761»** = the Terms' signed channel, the D-3 instrument); **A4 print** `@page 12mm` + `printing-report` class (report alone; the screen-4 print path unchanged); TIER-3 CTA rewired `openReport()`; refusal report = the engine's reason, no DEF-12; `api.py` UNTOUCHED; isolated **33/33** + siblings b15 49/49·b16 38/38·b3 14/14·b2.2 26/26·calc-visual 62/62 [the walk CAUGHT the report card carrying `calc-block` → contract breach → plain `.rc`] + DoD **392/15/45/broad 86** + E2E table EXACT + R14 [0 errors, DEF-12 3.4M/3.06M in the DOM, no accordions in the report, no overflow] + live smoke v186 [health b17; Marikh 3.4M+OSR unchanged; anchors byte-identical; served HTML carries reportScreen+DEF-12]; commit `4dfc345`; §20.51 / CHANGELOG_v100] · **b16 = B-2 EARLY slice — V001-anchored OLD-STOCK central re-anchor, n=1 DISCLOSED** [🔴 Gate-2 VALUE-AFFECTING — SIGNED BY DELEGATION («دعنا نبني على تقييم المعمورة…»); Gate-1 deploy-on-green AFTER the Phase-0 BAKE-OFF HALT band PASSED (`docs/PHASE0_b16_bakeoff.md`): the Marikh-class central re-anchors to **M4 = min(max(system-age DRC cost, the §20.10.1 FULL-window geo-bracket ppm² estimator [Marikh 5,567/m² ≈ **517/ft² exact, n=51**] or M2 matching-stratum at n≥10), thin median)**; fires iff villa/house · thin/widened · NOT dispersion-gated · OLD (≥10y/vintage_capped E24) · dominant stratum ∈ {luxury_new, modern_stock} ≥40% · no user premium input · over-anchored · **margin > T=20%** (anchored on the Empirical §3 clean asking-premium ceiling); range [max(land,cost) … thin median muted], range_is_headline, **MUC high + the verbatim n=1 disclosure label**; M2 supersession ladder documented (GT intake → n≥10 M2 precedence → n≥20 label upgrade); **ISS-A07: decomposition + value_floor RECOMPUTED on the new central + the b14 narrative re-run** (1,851,260+1,548,740=3,400,000 exact); precedence income_led > b13-trim > THIS > b11(`and not _osr`) > widen_down — the §3 premise resolution: b11 leaves the central UN-led, b16 upgrades it; `index.html` +2 TIER-1 lines render `old_stock_reanchor.note_ar` + `cost_triangulation.note_ar` (b11/b13 notes were JSON-only); `api.py` UNTOUCHED; **live smoke v185 = the expected-moves table EXACT: Marikh 54/541/6 5.4M → 3,400,000 [2.4M…5.4M] `old_stock_reanchor_indicative` basis_n=51 margin 59.2%; V001 ABSTAINS (margin 15.2% < 20%, converged) → 3.8M; Abu Hamour 2.4M / Maraad 2.6M / Apt refusal byte-identical; 0/8 spurious on the discovered old-villa cohort**; isolated **38/38** + b11 **52/52** + b13 **37/37** + b15 **49/49** + DoD **392/15/45/broad 85** + R14 390×844 [OSR note TIER-1, 0 errors, no overflow]; **🔴 HONEST RESIDUAL: calibration = ONE certified appraisal (V001) + the FULL-window MoJ pool — the label says so verbatim; the GT kit (D-3, `docs/GT_INTAKE_KIT_v1.md`) is the tightening channel (≥8 luxury_new + ≥6 old-plain + ≥6 valuer); the new-stock UNDER-anchor stays B-2-proper (E25)**; commit `665be93`; §20.50 / CHANGELOG_v99] · **b15 = screen-4 polished result — 3-tier progressive disclosure (DESIGN v4 screens-4/5 brief, slice 1)** [🟢 FRONTEND-ONLY VALUE-INVARIANT — Gate-2 SIGNED BY DELEGATION («افعل الأصوب»), Gate-1 deploy-on-green: `show()` restructured into tier buffers — **TIER-1** always-visible figure (range-as-lead b3 + tier badge + NEW MUC-level chip «تحفظ مادي: …» + the headline honesty notes (a17/a19·b4·B-1·b12) + moj n + NEW «📌 تقدير سوقيّ آليّ — ليس تقييماً معتمداً» line [a20 status appended] + NEW evidence one-row [`_evOneRow` reusing `_evidenceRatings` verbatim] + the a4 bare line) · the **FULL MVU clause card moved from FIRST to directly under TIER-1, always-visible/never collapsed** · **TIER-2** collapsed native `<details class="t2acc">` accordions (basic-info+b9 / full evidence panel / ALL brief sections incl. cap-rate / «التفاصيل الكاملة»: decomposition·10-Year·geometry·range-exp·trend·geo-findings·location·strata·known-unknowns·a8 — **no panel lost**) · **TIER-3** CTAs («✏️ حسّن التقدير» → refine · «📄 التقرير الكامل» → `printReport()`, **b17 rewires to screen 5**) · alerts (A11/reality/multi-QARS/scope/sanity) ABOVE the number · **refusal path byte-equivalent flat** (tiering gated on `hasValuation`) · **print parity**: `printReport()` force-opens accordions + restores (closed `<details>` doesn't print); `api.py` UNTOUCHED, engine diff = 2 version-string lines; **5 anchors numeric-identical** (2.4M/5.4M/2.6M/None/3.8M); isolated **49/49** + DoD **392/15/45/broad 84** (the first walk caught a b3 provenance-comment pin → restored, clean re-run) + R14 Chromium 390×844 [0 console errors, tier order in DOM, accordions+print-open proven, no overflow 370<390 / 1265<1280] + live smoke v184 [5 anchors byte-identical; served HTML carries `t2acc` + «ليس تقييماً معتمداً»]; Phase-0 `docs/PHASE0_b15.md` (27-panel inventory, **NO HALT** — no tier demotion hides a compliance surface); commit `1676ddb`; §20.49 / CHANGELOG_v98] · **b14 = decomposition coherence + report-voice (ISS-A07)** [🟢 VALUE-INVARIANT TEXT-ONLY — Gate-2 SIGNED BY DELEGATION (D-6, «افعل الأصوب»), Gate-1 deploy-on-green: a new pure `_reconcile_decomposition_narrative` post-pass [called in `evaluate_thammen` **after** the value_decomposition attach — NOT in `_build_unified_output` which runs before it, the call-site bug the local E2E caught] rewrites ONLY the implied-building narrative TEXT so the value-decomposition AGREES with the 10-Year/stratum panel — **Case A** (old/vintage_capped villa in a premium-dominated pool, no user luxury/new/renovated → «لا قيمةَ بناءٍ فعليّة لعقارٍ بهذا العمر؛ يتّسق هذا مع قاعدة الـ10 سنوات» + reverse cross-line) · Case B (genuinely new/luxury → keep) · Case C («حدّ أعلى استدلاليّ»); + 3 copy leaks (villa service-charge MUC softened [score/level invariant] · cap-rate body discloses the b7 bracket-borrow · `index.html` ad-empty-state → no-listing copy); **Phase-0 premise CORRECTION** (the brief's 5.3M/5.4M basis divergence does NOT exist live — `1,851,260+3,548,740=5,400,000`=headline, the `3,448,740` was a 100k typo, % stays 65.7 byte-identical); `api.py` UNTOUCHED; **4 anchors + V001 byte-identical** (2.4M/5.4M/2.6M/None/3.8M), **Marikh + V001 = Case A verbatim live**; isolated **34/34** + DoD **392/15/45/broad 83** + R14 390×844 [0 errors, no overflow] + local E2E & live smoke v183 [leaks #1/#2 live]; **🔴 HONEST RESIDUAL: DISCLOSES the R7 over-anchor — does NOT change the central** (B-2 `luxury_new` stratum, PARKED n≥20, E25, is the durable under-anchor fix); §20.48 / CHANGELOG_v97] · **b13 = §20.9 GATED slice — Lever 1 convergent-TRIM** [🔴 Gate-2 VALUE-AFFECTING — SIGNED BY DELEGATION + **RESHAPED at the Phase-0 gate**: the recon overturned Lever 2 (UP-lift — DRC cost ~2.6M < the V002/V003 4.0M sale → **B-2 GT-corpus calibration, NOT a cost lift**), PO confirmed **Lever-1-only**; an OLD over-anchored thin/widened villa WITH a **USER actual age** (`age_source=='user'`, eff=max(user,system)) + actual-age DRC cost below market **≤30%** → the cost **LEADS**, market muted in range, DISJOINT from b11 reanchor at 30% + **D-1 finish-floor 0.31** [lux; default 0.27 byte-identical] + **ladder** excellent −2/renovated −3 + **cliff-flag R3** [value-invariant `age_basis=vintage_capped` nudge, 62% of villas, E24]; `api.py` UNTOUCHED; **4 anchors+V001 bare byte-identical**, **V001+age25+lux+exc → 3.6M** (valuer), range [3.6M,3.7M]; isolated **37/37** + b11 52/52 + DoD **392/15/45/broad 82** + R14 390×844 [0 errors, no overflow] + live smoke v182; **🔴 HONEST RESIDUAL: DORMANT on live no-age traffic** (62% vintage-capped → fires only on a user actual age via Refine; the cliff-flag nudge is the activation surface, GT-collection the flow source); §20.47 / CHANGELOG_v96] · **b12 = Bug A15 HBU not-evaluated explicit disclosure** [DISCLOSURE-ONLY / value-invariant — `hbu_note_ar/en` surfaces when the zoning layer is unavailable (HBU silently skipped → «not evaluated» ≠ «no upside»); villa/house, co-located with the B-1 value_floor, muted `.rn`; **4 anchors byte-identical**; isolated 26/26 + DoD 392/15/45/broad 81 + R14 390×844 [0 errors, no overflow] + live smoke v181 [note absent — zoning present]; **A15 → CLOSED**; §20.46 / CHANGELOG_v95] · **b11 = §20.9 Cost-Approach DRC down-re-anchor** [🔴 Gate-2 VALUE-AFFECTING, **SIGNED** «وقّع وانشر الآن»: an independent RICS DRC (`land_floor` + depreciated building, from the **b9 SYSTEM age** + the b10 footprint × built-ratio 0.77) re-anchors a thin/widened **OLD over-anchored** villa DOWN with the **COST as the informed floor** (replaces §6 widen_down's bare land); **SHIP-NOW = the down-re-anchor ONLY** (the §11 Gate-2 SPLIT), precedence income_led > cost_reanchor > widen_down; **b9 system age = a FLOOR → conservative/IMMUNE** (V001 at 17 → +22% no-fire; at actual 25 → +30.6% would-fire → ship-now MUST use the system age); age-gate ≥10, >30% undercut threshold, MUC high, NO invented central (brief §7#2); backend-only (`api.py`/`index.html` UNTOUCHED); **live two-lane smoke v180:** Marikh 54/541/6 floor **1.9M→2.4M** `cost_reanchor_down` (cost 2,378,094, undercut 128%, bua 479), V001 3.8M [2.5M…3.8M] / Abu Hamour 2.4M / apartment refusal **byte-identical**; isolated **52/52** + DoD **392/15/45/broad 80** (the broad walk caught + fixed a real a2.p9 precision regression — «الصفقات المشابهة»→«القريبة في النوع والمساحة»); **HONEST RESIDUAL:** raises the FLOOR to the cost, does NOT drop the central (= the GATED slice); §20.45 / CHANGELOG_v94] · b10.2 = **multi-QARS-aware geometry footprint** [DISPLAY-only / value-invariant — the building footprint for a villa on a **SHARED 2+-villa parcel** is now computed on the per-villa share (`effective_per_villa`), NOT the whole cadastral parcel; **56/565/21 (900m² shared by 2 villas) → footprint 528→270**, with «حصة الوحدة في قطعة مشتركة بين N وحدات» disclosure on the confirm row + results card + refine hint; single-plot villas byte-unchanged; the value side already brackets on the effective share so amount stays **byte-identical (2.4M)**; **Anas-caught**; isolated **31/31** + DoD **392/15/45/broad 79** + R14 [0 console errors, no overflow 390×844] + live smoke v179 [56/565/21 footprint 270 n=2, amount 2.4M byte-identical; 54/541/6 311 single]; commit `e26680f` split `90a4efb`, CHANGELOG_v93] · b10.1 = **geometry building-area on the confirm/basis review** [DISPLAY-only / value-invariant — surfaces the auto max-buildable building footprint on **Screen 2 (the confirm basis review)** alongside plot area + PIN + electricity + building-age (b10 had it on the results card + refine hint only); honest «تقدير أقصى — عدّله» framing; **closes a gap Anas caught + verified empirically** (the building area was auto-computed + in the response + on the full report, but absent from the first screen the user sees); aggregator **392** + R14 [confirm row renders, 0 console errors, no overflow 390×844] + live smoke v178 [54/541/6 5.4M byte-identical, served HTML carries the row]; commit `f554900` split `09faac4`, CHANGELOG_v92] · b10 = **geometric footprint** [DISPLAY/CONFIRM-only / value-invariant: max-buildable ground footprint from the plot's actual dims − legal R1 setbacks (front 5/side 3/rear 3, E15) bounded by the 60% coverage cap; **edge-pairing on the 4-vertex ring** (rotation-safe — the bbox is WRONG, Qatar rectangles are rotated vs the 2932 grid; recon `shoelace==pdarea` on all 5 plots) + `is_rectangular` gate, non-rect → coverage-cap fallback; **zero new GIS** (reuses the already-fetched plot polygon); `valuation.geometry` += `plot_dims_m` + `max_buildable_footprint_m2` + `footprint_method` + the «الحدّ الأقصى المسموح — عدّله لواقع مبناك» note; index.html shows the dims + max-buildable on the results card + an editable footprint hint on `refineScreen`; **🔴 VALUE-INVARIANT (recon D1)** — `_suggested_fp`/`_eff_fp`/substantiality/`valuation.amount` UNTOUCHED, the footprint→BUA→headline auto-wiring is the **§20.9 cost-triangulation Gate-2** NOT here; isolated **24/24** + DoD **392/15/45/broad 79** + R14 Chromium [0 console errors, geometry card + hint render, no overflow 390×844] + live smoke v177 [3 villas byte-identical 5.4M/2.4M/3.8M; 54/541/6 → setback_envelope [35.0,17.5]=311, 56/647/6 V001 (5-vertex) → coverage_cap 391]; framing F-1..F-4 Anas-signed; recon `docs/PHASE0_2p22p0b10_geometric_footprint.md`; §20.44 / CHANGELOG_v91] · b9 = QARS **property-basis panel** [DISPLAY-ONLY / value-invariant: surfaces **PIN + رقم الكهرباء + water + building-age FLOOR** from QARS `SURVEYED_DATE` on every eval (the b2.3 confirm card + the results report), all auto-fetched from the QARS call `find_property` already makes [zero new GIS]; the age **NEVER feeds `building_age_years` → no Gate-2**; **56/647/6 reproduces the Al Manara bank report (TD 93317) LIVE** — pin 56101583, electricity 140502, age ≥17y; self-correction [Rule #36] — electricity IS auto-fetchable [`ELECTRICITY_NO`], a prior message was wrong; isolated **29/29** + DoD **392/15/45/broad 78** + R14 Chromium [0 errors, no overflow 390×844] + live smoke v176 [4 anchors byte-identical, property_basis on main+fast paths]; +Empirical **E15 corrected** [R1 front 5m not 3m + June-2026 amendments]; §20.43 / CHANGELOG_v90] · b8 = §6 v2 **income OPEX alignment** [🔴 Gate-2, delegated via PO «افعل الأصوب، بعد استبعاد البيتا»]: NOI opex now matches the calibration opex when the cap rate is calibrated (villa 0.20, was the flat 0.23) → closes the **-3.75%** villa-calibrated income understatement (income_led headline + the displayed cross-check); compound/fallback keep 0.23 **byte-identical**; **value-invariant on ALL live no-rent traffic** (4 anchors byte-identical); **the work was found PRE-BUILT in the tree by a parallel Claude Code session — CC independently re-verified it matches the recon analysis + re-measured all DoD/E2E before push**; `api.py`+`index.html` UNTOUCHED; isolated **19/19** + DoD **392/15/45/broad 77**; **live smoke v175:** 54/541/6 +rent 15k → **income_led 2.8M** [was 2.7M in b7], 56/565/21 2.4M / 54/541/6 no-rent 5.4M / 55/296/13 2.6M / 52/903/90 refusal byte-identical; **🔴 HONEST RESIDUAL: live payoff still BETA-GATED** [income_led needs a subject rent → live no-rent Marikh/villa-6 stay widen_down]; §20.42 / CHANGELOG_v89] · b7 = §6 v2 **cross-bracket yield-borrowing** [🔴 Gate-2, delegated via PO «افعل الأصوب» §20.18]: a read-only recon proved the §20.40-deferred "calibrate 600-900 yield cells" **data-infeasible** [**0/187** usable villa cells at 600-900; المعمورة sale n=7 frozen, امريخ rent n=0] → the data-feasible lever = borrowing: `_lookup_calibrated_cap_rate` now **borrows the area's usable 400-600 cell** when the subject's exact plot bracket has none [disclosure + MUC-high + the existing [land_floor,cost] clamp as rails], `_income_triangulation` forces MUC high on a borrowed yield; **decisive finding** — the lookup queried STRICTLY at the subject bracket, so a 600-900 subject **even WITH a rent** got the 4% fallback → income_led couldn't fire; **value-invariant on ALL live traffic** [borrowing fires only with a subject rent at a no-cell bracket — the exact-bracket 400-600 path is byte-identical]; `api.py`+`index.html` UNTOUCHED; isolated **22/22** + DoD **392/15/45/broad 76**; **live two-lane smoke v174:** 54/541/6 DEFAULT (600-900) + rent 15k → **income_led 2.7M via borrowed=True from=400-600, MUC high** [THE KEYSTONE — was widen_down in b6], 56/565/21 2.4M / 54/541/6 no-rent 5.4M widen / 55/296/13 2.6M / 52/903/90 refusal all byte-identical; **🔴 HONEST RESIDUAL: live payoff BETA-GATED** [income_led needs a subject rent → live no-rent Marikh/villa-6 stay widen_down; "ready-when-rents-flow"]; **DEFERRED §6 v2 remainder: opex 0.20 align [~3.75% pre-existing] + Fork C [robustness] + (ii) age-rent**; §20.41 / CHANGELOG_v88] · b6 = §6 R7 income-triangulation [**🔴 Gate-2 — the villa headline MOVES**, first non-opt-in value move since b4]: new PURE `_income_triangulation` — **income_led** [a GROUNDED subject rent (`actual_provided`) + a calibrated reliable/indicative cap-rate cell + within `[land_floor, cost×1.05]` → income LEADS the villa headline, comparison DEMOTED to a disclosed sibling, MUC by spread; **circularity guard** — only a subject-specific rent leads, the area-median rent ÷ area-yield reconstructs the comparison] + **widen_down** [a no-rent condition-blind THIN/widened/preliminary villa with `land_floor < comparison` (OVER-anchored) → range widens DOWN to the land floor + range_is_headline + condition-widen note + MUC high; **EXCLUDES** clean reliable bracket / dispersion-gated pools (a10/a14) / land-anchored villas; **no invented midpoint** — RICS cites the data median muted within a wide range]; villa/house only; b4-region wiring mutually-exclusive with teardown/luxury; `api.py`+`index.html` UNTOUCHED; **live two-lane smoke v173:** 54/541/6 → **widen_down 1.9M–5.5M↓** + range_is_headline + condition_widen_note + MUC high [the live un-anchoring of Marikh's 5.4M guess], 54/541/6@400-600+rent → **income_led 2.7M** [comparison 2.97M demoted], 56/565/21 → **2.4M byte-identical** [clean bracket untouched], 55/296/13 → unchanged [**land-anchored, correctly NOT widened**], 52/903/90 → refusal; isolated **23/23** + DoD **392/15/45/broad 75** [zero regression]; **🔴 HONEST RESIDUAL: income_led is BRACKET-GATED [400-600 cells only → Marikh/villa-6 live (600-900) get widen_down ONLY, NOT grounded to ~3.2M until 600-900 yield cells calibrated]; widen range WIDE [land→comp, tunable]; DEFERRED v2: Fork C + opex 0.20 + (ii) age-rent + 600-900 cells**; §20.40 / CHANGELOG_v87]; b5 = R7 villa-yield calibration DATA ship [swapped `cap_rates.sqlite` → per-area rebuilt DB: villa reliable **1→6** / indicative 2→10, incl. **امريخ الجنوبي 400-600 5.16% net n=46** (Marikh over-anchor area) + المعمورة 56 4.83%; the villa income cross-check now uses calibrated per-area net yields (vs the flat 4% fallback) when income fires + a usable (area,bracket) cell matches — **BRACKET-GATED** (most cells 400-600 → standard anchors in 600-900 stay 4% fallback, CORRECT); **HEADLINE VALUE-INVARIANT** (income downstream of `primary['value']`, reconciliation status-only; live smoke 4 anchors byte-identical 2.4M/5.4M/2.6M/refusal); **B confirmed LIVE** on Marikh forced to 400-600 → «معدل رسملة معايَر 5.2% n=46 reliable» source=calibrated; the §9/§10 "ship yield-data" **STANDALONE** branch (per-area gave §9's «and/or-broader» coverage); +Soft-Gate-3 stale 2.19.1 mock repair (R7 interface drift, latent red at `ba47835`); DB swap + engine version-string only, `api.py` UNTOUCHED; §20.39]; b4 = R7 condition/value axis [`condition=teardown` ↓ land−demolition · `new`+`is_luxury` DRC/Cost-Approach ↑ · `penthouse` ×2.5 BUA; **EXTREMES-ONLY — live-verified v171:** the standard 4 anchors are byte-identical (levers opt-in), `teardown`→2.4M & `new`+luxury→5.9M/5.2M fire correctly, BUT the **good/very-good/renovated MIDDLE still over-anchors at the widened value** (e.g. 56/647/6 `good`/`renovated` even +age=25 → 3.7–3.8M) — the 10-Year-Rule DOWN re-anchor is wired ONLY to explicit `teardown`, NOT to old-age+good-condition; that residual = the next R7 step; §20.36]; b3 = range-as-lead [§2b authority/finality dial-down: the results headline becomes the market RANGE («النطاق التقديري السوقي» = true low–high, asymmetry-ALLOWED — **NOT** a forced symmetric ±) + a muted central-estimate median marker «الوسيط (التقدير المركزي)», point fallback when no range; old two-box removed; value_floor stays SECONDARY (NOT land-to-median); **value-invariant**, engine version-string only]; b2.3 = confirmation gate [Screen 2, v4 owner-journey: a NEW `confirmScreen` between identification and the result, rendered from the SAME `/api/evaluate` response — muted preliminary range + READ-ONLY review of the GIS-fetched basis [no pencils, plot-area honesty label] + the b.2.2 evidence panel reused + «تابِع بهذه البيانات» → refine + permanent «التقرير الكامل الآن» → results; `run()` routes valued non-valuer → confirm, **valuer + refusals skip to results** [v4 two-path]; **value-invariant**, engine version-string only]; b2.2 = evidence-quality diagnosis panel [frontend: the binary confidence badge «🟢 شواهد كافية» → a 4-component **evidence-quality** panel (اكتمال بيانات العقار · جودة المقارنات · حداثة بيانات السوق · جودة توصيف المبنى — قوي/متوسط/محدود), each **DERIVED from its engine field** (§2c); **«explanation≠confidence» enforced** — refine improves ONLY the user-input axes (proven live: اكتمال محدود→قوي + توصيف محدود→متوسط while مقارنات/حداثة held); component 4 «غير منطبق — أرض» for raw_land; **value-invariant**, engine version-string only; DESIGN_2p2x §3]; b2.1 = separate input screens [frontend WRAP: `formScreen`=identification → bare `/api/evaluate`, new `refineScreen` hosts the relocated optional details, results staging card → display-only → `go('refine')`, tower CTA → refine (Rule #39); **value-invariant**, engine version-string only]; b2 = guided staged-input WRAP [Stage-2 geometry confirm + `effective_footprint_m2`, value-invariant]; b1 = zoning-driven ground-footprint suggestion + **basement excluded from the comparison driver** [geometry §5.2/§5.5, **value-invariant on no-building-input anchors**]; a25 = CC BY 4.0 MoJ source-attribution footer credit; a24 = beta consent **entry gate** + Terms/Privacy + DPIA; a23 = R15 strata-land a18) · capture **DORMANT** · `master == origin` (**b11 SHIPPED — Heroku v180**, commit `6e93d16` split `f7c3990` [Gate-2 SIGNED + Gate-1 «go» 2026-06-09; the deploy needed Anas `heroku login` — CC-side heroku auth was expired this session, Operational lesson in §20.45]; b10.2 = v179/`e26680f`, b10.1 = v178/`f554900`, b10 = v177/`c1c92fe`; read live via #57). **§20.9 GATED slice Lever 1 (convergent-TRIM) ✅ SHIPPED b13/v182** [actual-age handling via `age_source=='user'` + eff=max(user,system); D-1 0.31 finish-floor + ladder (excellent −2/renovated −3) + cliff-flag R3 shipped; **Lever 2 UP-lift DROPPED → B-2** (recon: DRC cost ~2.6M < the V002/V003 4.0M sale → GT-corpus calibration, not cost); HONEST RESIDUAL: the trim is DORMANT on live no-age traffic — fires only on a user actual age via Refine]. **The PO EXECUTION ORDER (2026-06-10, signed routing) = ✅ COMPLETE:** (1) **b15** screen-4 polished result [v184] → (2) **b16 = the B-2 EARLY slice** [v185 — HALT band PASSED, Marikh 3.4M ∈ [2.8M,3.6M] live] → (3) **b17 = screen-5 full report + DEF-12** [v186] → **+ b18 = the signed AGE-BASIS/LUXURY-EXIT directive [v187 — E26: system age leads, user age = sensitivity; finish-delta through the replacement coefficient]**. **The v4 five-screen owner journey is COMPLETE** (identify → confirm → improve → result → report). **NEXT = the GT-collection track (D-3)** [the kit `docs/GT_INTAKE_KIT_v1.md` is now INSTRUMENTED end-to-end: the report footer carries the GT hook → واتساب; targets ≥8 luxury_new + ≥6 old-plain + ≥6 valuer reports — unlocks B-2-proper (n≥20) + upgrades the b16 indicative label; ISS-G03: beta = parallel non-blocking track, no cohort gate; a24 consent gate + «ليس تقييماً معتمداً» label + free-only = R13 cover, PRESERVED] **· OR §6 v2 remainder** [Fork C + (ii) age-rent] **· OR B-2-proper** [the under-anchor half, PARKED n≥20]. `heroku auth:whoami` before every subtree push. Deferred backlog: the b11 low>high range inversion micro-fix (54/788/10-class, §20.50) · the income_led/b13-trim decomposition-recompute sibling gap (input-gated, §20.50) · the b16 n≥20 label-upgrade wording (future Gate-2 copy).
> - **✅ SHIPPED 2026-06-11: Sprint 2.22.0b.20 «EVIDENCE-CONDITIONAL LEADERSHIP + three-value stack» — Heroku v188** (engine `thammen-sprint2p22p0b20-conditional-leadership`; 🔴 Gate-2 SIGNED [Anas F6, `docs/SESSION_CLOSE_2026-06-11_F6_SIGNED.md`; spec `docs/BRIEF_conditional_leadership_SIGNED.md`; basis `docs/PHASE0_conditional_leadership.md` §2.7 = **7/13 = 54% cost-led**]; Gate-1 «go» 2026-06-11). The single `_leadership_gate` replaces the b6/b11/b16 elif chain (income_led precedence + b13/b18 sensitivity + b4 levers unchanged; pure calculators kept): **RULE 1** matched n≥10/disp36<0.30/E26-band match → market · **RULE 2** geo-full n≥20/disp<0.30 → market + «حوض جغرافي غير مطابق طبقياً» + MUC+1 + cost floor · **else COST LEADS** F3(b) [cost…market-muted] + MUC high + the age-honesty line · **E25 rail LOCKED** (cost never leads upward; double-weak divergence) · F2=B re-survey untestable · F4/F5 subsumed · the §4-a invariant. `valuation.leadership` + `valuation.value_stack` emitted on every valued villa/house (G1+G2 — the DRC always shown + the dispersion numeric in JSON); raw_land emits «قيمة التكلفة (نهج DRC) ≡ قيمة الأرض». **Live smoke v188 (22-cohort, two-lane CC) == the signed table:** امريخ **cost-led 2,400,000 [2.4M…5.4M-muted]** (cost 2,378,094; its geo-full basis pool n=51 disp **0.620** fails its own reliability bar) · V001 **3.8M floor→3.1M** geo-rescue (22/0.203) · V002/V003 rescued + «عمر مُعاد تسجيله — غير موثوق» + floor 2.3M · المعراض **2.6M E25-capped** + divergence · AbuHamour/56-565-19 byte-identical + stack · F4 trio cost-led 2.4/3.0/2.5M · F5 trio 1.1/1.7/2.1M · land 1.2M + DRC≡land · 8 refusal/hybrid unchanged. Verified: isolated **69/69** (incl. the 135-point invariant grid) · siblings green (b16 38/38 + b18 26/26 after 4 superseded wiring-pin re-points; V001 TD-93317 ±1% untouched-green) · DoD **392/15/45/broad ALL GREEN** · local E2E **46/46** · R14 [0 errors, no overflow]. **The four legacy anchors are RETIRED as methodology truth** (SESSION_CLOSE §2.1) — fresh post-ship captures = `.b20_live_fixtures.json` (ENGINEERING fixtures only, Phase-0 §4.2); **V001 ±1% = the sole surviving value anchor**. CHANGELOG_v102 · §20.53. **✅ b19 SHIPPED same-day (Heroku v189, CHANGELOG_v103, §20.54 — the three-value report display: the DEF-12 block now composes سوقية/كلفة/جبري×0.90 verbatim from `value_stack` [display-only; engine = 2 version lines; live smoke 4-case byte-identical to the fixtures] + `validate_gt_sheet.py` = the D-3 sheet-calibration kit [self-check on V001: +0.35% ±1%; appends to docs/validation/VALIDATION_LOG.md]). **+ b21 SHIPPED same-day (Heroku v190, CHANGELOG_v104, §20.55):** the Marikh 632-case surface sweep (the 8-invariant checker; kit committed) found the ONLY breach — INV-3/E26 via the b6 income-rail ceiling (the v3 cost depreciates on the USER age; fp+rent combos only) → `_age_neutral_rail_cost` closes it (#39 measured deviation: the literal `_cost_av` ceiling breached the signed enumeration — the age-neutralized v3 ceiling reproduces it exactly); the re-sweep = 8/8 ZERO breaches; live v190 fixtures byte-gate 4/4 + the cured pair (fp450+rent15k+age40 → 2.8M income_led). **+ b22 SHIPPED 2026-06-11 (Heroku v191, CHANGELOG_v105, §20.56): سياج زوج الأبراج (tower-pair fence)** — the Phase-0 income-types recon (`docs/PHASE0_income_types_exposure.md`, commit `7a3dc80`) measured the worst back-door: the 2.16.10 (unit_count×avg_monthly_rent_per_unit) multiply was UNGATED on asset type (the documented §19 TOWER_LIKE gate never existed in code, #58 → **R23**) → villa 54/541/6 + 12×5,000 = a laundered «إيجار فعلي» → **income_led 11.2M vs the signed 2.4M cost-led**, reachable with ZERO typing via a UI stale-leak (the pair persisted across evaluations); 🔴 micro Gate-2 SIGNED (the contract enumeration) → pure `_derive_rent_from_unit_pair` + `_TOWER_PAIR_ASSETS` (incl. compound_small — the address-entry large compound quick-classifies there, #39) — non-tower + pair ⇒ IGNORED + the verbatim «مدخل برجي على أصل غير برجي — تم تجاهله», never feeds income; tower-like byte-identical; b6 bare-rent untouched; UI `syncTowerPair` clear-on-non-tower + the «يتطلب:» line drops once valued + a disclosure chip; isolated **63/63** + local E2E 4/4 byte vs the v190 captures + DoD **392/15/45/broad ALL GREEN** (one stale 2.16.10 sync pin re-pointed, R6-class) + R14 [0 errors, no overflow] + **live smoke v191: villa+pair → 2.4M cost-led + the flag (was 11.2M)** · apt+pair 8,529,231 byte · compound refusal byte · bare ≡ fixture; the recon also proved **the apartment income path works end-to-end** (8.53M via the pair, no value_stack/leadership on buildings — outside b20 by design) and **the compound GAI promise is BROKEN** (44.35M computed then discarded — the DCF fork precedes the E20 promotion). **+ b23 SHIPPED 2026-06-12 (Heroku v192 + key v193, CHANGELOG_v106, §20.57): «بثّ المختصر» (short-report scenarios + tamper-evident verify)** — 🔴 micro Gate-2 ADDITIVE-ONLY SIGNED: (1) `valuation.scenarios` (villa/house — as_is/renovated_excellent/luxury_finish/teardown_land via the EXISTING b11/b13 DRC + B-1 floor + b4 demolition, ZERO new GIS) · (2) `report_ref` TH-YYYYMMDD-ZZSSSBBB[-4hex] · (3) `report_fp` = HMAC-SHA256(HMAC_REPORT_KEY,"v1|addr|date|engine|amount|low|high|rule")[:12] (DORMANT without the key, #62) · (4) `GET /verify` recomputes via the SHARED canonical+fp (can't drift), constant-time compare, RTL ✓/✗, no storage, rate-limited string-form; index.html report renders the scenarios panel + report_ref + verify link; the headline (amount/low/high/method/rule) is NEVER touched — the 22 fixtures byte-identical (structural + isolated 7.3). Verified: isolated **47/47** + DoD **392/15/45/broad 92 ALL GREEN** + R14 [scenarios 2.4/2.7/3.0/1.7M, verify link, no overflow, 0 errors]; **live /verify PROVEN (✓ valid / ✗ forged) with the real Heroku key + served HTML carries the b23 surfaces**; 🟡 **the value-byte-smoke (4 fixtures) is DEFERRED — khazna is hanging for Heroku (R5 infra, NOT a b23 defect: H12@30000ms, no Python exceptions); runs on khazna recovery**. The PO named the deferred non-villa data work **«بوابة بيانات الأنواع»** (the types-data gate): compound GAI promise + buildings value_stack/leadership + types-tab/coming-soon + buildings cap-rate calibration — each its own signed Gate-2. **NEXT = the value-byte-smoke on khazna recovery (the one open b23 item) · then the GT-collection track (D-3) · OR §6 v2 remainder · OR B-2-proper (n≥20 documented) · OR «بوابة بيانات الأنواع» candidates · deferred: the income-vs-system-DRC ceiling doctrine (#42, §20.55).**
> - **R9/A16 CLOSED at a18** (errata 2026-06-09, see RISK_SUMMARY.md).
> - **Operating Model v2 (lean) ADOPTED 2026-06-02** → `docs/OPERATING_MODEL_v2_lean.md` (classes-of-service Fast/Full/Gated; C1 = doc-delta committed before build routing).
> - **Engineering = ACTIVE on signed briefs (#32)** — **last shipped b13 / Heroku v182** (current detail = the **Live** bullet above; the b10→a19 cascade below is **FROZEN HISTORY** — its per-sprint «NEXT» / «gate #6» / «§20.9 cost-triangulation = NEXT» lines are point-in-time and superseded by the **Live** bullet + RISK_SUMMARY: the §20.9 GATED slice SHIPPED b13, the UP-lift was DROPPED→B-2/E25, and "gate #6" is DELETED/ISS-G03). _Frozen cascade from b10:_ Sprint 2.22.0b.10 — **geometric footprint**: max-buildable ground footprint from the plot's actual dims − legal R1 setbacks [front 5/side 3/rear 3, E15] bounded by the 60% coverage cap; **edge-pairing on the 4-vertex ring** [rotation-safe — the bbox is WRONG, Qatar rectangles are rotated vs the 2932 grid] + `is_rectangular` gate, non-rect → coverage-cap fallback; **zero new GIS** [reuses the already-fetched plot polygon]; `valuation.geometry` += plot_dims_m + max_buildable_footprint_m2 + footprint_method + the «الحدّ الأقصى — عدّله» note; index.html shows the dims + max-buildable on the results card + an editable footprint hint on `refineScreen`; **🔴 VALUE-INVARIANT [recon D1]** — `_suggested_fp`/`_eff_fp`/substantiality/`valuation.amount` UNTOUCHED, the footprint→BUA→headline auto-wiring is the §20.9 cost-triangulation Gate-2 NOT here; Heroku **v177**, commit `c1c92fe` split `588a3b6`, CHANGELOG_v91, §20.44; isolated **24/24** + DoD **392/15/45/broad 79** + R14 Chromium [0 console errors, geometry card + hint render, no overflow 390×844] + live smoke v177 [3 villas byte-identical 5.4M/2.4M/3.8M; 54/541/6 → setback_envelope [35.0,17.5]=311; 56/647/6 V001 5-vertex → coverage_cap 391]; framing F-1..F-4 Anas-signed; recon `docs/PHASE0_2p22p0b10_geometric_footprint.md`). Prior **b9** (Sprint 2.22.0b.9 — QARS **property-basis panel**: DISPLAY-ONLY / value-invariant — surfaces **PIN + رقم الكهرباء + water + building-age FLOOR** [from QARS `SURVEYED_DATE`] on every eval; the age **NEVER feeds `building_age_years`** [no Gate-2]; all auto-fetched from the QARS call `find_property` already makes [zero new GIS — `SURVEYED_DATE` the only newly-captured field]; **56/647/6 reproduces the Al Manara bank report (TD 93317) LIVE**: pin 56101583, electricity 140502, age ≥17y; self-correction [Rule #36] — electricity IS auto-fetchable [`ELECTRICITY_NO`]; Heroku **v176**, commit `cb090bc` split `143c617`, CHANGELOG_v90, §20.43; isolated **29/29** + DoD **392/15/45/broad 78** + R14 Chromium [0 errors, no overflow 390×844] + live smoke v176 [4 anchors byte-identical]; +Empirical **E15 corrected** [R1 front 5m not 3m, +June-2026 amendments]; born from a real bank Cost-Approach valuation. **NEXT = the §20.9 cost-triangulation = Gate-2** [the **durable R7 over-anchor fix**: BUA × depreciated construction rate + land → an independent Cost-Approach value ~2.9M; **the b10 geometric footprint ✓ is the BUA input — the prerequisite is now shipped**; needs a calibrated construction rate (the Al Manara bank's ~2,380 ر.ق/م² for V001's premium build = the anchor; ordinary < premium) + its own §5 audit + Gate-2 sign-off — hand-proven on 54/541/6: 35×17.5m plot − setbacks → ~238–311m² footprint → ~2.8–3.0M vs the blind 5.4M; seed `docs/METHODOLOGY_cost_triangulation_v1.md` + §20.9] · OR §6 v2 remainder [Fork C + (ii) age-rent] · OR B-2 [PARKED n≥20] · OR beta go-call [gate #6, Anas — the binding launch unlock]). Prior **b8** (Sprint 2.22.0b.8 — §6 v2 **income OPEX alignment**: 🔴 Gate-2 delegated via PO «افعل الأصوب، بعد استبعاد البيتا»; NOI opex matches the calibration opex on calibrated villa cap rates [villa 0.20, was the flat 0.23] → closes the **-3.75%** villa-calibrated income understatement [income_led headline + displayed cross-check]; compound/fallback keep 0.23 byte-identical; **value-invariant on live no-rent traffic** [4 anchors byte-identical]; **found PRE-BUILT in the tree by a parallel session — CC independently re-verified vs the recon + re-measured DoD/E2E before push**; Heroku **v175**, commit `f01704b` split `7d1f7fa`, CHANGELOG_v89, §20.42; isolated **19/19** + DoD **392/15/45/broad 77**; **live smoke v175:** 54/541/6 +rent 15k → income_led **2.7M→2.8M**, 4 no-rent anchors byte-identical [2.4M/5.4M/2.6M/refusal]; **🔴 HONEST RESIDUAL: live payoff BETA-GATED** [income_led needs a subject rent → live no-rent traffic headline-unaffected; only the displayed income cross-check on villa-calibrated surfaces moves]. **NEXT = §6 v2 remainder [Fork C (robustness, GIS↔GIS works today) + (ii) age-rent (gated on auto-age reliability, E22)] · OR B-2 condition axis [the durable no-rent gap-narrower, R7 PARKED n≥20] · OR beta go-call [gate #6, Anas — the binding unlock + the rent source that activates income_led's live payoff]**). Prior **b7** (Sprint 2.22.0b.7 — §6 v2 **cross-bracket yield-borrowing**: 🔴 Gate-2 delegated via PO «افعل الأصوب» [§20.18]; recon `PHASE0_R7_income_v2_600-900_recon` overturned the §20.40-deferred "calibrate 600-900 cells" [**data-infeasible** — 0/187 usable villa cells at 600-900] → `_lookup_calibrated_cap_rate` now **borrows the area's usable 400-600 cell** when the subject's exact plot bracket has none [disclosure + MUC-high + the [land_floor,cost] clamp as rails], `_income_triangulation` forces MUC high on a borrowed yield; **decisive finding** — the lookup queried strictly at the subject bracket so a 600-900 subject EVEN WITH a rent got 4% fallback → income_led couldn't fire; **value-invariant on ALL live traffic** [borrowing fires only with a subject rent at a no-cell bracket; exact-bracket 400-600 byte-identical]; `api.py`+`index.html` UNTOUCHED; Heroku **v174**, commit `731f864` split `c77302e`, CHANGELOG_v88, §20.41; isolated **22/22** + DoD **392/15/45/broad 76**; **live two-lane smoke v174:** 54/541/6 DEFAULT (600-900) + rent 15k → **income_led 2.7M via borrowed=True from=400-600, MUC high** [THE KEYSTONE — was widen_down in b6], 56/565/21 2.4M / 54/541/6 no-rent 5.4M widen / 55/296/13 2.6M / 52/903/90 refusal byte-identical; **🔴 HONEST RESIDUAL: live payoff BETA-GATED** [income_led needs a subject rent → no-rent Marikh/villa-6 stay widen_down]; **DEFERRED §6 v2 remainder: opex 0.20 + Fork C + (ii) age-rent**. **NEXT = §6 v2 remainder [opex 0.20 + Fork C + (ii)] · OR B-2 condition axis [the durable no-rent gap-narrower, R7 PARKED n≥20] · OR beta go-call [gate #6, Anas — the binding launch constraint + the rent source that activates b7's payoff] · OR condition-descriptors-from-listings exploration [Q-session idea]**). Prior **b6** (Sprint 2.22.0b.6 — §6 R7 income-triangulation: **🔴 Gate-2 — the villa headline MOVES** [first non-opt-in value move since b4]; PO «go» signed brief B1–B3 + «افعل الأصوب»; new PURE `_income_triangulation` — **income_led** [a GROUNDED subject rent (`actual_provided`) + a calibrated reliable/indicative cap-rate cell → income LEADS, comparison DEMOTED; **circularity guard** — only a subject-specific rent leads, the area-median rent ÷ area-yield reconstructs the comparison] + **widen_down** [a no-rent condition-blind THIN/widened/preliminary villa with `land_floor < comparison` → range widens DOWN to the land floor + range_is_headline + condition-widen note + MUC high; **EXCLUDES** clean reliable bracket / dispersion-gated / land-anchored; **no invented midpoint**]; villa/house; b4-region mutually-exclusive with teardown/luxury; `api.py`+`index.html` UNTOUCHED; Heroku **v173**, commit `575aa24` split `df41f3d`, CHANGELOG_v87, §20.40; isolated **23/23** + DoD **392/15/45/broad 75** [zero regression]; **live two-lane smoke v173:** 54/541/6 → **widen_down 1.9M–5.5M↓** MUC high [the live un-anchoring of Marikh's 5.4M guess], @400-600+rent → **income_led 2.7M**, 56/565/21 → **2.4M byte-identical**, 55/296/13 → unchanged [land-anchored, correctly NOT widened], 52/903/90 → refusal; **🔴 HONEST RESIDUAL: income_led is BRACKET-GATED [400-600 cells only → Marikh/villa-6 live (600-900) get widen_down ONLY, NOT grounded to ~3.2M until 600-900 yield cells calibrated]; widen range WIDE [tunable]; DEFERRED v2 = Fork C [a18/override lookup] + opex 0.20 + (ii) age-rent + 600-900 cells**. **NEXT = §6 v2** [Fork C + opex 0.20 + (ii) age-rent + **600-900 yield cells** so Marikh/villa-6 income-LEAD not just widen] · widen-width tune [PO call] · beta go-call [gate #6, Anas]). Prior **b5** (Sprint 2.22.0b.5 — R7 villa-yield calibration DATA ship: swapped `cap_rates.sqlite` → the per-area rebuilt DB [villa reliable **1→6** / indicative 2→10, incl. امريخ الجنوبي 400-600 5.16% net n=46 + المعمورة 56 4.83%]; the villa income cross-check now uses calibrated per-area net yields [vs the flat 4% fallback] when income fires + a usable (area,bracket) cell matches — **BRACKET-GATED** [most cells 400-600; standard anchors in 600-900 stay 4% — CORRECT]; **HEADLINE VALUE-INVARIANT** [income downstream of `primary['value']`, reconciliation status-only; live smoke **4 anchors byte-identical** 2.4M/5.4M/2.6M/refusal]; lookup correct as-is [GIS↔GIS `district_aname`, no §6 override needed]; **B confirmed LIVE** [Marikh @400-600 → «معدل رسملة معايَر 5.2% n=46 reliable» source=calibrated]; the **§9/§10 "ship yield-data" STANDALONE branch** [per-area gave §9's «and/or-broader» coverage]; +**Soft-Gate-3** stale 2.19.1 mock repair [R7 interface drift — `test_sprint_2p19p1_polish.py` latent red at `ba47835` the skipped broad-walk never caught; restores Fix#4/Fix#5 coverage, test-only]; Heroku **v172**, commit `0015600` split `148ef34`, CHANGELOG_v86, §20.39; DoD **392/15/45/broad 74** + calibrator 59+42 + 2p19p1 41; `api.py` UNTOUCHED. **NEXT R7 = §6 income-triangulation** [income SETS the villa headline + a18/override-aware `_lookup_calibrated_cap_rate`; separate Gate-2, **needs a signed Claude.ai brief**] — the bracket-gated footprint motivates it). Prior **b4** (Sprint 2.22.0b.4 — R7 condition/value axis: `condition=teardown` ↓ land−demolition [PO floor/cap 100k/120k/150k] · `new`+`is_luxury` luxury-new DRC/Cost-Approach ↑ · explicit `penthouse` ×2.5-BUA input; **Gate-2 PO-directed in the prior session [his demolition numbers embedded] + Gate-1 «go» 2026-06-07**; Heroku **v171**, commit `2cc5d2b` split `d0ecd82`, CHANGELOG_v85, §20.36; py_compile OK + isolated **29/29** + DoD **392/15/45/broad 73/73** + live smoke v171 [**4 standard anchors byte-identical** 2.4M/5.4M/2.6M/refusal — levers opt-in; `teardown`→2.4M ↓, `new`+luxury+PH→5.9M / −PH→5.2M ↑, penthouse +0.7M BUA]; **HONEST RESIDUAL [Rule #52, §20.36]: EXTREMES-ONLY — `good`/`very-good`/`renovated` middle (even +age=25) still over-anchors at the widened value [56/647/6 → 3.7–3.8M]; the 10-Year-Rule DOWN re-anchor fires ONLY on explicit `teardown`, not on old-age+good-condition → that is the next R7 step**; calibration n=2 [V002/V003] → MUC high + 💎 «منهج التكلفة، مُعاير على صفقات محدودة»). Prior **b3** (Sprint 2.22.0b.3 — range-as-lead [§2b authority/finality dial-down]: the results headline becomes the market RANGE [true low–high, asymmetry-ALLOWED — **NOT** a forced symmetric ±; PHASE0 recon §1: on thin paths the median sits AT the high edge, a symmetric bar would invent refused upside] + a muted central-estimate median marker «الوسيط (التقدير المركزي)», point fallback when no range; old two-box removed; **value_floor stays SECONDARY** [NOT land-to-median]; condition note + evidence panel + showConfirm UNTOUCHED; **value-invariant** [`api.py`+`evaluate_unified.py`-logic UNTOUCHED, engine diff = 2 version-string lines]; Heroku **v170**, commit `e39097c` split `29885bb`, CHANGELOG_v84, §20.35; isolated 15/15 + DoD 392/15/45/72 + **R14 real-Chromium** [0 console errors, range-lead live on bracket(symmetric) + thin(all-downside, NO invented upside), no overflow 390×844 hlRight 336<390] + live smoke v170 **4 anchors byte-identical** [2.4M/5.4M/2.6M/refusal] + served HTML carries «النطاق التقديري السوقي»; recon `docs/PHASE0_range_as_lead_recon.md` re-shaped the «symmetric ±» wording → true-range, Anas «GO» signed Gate-2; second step of the v4 «thinnest-flow» sequence). Prior **b2.3** (Sprint 2.22.0b.2.3 — confirmation gate [Screen 2, v4 owner-journey]: a NEW `confirmScreen` between identification and the result, rendered from the SAME `/api/evaluate` response [no 2nd fetch] — muted preliminary range [`valuation.low–high`] + READ-ONLY review of the GIS-fetched basis [no pencils/no correction CTA, existing AR labels, plot-area honesty label «المساحة المعتمدة في التقدير»] + the b.2.2 evidence panel reused + explicit «تابِع بهذه البيانات» → refine + permanent «التقرير الكامل الآن» → results; `run()` routing: valued non-valuer → confirm, **valuer + refusals skip to results** [v4 two-path, Rule #39]; **value-invariant** [`api.py` + `evaluate_unified.py`-logic UNTOUCHED, engine diff = 2 version-string lines]; Heroku **v169**, commit `6d3ac37` split `39b6f36`, CHANGELOG_v83, §20.34; isolated 32/32 + DoD 392/15/45/71 + **R14 real-Chromium** [9 fns defined, 0 console errors, no overflow 390/375/1265, full live flow buyer→confirm→refine/results + valuer-skip] + live smoke v169 **4 anchors byte-identical** [2.4M/5.4M/2.6M/refusal] + served HTML carries `confirmScreen`; first sprint of the v4 «thinnest-flow» sequence). Prior **b2.2** (Sprint 2.22.0b.2.2 — evidence-quality diagnosis panel: the binary confidence badge «🟢 شواهد كافية» → a 4-component **evidence-quality** panel [اكتمال بيانات العقار · جودة المقارنات · حداثة بيانات السوق · جودة توصيف المبنى — قوي/متوسط/محدود], each **DERIVED from its engine field** [§2c]; **«explanation≠confidence» enforced** [refine improves ONLY the user-input axes — proven live: اكتمال محدود→قوي + توصيف محدود→متوسط while مقارنات/حداثة held]; component 4 «غير منطبق — أرض» for raw_land; **value-invariant** [engine version-string only, 4 anchors byte-identical], `api.py` UNTOUCHED; Heroku **v168**, commit `74233e6` split `e6aa5b4`, CHANGELOG_v82, §20.33; isolated 26/26 + DoD 392/15/45/70 + **R14 real-Chromium** [0 console errors, 390×844 + desktop no-overflow, panel on bare/refine/land]; implements **DESIGN_2p2x §3, Phase 2** of the suspense-reveal arc). Prior **b2.1** (Sprint 2.22.0b.2.1 — separate input screens: frontend WRAP — `formScreen`=identification → bare `/api/evaluate`, new `refineScreen` hosts the relocated optional details, results staging card → **display-only** → `go('refine')`, tower CTA `goForm`→refine [**Rule #39** — preserves the tower/apartment rent path]; **value-invariant** [engine **version-string only**, 4 anchors byte-identical], `api.py` UNTOUCHED; Heroku **v167**, commit `80d0b1a` split `2ce45bb`, CHANGELOG_v81, §20.32; isolated 26/26 + DoD 392/15/45/69 + **R14 real-Chromium** [9 fns defined, 0 console errors, 390×844 + desktop no-overflow, full live flow form→2.4M→refine→**2.9M/eff 540** + tower CTA→refine] + live smoke v167 **4 anchors byte-identical** [2.4M/5.4M/2.6M/refusal] + `/details` fp600 → 2.9M/eff 540; served HTML carries `refineScreen`). The §4 strategic fork is now **RESOLVED → «enforce-visible-stage-boundary»** (`docs/DESIGN_2p23_stage_authority_boundary.md` §4; mockup-approved + Anas-signed via `docs/DESIGN_2p2x_v4_owner_journey.md`); the §2b authority/finality dial-down (range-as-lead) is now a **scheduled thin-flow step**, no longer an open fork. Prior **b1** (Sprint 2.22.0b.1 — zoning-driven ground-footprint suggestion [QNMP R1=60/R2=50, ×0.8, **capped at the legacy default → no silent inflation**] + **basement EXCLUDED from the comparison driver** [§5.5 — captured/displayed + future-DRC input, NOT a sales-comp premium; was +11.5% on a25] + assumed-vs-confirmed MVU labelling; **value-invariant on no-building-input anchors — only changes when the user supplies geometry**; recon RESHAPED the signed brief [§6 /quick-add was dead: the frontend posts to `/details`; basement-separate + floors-above-ground already held] → **3 deltas + augment-existing-panel**; backend localized to the substantiality stage [zone-aware `subst_bua` with `basement=False`, reuses already-fetched zoning → **zero extra GIS**]; `api.py` UNTOUCHED; Heroku **v165**, commit `4b39ba2`, CHANGELOG_v79, §20.29; isolated 34/34 + DoD 392/15/45/67 + R14 real-Chromium [0 console errors, 390×844 no overflow] + **local E2E on the real engine** [caught & fixed a §5.2 large-plot inflation edge]; live smoke v165 **4 anchors byte-identical** [2.4M/5.4M/2.6M/refusal] + **basement-excluded LIVE** [fl3 ≡ fl3+basement = 2.8M] + fp-cap [600→540 → 2.9M] + geometry surfaced). Prior **a25** (CC BY 4.0 **source attribution** for MoJ data — a persistent verbatim AR+EN credit [+ licence link → creativecommons.org/licenses/by/4.0/] in the results footer where derived MoJ figures appear; **user-facing copy / compliance hygiene — NO methodology/valuation change, value-invariant, every headline + B-1 `value_floor` byte-identical**; bidi `dir="ltr"` islands on `data.gov.qa`/`4.0`/`CC BY 4.0` per a24; confirmed the engine ingests `weekly-real-estates-sales-bulletin`; **closes COMPLIANCE Q13 + the open-data sub-item of RISK_REGISTER R13** [licence verified CC BY 4.0 = commercial+derivatives+redistribution OK w/ attribution]; `api.py` UNTOUCHED; Heroku **v164**, commit `d9d148a`, CHANGELOG_v77, §20.25; R14 real-Chromium [credit renders, 390×844 + desktop no-overflow, dir rtl/ltr islands measured, JS unchanged/0 console errors] + DoD 392/15/45/66; live v164 **ZERO value drift** [2.4M/5.4M/2.6M/refusal] + credit live in served HTML). Prior **a24** (beta-launch onboarding + affirmative-consent **entry gate** + **Terms/Privacy notice** + **DPIA** — content/frontend + a doc, **NO valuation logic, every headline + B-1 `value_floor` byte-identical**; gate is session-only [`sessionStorage` + in-memory fallback, **no cookie, no server write, stores nothing**; returning-within-session skips it, new session re-shows], Terms modal verbatim [7 AR + 7 EN] linked from gate/home/results-footer; **§4: scrubbed the property address from the two `/api/evaluate*` INFO logs** to back the DPIA "we don't store the address" [client IP kept for ops]; `docs/DPIA_AI_impact_beta_v1.md` committed verbatim; Heroku **v163**, commit `d538e93`, CHANGELOG_v76, §20.24; R14 **real-Chromium** [0 console errors, 390×844 + desktop no-overflow, RICS/IVS + 2025 + phone + Heroku-وCloudflare bidi all measured correct] + DoD 392/15/45/66; live v163 **ZERO value drift** [2.4M/5.4M/2.6M/refusal] + apartment refusal renders clean). Prior **a23** (R15 — `stock_strata.compute_land_median` now pools areas via a18 `area_match_key` like the floor → strata-card land **sibling-drop removed** [المعمورة 4032→3754 ≈ floor 3768]; **Gate-2 DISPLAY, HEADLINE value-invariant** — every headline + the B-1 `value_floor` byte-identical, bracket-matched anchors unchanged [E4 by-design, not sibling-drop — Rule #36 refinement]; `api.py`+`index.html` UNTOUCHED [R14 N/A]; Heroku **v162**, commit `ff483b0`, CHANGELOG_v75; a23 12/12 + DoD 392/15/45/66; audit `docs/PHASE0_R15_stock_strata_a18.md`; **RISK_REGISTER R15 → RESOLVED**). Prior **a22** (B-1.1 multi-AI-validated framing tweaks — **value-invariant, RICS/IVS citations UNCHANGED**: land floor → «indicative land component on an HBU premise», implied building → «contribution / mathematical allocation», widened `method_label` → «منهج المقارنة بالمبيعات (مجموعة موسَّعة جغرافياً)»; the models' 2025-renumbering "fixes" were REJECTED by Claude.ai primary-source adjudication [→ Rule #54 refinement: web-check GATES multi-AI on standards numbering]; `api.py`+`index.html` UNTOUCHED; Heroku **v161**, commit `2d401b5`, CHANGELOG_v74; a21 33/33 + a22 15/15 + DoD 392/15/45/65; R14 Chromium 390×844 re-measure clean; live v161 ZERO value drift + citations VPS 3/IVS 103/VPS 2/IVS 102 unchanged). Prior **a21** (B-1 land-floor / HBU decomposition + condition surfacing — **PRESENTATION ONLY, NO valuation logic, every value byte-identical**; surfaces a villa/house `value_floor` block [land-value FLOOR + implied-building residual + land-anchored disclosure] next to the a17/a19 condition caveat via new `evaluate_unified._villa_value_floor` [F2-prefer `value_decomposition.land`; F1-recompute `land_ppm²×plot` from the `moj_reference` land category **INDEPENDENT of Patch-C** → surfaces for the land-priced cohort ~10%/0%-reliable where `_decompose_value` returns None, implied clamped ≥0]; rides the `_condition_note_applies` gate [Rule #39: placed at the decomposition site, not the literal a14 block]; brief MU + muted `.rn` render; `api.py` UNTOUCHED; Heroku **v160**, commit `62f902a`, CHANGELOG_v73, §20.21; isolated 33/33 + DoD 392/15/45/64; **R14 EXECUTED** [real Chromium whole-file JS syntax + 390×844 overflow, node absent]; live smoke v160 **ZERO value drift** [56/565/21 2.4M, 54/541/6 5.4M, 55/296/13 2.6M land_anchored, 56/647/6 3.8M floor 2.46M, 52/903/90 refusal]). Prior **a20** (A7 `rics_compliant` honest status label — DISPLAY/LABEL ONLY, **NO valuation logic, every value byte-identical**; adds a neutral companion `rics_compliant_status_ar/en` = «بانتظار مراجعة مُقيِّم مُرخّص (المرحلة الخامسة)» / «Pending licensed-valuer review (Stage 5)» next to the `rics_compliant` bool on every JSON surface [root MU via `_enrich_material_uncertainty` + main-path 4714; brief MU section via output_briefs 595/933] so bare `false` reads "review pending," not "non-compliant"; emitted ONLY when bool is False [True/hybrid → no status; None → pending fail-safe]; bool + the `if not rics_compliant` recommendation [material_uncertainty.py:385] UNTOUCHED; recon: the bool **renders nowhere** in index.html [MU case ignores it; generic dumps skip booleans] → **backend-only**, `api.py`+`index.html` UNTOUCHED [R14]; wording verbatim-matches the live `rics_methodology_note`; Heroku v159, CHANGELOG_v72, §20.20; isolated 20/20 + DoD 392/15/45/63; live smoke v159: 56/565/21 = 2.4M, 54/541/6 = comparison_thin 5.4M, 52/903/90 refusal — all byte-identical + status_ar PRESENT). **Engineering NEXT (canonical):** Sprint **2.22.0b.3** ✅ **SHIPPED Heroku v170** (range-as-lead — §2b authority/finality dial-down; frontend, value-invariant; isolated 15/15 + DoD 392/15/45/72 + R14 + live smoke 4 anchors byte-identical; §20.35; second step of the «thinnest-flow» sequence). Prior **2.22.0b.2.3** ✅ v169 (confirmation gate, §20.34). Prior **2.22.0b.2.2** ✅ v168 (evidence-quality panel, §20.33). Prior **2.22.0b.2.1** ✅ v167 (separate input screens, §20.32). **v4 (`docs/DESIGN_2p2x_v4_owner_journey.md`, mockup-approved) RE-SHAPED the forward plan** — the v3 decision-framed-chapters plan (old b.2.3) is RETIRED; the §4 fork is **RESOLVED → «enforce-visible-stage-boundary»** (no longer a standing item). **The «thinnest-flow» sequence (#38, each Gate-2 + R14 + value-invariant):** (1) **confirmation gate** ✅ **SHIPPED v169** (Screen 2, §20.34); (2) **range-as-lead** ✅ **SHIPPED v170** (§2b dial-down — **true-range + median marker**, NOT a forced symmetric ±, NOT the rejected land-to-median; CC recon falsified «symmetric» — thin paths put the median at the high edge; §20.35); **NEXT = (3) condition sensitivity reading** (B-2 stays PARKED, n≥20); then (4) **decomposition in the polished result + report refinement**. **Ball = Claude.ai drafts the §6 income-triangulation brief** (income → villa headline + a18/override-aware lookup — the active R7 next after the b5 yield-data ship) **or the condition-sensitivity brief** (thin-flow step 3) — or Anas's beta go-call, gate #6 (the binding launch constraint). Prior Sprint **2.22.0b.2** ✅ SHIPPED Heroku v166 (guided staged-input flow — **frontend staging + 1 additive backend field** `effective_footprint_m2`; consumes b1's geometry surface; commit `39fb949` split `a13cfc8`, Gate-2 SIGNED brief `21f2e53`) — WRAP staging of the existing form into an explicit revisable Stage-2 confirm [«حسّن التقدير (المرحلة 2)» inline floors/footprint/basement → re-POST `/details` via `window._lastSubmit`] + backend `effective_footprint_m2` [brief F3 — the post-cap footprint the comparison used, value-invariant; F2 gates the confirm to villa/house, excludes raw_land/refusal]; isolated 22/22 + b1 34/34 + DoD 392/15/45/**broad 68/68 clean** + local E2E value-invariant [2.4M/2.9M, effective 540] + R14 390×844 0-errors/no-overflow; CHANGELOG_v80, §20.31; live smoke v166 **4 anchors byte-identical** [2.4M/5.4M/2.6M/refusal] + `/details` fp600 → **2.9M + effective_footprint_m2:540** [F3 live]. **NEXT = b.3** [F5 §2b authority/finality dial-down — brief + multi-AI #54] · beta go-call [gate #6, Anas] · **B-2 PARKED** [R7 n≥20]). Other near-term: (a) Anas's beta go-call (cohort + 6/2 path); (b) instrumentation activation — gated on Anas's DB-residency (Q3/Q4) + free-text (Art-16) decisions, then CC's gate-11 capture-surface security pass. Sprint **B-2** (R7 built-type/condition mechanism) is **Gate-2 SIGNED 2026-06-05** — two levers [UP finish/new-build premium on comp ppm² · DOWN 10-Year-Rule land re-anchor reusing the a21 `_villa_value_floor`], **Fork#1=MODERATE** (Lever-2 re-anchor: floor +0–10%, luxury-finish exception → floor +~20%, wide MUC), **Fork#2=WAIT-for-n≥20**; framing Rule-#54 web-checked (VPS 2 / VPGA 10 / IVS 102 ✓; stated condition = assumption+MVU, NOT a Special Assumption; +IVS 104) + §5-audited [DECISIVE: local `luxury_new` E4 stratum **n=0** in BOTH motivating areas → Lever 1 must be **corpus-calibrated** (cross-area GT-2), NOT a per-area MoJ lookup; Lever 2 **data-ready** (land floor n=20–33)]. **PARKED — resume trigger = Confirmed-Sales GT-2 corpus n≥20** (2.16.16, beta-fed). Brief `docs/BRIEF_SprintB2_mechanism_elicitation_SIGNED.md` · recon `docs/PHASE0_B2_condition_recon.md` · §20.27. (B-1 disclose-only already shipped; **Stage-1 input-honesty sprint CLOSED — premise falsified**, CHANGELOG_v78/§20.26.) **MULTI_AI batch decision-record COMMITTED** (`docs/MULTI_AI_VALIDATION_BATCH_SprintB1.md` — LOCKED outcomes from brief §2 D3; only the optional raw GPT-5/Gemini transcript pending Anas paste); non-blocking flags = PDF-prominence (brief §7) + R15 + a18 sub-zone live hit. **Bidirectional R7 now MEASURED + CONFIRMED:** **V002/V003** = project's FIRST **GT-2 confirmed sales** (56/565/10+12 Abu Hamour, NEW premium villas **SOLD 4.0M** each, engine 2.4–2.5M = **−37/−40% UNDER-anchor**) + **V001** Maamoura 56/647/6 (old premium, OVER-anchors the rejected 3.8M ask) — `docs/validation/VALIDATION_LOG.md`. **Confirmed-sales corpus (2.16.16) REVIVING (Anas-fed)** = the calibration unblock. Discipline: n<20 → motivates, NOT calibrates; B-1 = presentation-only; a17/a19 caveat VALIDATED by V002/V003. A7 → CLOSED (label shipped; bool by-design, no rename per #47). Prior: a19 (thin-path condition caveat, v158, §20.19). Monetization still gated on the regulatory enquiry (MoJ open-data licence ✅ CLOSED a25 — CC BY 4.0 verified + attribution live).
> - **Rule-count = FROZEN** — no new Operational rules beyond #65; lean defaults live as `ROLES_AND_COMMS` conduct, not rules.
> - **Launch gating (canonical, 2026-06-05 — SINGLE SOURCE; close-out "Carried forward" sections reference THIS):** the free invited beta launches under the **2026-06-02 conservative self-clearance**. **Pre-invite requirements:** (1) decision-support framing + consent layer — **a24 ✓**; (2) CC-BY source attribution — **a25 ✓**; (3) cohort + access setup (Anas) — **practical step, NOT a blocking gate (ISS-G03: beta = parallel non-blocking track, no cohort gate; the a24/a25 consent+attribution layers above ARE the preserved R13 cover)**. The **Aqarat regulator enquiry is a pre-MONETIZATION gate** (held until product design/build complete; sent before any paid access) — **NOT a pre-invite / beta blocker** (التقييم العقاري is regulated, Decision 28/2023 — licence BEFORE any paid access). **Open-data licence gate: ✅ CLOSED** (CC BY 4.0, a25). In-beta feedback flows to Anas's WhatsApp per the notice → the in-app feedback UI (Sprint 2) is **not required for the beta**. Pointers: Aqarat draft `docs/Aqarat_Enquiry_DRAFT_hold.md`; PDPPL self-clear = R11; checklist `docs/COMPLIANCE_SELF_CLEARANCE_beta_v1.md`; decision = R13. Earned gates (safety / compliance / valuation-honesty) stay; only delivery ceremony is trimmed.

> **Operating Mode (Autonomous Lead) — adopted 2026-05-29.** Supersedes the
> implicit "Claude.ai drafts → Anas signs every step → Claude Code implements"
> loop for **reversible** work only. The two hard gates below are unchanged from
> Rule #32 and the §"Self-correction triggers" STOP list — this block does not
> relax them, it scopes everything *else* to autonomous lead.

### Operating Mode — Claude Code leads

**Default: Claude Code drives the whole process and self-corrects.** Run recon,
instrument, refactor, write/fix tests, iterate locally, run smoke probes, and
**correct what is wrong as you find it** — without round-tripping to Claude.ai or
waiting for a sign-off — for anything **reversible**. "Reversible" = revertable by
a single local edit or redeploy, with no change to user-facing output and no
production state left behind.

Three gates remain. They are not velocity drag; each has documented scar tissue.

#### 🔴 HARD GATE 1 — Production push to Heroku
Never `git subtree push --prefix "deploy v2" heroku master` without **explicit
Anas consent in that session**. Unchanged from Rule #32 / STOP list. A wrong push
= deploy + log churn + regression + broken Sprint atomicity — irreversible at low
cost. Before asking: state branch, tests pass (actual numbers), ENGINE_VERSION
bumped y/n, CHANGELOG present y/n.

#### 🔴 HARD GATE 2 — Methodology / user-facing output change
Any change to *what the engine returns to a user* — valuation logic, confidence
tiering, MUC, refusal/scope decisions, disclaimers, Arabic copy semantics — stops
for an Anas sign-off **before** it lands. Reason is audit, not quality: "Anas signs
all methodology decisions for auditability." An unsigned methodology change = a
broken RICS audit trail. The §9 degraded-QARS fail-soft is the live example: it
looked like "perf" but invents a new output → gated. **Test for this gate:** "would
the JSON/UI a user sees differ?" If yes → gate. If the change only moves
*when/how fast/whether-it-503s* with identical success-path output → reversible,
no gate.

#### 🟡 SOFT GATE 3 — Scope beyond the signed brief
Single-purpose discipline holds (Rule #38), but does not require a stop.
**Flag-and-proceed:** if a fix needs to step outside the signed brief, state in one
line *what* and *why* (Rule #39: why / what-is-lost / what-Anas-needs-to-know), then
proceed unless Anas objects. New genuinely-separate work → log to the deferred list
(Rule #42), don't fold it in silently.

#### Multi-AI (GPT-5 / Gemini) — sprint open only
Invoke at the **start of a sprint when needed** (evolving-standard checks,
effective-date traps, methodology framing) — this is exactly Rule #54. Not part of
the per-change loop; Claude Code does not pause mid-execution for it.

#### What this changes vs. before
- **Gone:** Claude.ai brief-review ping-pong as a precondition for routine
  reversible work, and per-step "may I?" on recon / instrumentation / refactor / tests.
- **Kept:** the two 🔴 gates (push, methodology/output), the soft 🟡 scope flag, the
  full STOP list (#33–#42, E7, A2, 51/835/17, etc. — *correctness* triggers, not
  approval gates, and Claude Code self-applies them).
- **Claude.ai role now:** sprint-open methodology framing + multi-AI when asked;
  available for review on request; not a mandatory gate on reversible work.

```
⚠️ HISTORICAL SNAPSHOT — FROZEN at Sprint 2.22.0a.16 / Heroku v155 (2026-06-01). NOT current.
   Live truth = the «🧭 CURRENT STATE + NEXT STEP» bullet ABOVE + Session_Log §20.45–§20.47 + RISK_SUMMARY.md.
   Everything in this block (engine / api-health / Heroku vN, "Latest Sprint", "NEXT", bug lists, gate
   framing) is PINNED to a16 and STALE for forward state — read it as history, not current state.
   Now live: b13 / v182. Open Medium bugs = A5 ONLY (A15 ✅ CLOSED b12/v181; A16=R9 ✅ CLOSED a18).
   The §20.9 GATED slice ✅ SHIPPED b13 (Lever-1 convergent-TRIM, user-age-gated); Lever-2 UP-lift
   DROPPED → B-2 (E25: V002/V003 sold above replacement cost → market premium, not cost-reachable).
   "gate #6" (beta cohort) DELETED — ISS-G03 (beta = parallel non-blocking GT track, no cohort gate).

Engine version deployed:  thammen-sprint2p22p0a16-precapture-privacy-hardening
                          (Heroku v155, 2026-06-01, commits 03a4fb8 [code] + 94075f2 [label tweak]; Sprint
                          2.22.0a.16 — pre-activation PRIVACY HARDENING of the a15 dormant capture. ADDITIVE/
                          structural, NO valuation-logic change; capture STILL DORMANT. (Q1) UUID id = SOLE
                          key + join target; address-embedding valuation_id NEVER stored (display-only in
                          the response); active mode returns it as capture_id; feedback FK via prediction_id.
                          SHA-256(valuation_id) REJECTED (enumerable -> brute-forceable; Operational #62).
                          (Q2) zone PLAINTEXT; street/building Fernet-encrypted, gated on CAPTURE_ENC_KEY
                          (NULL — never plaintext — without a key). (Q3) created_at + 180d expires_at; dormant
                          aggregate_and_purge_expired -> prediction_zone_agg + DROP per-record rows;
                          erase_prediction row-level; backup erasure = activation runbook. (Q4/D1) free-text
                          note REMOVED (note OR valuation_id -> 422). (D3) 4 output labels «التقييم» ->
                          «التقدير السوقي» [provisional; mobile-shortened]. +cryptography (lazy). Two-lane
                          post-deploy smoke v155 BYTE-IDENTICAL on the 4 anchors (56/565/21=2.4M, 54/541/6=
                          4.5M, 55/296/13=2.6M, 52/903/90=refusal; NO capture_id -> dormant); /api/feedback
                          {prediction_id} dormant -> {accepted, stored:false}; note/valuation_id -> 422.
                          ACTIVATION counsel-gated (§8.1 PDPPL + §8.2 cross-border + gate-11 security pass)
                          [RISK_REGISTER R11]. origin in sync 94075f2.)
api/health version:       3.1.0-sprint2.22.0a.16
Latest CHANGELOG:         CHANGELOG_v68.md  (Sprint 2.22.0a.16 — pre-activation capture privacy-hardening.
                          instrumentation.py rewritten [UUID-only key, no stored valuation_id, street/
                          building Fernet-enc gated on CAPTURE_ENC_KEY, 180d retention, aggregate/purge/
                          erase, no note]; api.py capture_id active-only + FeedbackRequest prediction_id
                          [drops valuation_id + note]; index.html 4 labels -> «التقدير السوقي»;
                          +cryptography. Isolated 26/26; DoD 392/15/45/58 [broad 58/58 on re-run].)
Latest Sprint:            2.22.0a.16 — pre-activation capture privacy-hardening (Session_Log §20.16)
                          - capture STILL DORMANT (flag-off + no DB + no key -> zero footprint); additive, NO valuation change
                          - UUID-only key + valuation_id NOT stored (Q1); street/building Fernet-enc gated on key (Q2)
                          - 180d retention + dormant aggregate/purge/erase (Q3); free-text note removed (Q4/D1)
                          - output label «التقدير السوقي» provisional (D3); SHA-of-enumerable REJECTED -> Operational #62
                          - two-lane smoke v155: 4 anchors byte-identical; /api/feedback 200 dormant; note/valuation_id -> 422
                          - ACTIVATION gated on §8.1 PDPPL + §8.2 cross-border + gate-11 security pass (R11)
                          (full narrative: Session_Log §20.16; CHANGELOG_v68)

--- prev (2.22.0a.15, kept for reference — full detail in Session_Log §20.15 / CHANGELOG_v67) ---
Engine version deployed:  thammen-sprint2p22p0a15-eval-capture-feedback
                          (Heroku v154, 2026-06-01, sprint commit 8d6f304; Sprint 2.22.0a.15 — beta
                          instrumentation: prediction capture + POST /api/feedback. ADDITIVE BACKEND,
                          NO valuation-logic change. Shipped DORMANT: new instrumentation.py + guarded
                          capture call in both /api/evaluate* handlers + /api/feedback (FeedbackRequest,
                          extra=forbid). Capture writes NOTHING unless BOTH EVAL_CAPTURE_ENABLED=true AND
                          DATABASE_URL set -> zero data footprint; +psycopg2-binary (lazy, unused while
                          dormant). Field set data-minimized (§3): UUID id PK + valuation_id + address
                          SEPARATE/redactable (§8.3); refusals captured (§8.4); IP NOT stored. Two-lane
                          post-deploy smoke (CC browser-UA curl + Anas) BYTE-IDENTICAL on the 4 anchors:
                          56/565/21=2.4M, 54/541/6=4.5M, 55/296/13=2.6M comparison_thin n=8, 52/903/90=
                          refusal; /api/feedback dormant -> {accepted, stored:false}; extra -> 422.
                          ACTIVATION counsel-gated (§8.1 PDPPL + §8.2 cross-border) — add-on NOT
                          provisioned [RISK_REGISTER R11]. First beta-track sprint. origin in sync 8d6f304.)
api/health version:       3.1.0-sprint2.22.0a.15
Latest CHANGELOG:         CHANGELOG_v67.md  (Sprint 2.22.0a.15 — beta instrumentation. NEW
                          instrumentation.py (dormant capture/feedback, Postgres target, lazy psycopg2);
                          api.py +capture seam x2 + POST /api/feedback (extra=forbid); +psycopg2-binary;
                          ENGINE/SPRINT_TAG -> a15. Isolated 27/27; DoD 392/15/45/58.)
Latest Sprint:            2.22.0a.15 — beta instrumentation: prediction capture + feedback (Session_Log §20.15)
                          - DORMANT (flag-off + no-op without DATABASE_URL); additive, NO valuation change
                          - capture {id(UUID PK), valuation_id, zone/street/building, value, range, method,
                            tier, muc, ts}; feedback {id, valuation_id, outcome, price?, note?, ts}; IP NOT stored
                          - H1/H2/H3 verified; 4 anchors byte-identical (two lanes); /api/feedback 200 dormant
                          - ACTIVATION gated on §8.1 PDPPL + §8.2 cross-border (counsel); add-on NOT provisioned (R11)
                          - lesson: CC post-deploy POST smoke = browser-UA curl (Cloudflare 1010 blocks urllib) — #61
                          (full narrative: Session_Log §20.15; CHANGELOG_v67)

--- prev (2.22.0a.14, kept for reference — full detail in Session_Log §20.14 / CHANGELOG_v66) ---
Engine version deployed:  thammen-sprint2p22p0a14-bracket-honest-range
                          (Heroku v153, 2026-06-01, sprint commit 78ffd9b; Sprint 2.22.0a.14 — (vi)
                          bracket honest-range + window disclosure. PRESENTATION/COPY ONLY — no median/
                          value change. (a) _stage1_dispersion_gate extended to comparison_bracket (36mo
                          ppm2 >=0.30); the a10 application block reuses UNCHANGED (range_is_headline +
                          indicative tier + MUC high + AR/EN disclosure). (b) source_ar appends «(نافذة
                          36 شهراً)» + the Methodology brief shows «{n36} معاملة، منها {n24} خلال 24
                          شهراً» when n is a 36mo count; pure-24mo unchanged. Scope: all 20 dispersed
                          reliable villa cells gated (7 a13-rescued + 13 pre-existing); anchors clean
                          (Abu 0.208, Marikh 0.197). Live smoke v153 4/4: 56/565/21=2.4M [value IDENTICAL
                          + «(نافذة 36 شهراً)» + window split — CHECK-3 closed], 54/541/6=4.5M unchanged,
                          55/296/13 comparison_thin n=8 unchanged, 52/903/90 refusal. origin in sync 78ffd9b.)
api/health version:       3.1.0-sprint2.22.0a.14
Latest CHANGELOG:         CHANGELOG_v66.md  (Sprint 2.22.0a.14 (vi) — bracket honest-range + window
                          disclosure; additive ppm2_dispersion_36 + 2 MoJValuation fields + gate branch;
                          isolated 19/19; DoD 392/15/45/57. R10 generalized -> CLOSED-by-a14.)
Latest Sprint:            2.22.0a.14 (vi) — bracket honest-range + window disclosure (Session_Log §20.14)
                          - (a) comparison_bracket dispersion gate (36mo ppm2 vs 0.30) reusing the a10
                            block; (b) window disclosure on source_ar + Methodology brief
                          - scope ALL 20 dispersed reliable cells (7 a13 + 13 pre-existing); anchors clean
                          - PRESENTATION/COPY ONLY — central estimate byte-identical to a13
                          - boundary: 3 cells ±0.006 of T=0.30 may flip on a MoJ refresh (expected)
                          - fast-follow: direct live hit on a gated bracket cell (الغرافة/العب 600-900)
                          (full narrative: Session_Log §20.14; CHANGELOG_v66)

--- prev (2.22.0a.13, kept for reference — full detail in Session_Log §20.13 / CHANGELOG_v65) ---
Engine version deployed:  thammen-sprint2p22p0a13-thincell-credibility
                          (Heroku v152, 2026-06-01, sprint commit c366d66 [docs ->2bfec00]; Sprint
                          2.22.0a.13 — thin-cell credibility. Per-cell 36mo-capped fallback as continuous
                          P2 shrinkage of the surfaced TOTAL-PRICE median toward the cell's OWN 36mo
                          [w=n24/(n24+10), k=10], VILLA bracket only, n24>=5 floor, cap 36mo, range from
                          raw 24mo [gate-before-shrink]; ppm2 untouched. +10 thin->reliable; reliable
                          cells ~0% [max 2.2%, none >5%]; 154 <5-floor cells no-rescue. Live smoke v152
                          4/4: 56/565/21=2.4M [IDENTICAL, comp 28->37], 54/541/6=4.5M [byte-identical,
                          geo/A16-starved], 55/296/13=2.6M comparison_thin n=8 [no upgrade, ~-4%],
                          52/903/90=refusal. origin in sync 2bfec00.)
api/health version:       3.1.0-sprint2.22.0a.13
Latest CHANGELOG:         CHANGELOG_v65.md  (Sprint 2.22.0a.13 — thin-cell credibility. moj_reference
                          additive per-bracket n_24/n_36/total_price_median_24/36 + 24mo quartiles
                          [existing fields untouched]; apply_moj_strategy villa-only P2 blend + tier on
                          n36 + range from raw 24mo + trace note. Isolated 16/16; DoD 392/15/45/56.)
Latest Sprint:            2.22.0a.13 — thin-cell credibility (full narrative: Session_Log §20.13)
                          - 10 thin->reliable upgrades; reliable-move guard PASS; anchors 2.4M/4.5M
                            unchanged; 55/296/13 stays comparison_thin n=8 (gentle ~-4%)
                          - OPEN R10: 7/10 rescued cells dispersed >=0.30 present as clean reliable w/o
                            honest-range (a10 gate is widened-only); CHECK-3-live: bracket-success
                            source_ar discloses NO window for ANY villa cell
                          - NEXT=(vi) URGENT [bracket-success surface only, no value change]:
                            (a) a10 honest-range -> bracket path; (b) 24-vs-36mo window disclosure
                          - A16 = only Marikh lever (R9, own sprint); A7 open; LAND path deferred (villa-only)

--- prev (2.22.0a.12, kept for reference — full detail in Session_Log §20.12 / CHANGELOG_v64) ---
Engine version deployed:  thammen-sprint2p22p0a12-builttype-stratification
                          (Heroku v151, 2026-05-31, commit 9fa375c; Sprint 2.22.0a.12 — A2
                          built-type stratification of the villa comparable pool. USER-VISIBLE:
                          villa pool now PURE-villa [house بيت/مسكن + فيلتان + compound removed] →
                          pooled villa ppm2 median ~+9.7%; reference anchors STABLE [56/565/21 =
                          2.4M, 54/541/6 = 4.5M — robust total-price median]. origin in sync 9fa375c)
api/health version:       3.1.0-sprint2.22.0a.12
Latest CHANGELOG:         CHANGELOG_v64.md  (Sprint 2.22.0a.12 — A2 built-type stratification,
                          backend only. New shared built_type.py [built_type(row) → LAND / HOUSE /
                          STANDALONE_VILLA / None] applied at the two comp-selection sites
                          [moj_reference.build_reference bracket + geo_reference_v2.
                          _get_area_transactions geo], composes with A1's usage filter. LOCKS:
                          فيلتان EXCLUDED [measured −6 to −10% discount, distinct product — overturned
                          the earlier "fold"]; بنت هاوس FOLDED into villa [villa-range +18%, far from
                          APT ~827]; compound EXCLUDED by LABEL [مجمع/فلل/count-words]; بيت+مسكن →
                          HOUSE [resolved the مسكن categorizer split], removed from the villa pool.
                          Impact: villa ppm2 +9.7% pooled [pure-villa, H1]; net A1+A2 ~+4.5% above the
                          original contaminated median. Subject side CANNOT distinguish HOUSE from
                          VILLA → house subjects pool as standalone_villa → fix DEFERS to B. categorize/
                          _categorize KEPT for compute_trend + geo non-villa [Rule #39]. Isolated 28/28;
                          DoD 392/15/45/55; live smoke 4/4. Prior: v63=2.22.0a.11 A1 usage filter
                          [villa median −4.75%, §20.11])
A14 (CLOSED 2026-05-30):  villa cold-dyno first-try 503 — FIXED by Sprint A14 lever 2 (v146).
                          Live post-deploy H_lat: 56/565/21 cold first-try 200@14.4s + 200@15.0s
                          (×2) · 56/647/6 cold 200@15.9s — all <30s, margin ~15s, ZERO 503
                          (baseline was 503@31s). Lever 1 (overlap, H_A-cleared) DEFERRED —
                          unneeded (lever-2 margin huge). Bug A15 (silent-HBU-drop) ✅ CLOSED b12/v181 (§20.46)
                          (§20.5, separate sprint). NOT the closed A6 case (#53).
Latest Sprint:            2.22.0a.12 A2 — built-type stratification of the villa comparable pool
                          - built_type.py: built_type(row) → LAND / HOUSE / STANDALONE_VILLA / None
                            (NBSP-normalized نوع العقار) + matches_category; DRY, like usage_filter
                          - applied at BOTH comp sites (villa→STANDALONE_VILLA pool, land→LAND);
                            composes with A1 usage filter (a row must pass BOTH)
                          - LOCKS: فيلتان→None (measured −6 to −10% discount); بنت هاوس→villa (fold,
                            +18% villa-range); مجمع/فلل/count-words→None (LABEL-based); بيت/مسكن→HOUSE
                            (resolves the مسكن split), removed from villa
                          - impact: pooled villa ppm2 +9.7% FULL/+11.6% 24mo (H1; removed 41.5%:
                            house@350 + فيلتان/compound); reference anchors STABLE (56/565/21 2.4M,
                            54/541/6 4.5M — robust total-price median; anchors' valuation = CONDITION→B)
                          - subject side UNCHANGED: engine can't distinguish HOUSE from VILLA (QARS
                            subtype 1 = "Villa/House") → house subjects pool as standalone_villa
                            (live 55/296/13) → house-subject fix DEFERS to B (2.22.0b)
                          - thinning HONEST-not-broken: reliable cells 20%→12%, insufficient 48%→56%,
                            absorbed by 36mo fallback + a10 gate (live proof 55/296/13 = comparison_thin
                            n=8); a10 gate share ~unchanged 37%→39%
                          - Rule #39: categorize/_categorize KEPT for compute_trend + geo non-villa
                          - py_compile 4/4; isolated 28/28; DoD 392/15/45/55; live smoke 4/4
                          - CORRECTION to §20.11: compound exclusion is LABEL-based (نوع العقار), NOT
                            "via E20 area" — E20 is a SUBJECT-side guard, never touches comp rows
                          - NEXT: window-fallback 36mo-cap + light shrinkage (recon F: 36mo ≈ half the
                            +8-13% staleness drift); compute_trend categorizer alignment; methodology
                            doc §4 (3 strata + فيلتان excluded + compound label-based)
                          (full narrative: Session_Log §20.12; CHANGELOG_v64)

--- snapshot block below is PRE-2.22.0a (2.21.4-era), kept for reference ---
Engine (pre-arc):         thammen-sprint2p21p4-t3-aryan-lusail  (Heroku v125 code,
                          v127 config — T3_INVENTORY_ENABLED unset, default true)
prev api/health version:  3.1.0-sprint2.21.4
prev Latest Sprint:       2.21.4 T3 Developer-Inventory (Aryan, Lusail)
                          - HYBRID_TIER_CONFIG: T3_status_discount_map dict
                            (off_plan / under_construction → −17.5%; ready → −10%)
                            + T3_discount_default scalar + T3_stale_evidence_multiplier=0.5
                            + T3_discount_midpoint preserved as back-compat alias
                          - hybrid_valuation._process_t3_input — 3-shape detection
                            (dict_new with status / dict_legacy 2.21.2 / float / empty)
                            + per-row status discount + 0.5× stale freshness multiplier
                            + 7-field tier_breakdown sources per Rule E10
                          - developer_inventory.sqlite (17 cols, idempotent migration,
                            committed pre-deploy per ephemeral-FS workflow)
                          - 4 Aryan/City Avenues rows seeded (status=under_construction
                            post Anas pre-deploy correction §5.8; was inferred 'ready')
                          - T3 weight ceiling 0.12 = 0.15 cap × 4/5 evidence_strength
                            (BRIEF §9 architectural seal verified live)
                          - D10 flag T3_INVENTORY_ENABLED (mirrors HYBRID_APARTMENTS_ENABLED)
                          - H_WALK PASS: H1 canary + H11 live + H2 kill-switch live;
                            H3-H9 cited from 26/26 isolated + 29/29 regression;
                            H10 UI deferred to Sprint 2.21.5
Tests passing:            29 standalone files (28 pre-existing + new
                          test_sprint_2p21p4_t3_inventory.py @ 26 functions / 26 PASS).
                          Full regression 29/29 in 35.0s.
                          Sprint 2.21.2 tests (67/67) preserved via T3_discount_midpoint
                          back-compat alias. Run with PYTHONIOENCODING=utf-8.
Critical bugs open:       0
High bugs open:           0  (A6 latency ✅ CLOSED via 2.18.0 + 2.18.1 + 2.18.1.1;
                          A8 closed by 2.20)
Medium bugs open:         1  (A5 asset_type unknown [the ONLY open medium]; A15 silent-HBU-drop ✅ CLOSED b12/v181 §20.46;
                          A16=R9 MoJ-bracket under-match ✅ CLOSED a18 §20.18 [resolved-as-pool-fix];
                          A7 rics_compliant ✅ CLOSED a20 — honest status label, §20.20)

Recent Sprints (chronological):
  2.18.0   Parallel property_factors fan-out (−4s villa/raw_land, v99)
  2.18.1   Parallel BFS upfront-prefetch (−60s compound_small, v100, kills 503)
  2.18.1.1 Compound-misroute fix Patches A+C (v101)
  2.21.2   Hybrid Foundation: Rule E3 → 8 constraints + hybrid_valuation.py (v107)
  2.21.3   T2 PF Lusail apartments hybrid path (v124 = v121 code; first live
           hybrid evaluation; Heroku v110→v118→v121 audit-driven loop; D10
           Lusail sub-district whitelist; list-page-only connector refactor
           after detail-fetch latency overran 30s router)
  2.21.4   T3 Aryan/City Avenues + status-aware discount + freshness (v125;
           4 seed rows under_construction; T3 weight 0.12 = 0.15 × 4/5)
  → all live; engine version reflects the most recent (2.21.4).

Hybrid arc (Sprints 2.21.2/2.21.3/2.21.4): full T2+T3 weighted evaluation
                          path live for Lusail apartments. PIN 69/255/75 = H1
                          anchor (City Avenues, district='لوسيل 69', T3 fires).
                          PIN 69/329/20 = H11 anchor (Fox Hills, district='غار
                          ثعيلب', T2-only — natural partial-population test).

Pre-Sprints since (no engine change, diagnostic only):
  2.21.1 pre-MME smoke v1+v2 — Heroku reaches MME (P1 TRUE), but JWT is
         anonymous Directus token (role=null) → kpi29 returns count:0 for
         all queries. Rent paths (kpi30/31/32) verified DEAD. Sprint 2.21.1
         deferred pending DevTools capture of authenticated session.
         (Operational §28 annotated 2026-05-24 with the auth-scope caveat
         + the real {count, transactionList} response schema.)
  2.22.0 audit — H5 FALSE: apartment failures are DATA-driven, not
         latency-driven (3/3 reps on 52/903/90: HTTP 200 + val=None +
         4.7s). 3-stage architecture does NOT solve apartments;
         BRIEF_2p21p2 (hybrid foundation) returned to top of queue and
         shipped as Sprint 2.21.2. H1+H3+H4 evidence preserved.
  2.21.3 smoke — T2 connector reachability + URL discovery (Heroku-IP).
         4 of 5 TRUE. arady canonical search URL = /listings (HTTP 200,
         70 hits page 1); /sitemap.xml available. PropertyFinder reachable
         (Heroku-sandbox parity exact). H5 confirmed PF detail pages
         expose both price + area extractable tokens (CSS class
         property-price + regex fallbacks). DOM duplication finding:
         PF raw matches inflate ~6× over unique listing count — connector
         in 2.21.3 MUST deduplicate by canonical URL or listing ID.

Land Arc:                 ✅ COMPLETE — PIN input (2.21.0) + output polish (2.21.0.5)
                          + Asset Type Reality Check (2.21.0.7/.7.1).
Multi-QARS (2.21.0.9):    ✅ STAGE 1 LIVE (n_qars≥2 detection + bracket adjust).
                          Stage 2 wall-to-wall (E18) pre-specified for 2.21.0.10.
A6 latency arc:           ✅ COMPLETE in 3 Sprints (2.18.0 / 2.18.1 / 2.18.1.1).

Rule E3 (Empirical_Findings): EXPANDED 2026-05-24 by Sprint 2.21.2.
                          Now 8 numbered constraints permitting tier-weighted
                          listing entry via hybrid_valuation_v1(). E1 (no MoJ
                          uplift) preserved. T2 cap 0.40, T3 cap 0.15, T1 floor
                          0.45, MUC mandatory when T1 absent, no T3-alone valuation.

Operational rules added 2026-05-24:
  #53  Closed cases stay closed — including as comparison anchors. Cite
       §X / Rule #N, never the originating closed case as foil/precedent.

Mthamen integration:      ⏸️ Deferred indefinitely (Project_Instructions §20.8)
MME apartments (2.21.1):  ⏸️ Deferred — awaits DevTools auth capture on
                          mme.gov.qa (see 2p21p1_pre/CHANGELOG)

Roadmap:                  **AUTHORITATIVE = Project_Instructions §11 ("Deferred Sprints",
                          refreshed 2026-06-01, BETA-FIRST). The numbered convenience-copy that
                          used to live here was removed so §11 is the SOLE source (it kept drifting).**
                          §11 now leads with the confirmed priority queue:
                            1. A7 (rics_compliant — beta-credibility quick-win)
                            2. Sprint 2 — feedback UI prompt (index.html; consumes /api/feedback)
                            3. ACTIVATION of a15 — counsel §8.1 PDPPL + §8.2 cross-border (R11)
                               + a15 capture-surface security pass; then DATABASE_URL + flag
                            4. B — condition sprint (R7; prereq for 2.22.0b Stage-2)
                            5. 2.22.0b — 5-stage UX + Stage-2 elicitation
                            6. cost-triangulation (independent DRC, §20.9) — POST-2.22.0b
                          + behind-beta backlog (2.21.5 hybrid UI, 2.21.4.1/.2 data, 2.21.3.2 arady,
                          2.21.0.10 Stage-2 E18, 2.21.0.11/.12 cosmetic, 2.18.2 GIS dedup, 2.17, 2.20)
                          + deferred-indefinite (2.16.16 Confirmed Sales, Mthamen §20.8, MME apartments
                          [2.21.1/2.29, auth]). Open mediums: **A5 only** (A15 ✅ CLOSED b12/v181 §20.46; A16=R9 ✅ CLOSED a18 §20.18).

D5/D6 calibration:        provisional, broker-experience-grounded — and remain
                          so INDEFINITELY. NO viable recalibration source: both
                          the secretary feed (closed 2026-05-24) and the
                          brokerage (Gardenia, closed) are gone. Recalibration
                          would need a future genuinely-PIN-keyed T1 sale source
                          (none exists). NOT a blocker (discounts ship with the
                          MUC clause). Empirical basis (interim): EMPIRICAL_FINDINGS
                          §3 asking-premium ranges + broker negotiation
                          experience.

Deploy:                   git subtree push --prefix "deploy v2" (Operational #43)
```

-----

## Non-negotiable rules (recite verbatim)

### Pre-Sprint Audit (§5 of Project Instructions)

Before ANY Sprint proposal:
1. Pick 3–5 diverse properties (varied zone/age/asset type, include tower or apartment_building)
2. Pull ground truth from Qatar GIS (`khazna.gisqatar.org.qa` primary, Sprint 2.16.5)
3. Hit `https://thammen.qa/api/evaluate` for each
4. Compare GIS vs thammen field-by-field including BUILDING_NO_SUBTYPE
5. Open `index.html` and grep for the field name — confirm visible to user
6. Test mobile viewport (390×844) — Sprint 2.16.4 lesson
7. Quantify scope via GIS counts
8. Only then propose Sprint

For external endpoints (especially Qatar government):
1. Write `smoke_<endpoint>.py` as standalone file
2. `git subtree push --prefix "deploy v2" heroku master` (Rule #43 — not plain `git push heroku master`) + `heroku run python smoke_<endpoint>.py`
3. Verify reachability + content type + WAF response
4. Only then build integration

### Delivery format (§2)

- One zip per Sprint via `present_files` (when in chat) or direct file edits (when in Claude Code)
- `CHANGELOG_vN.md` mandatory per Sprint
- Sprint numbers sequential, never reused
- Windows `cmd` syntax (`cd /d`, `copy /Y`, `tar -xf`, `findstr`)
- **One command per line. Never use `&&`.**
- Engine version format: `thammen-sprint{Major}p{Minor}p{Patch}-{slug}`

### Pre-deploy 6-item checklist

1. `python -m py_compile` on every modified Python file
2. `node --check` on extracted inline JS from index.html
3. Mobile viewport test 390×844
4. Regression — **DoD TEST MATRIX (SINGLE SOURCE; other docs reference this), measured 2026-06-01 (Sprint 2.22.0a.13), run with `PYTHONIOENCODING=utf-8`:** aggregator `run_sprint_2p22p0a_suite.py` = **392/392** · security `test_sprint_2p16p17_security.py` = **15/15** · `test_sprint_2p22p0a3_surface_honesty.py` = **45/45** · broad `2p22p0_pre/run_regression_2p22p0a.py` = **56/56** (measured 2026-06-01; the prior known fail — `test_sprint_2p22p0a5_request_budget.py`'s EXACT-version-pin — was relaxed to a version-agnostic FORMAT check [verified in-code 2026-06-01: `startswith('thammen-sprint')` + dotted-numeric `SPRINT_TAG`], so RISK_REGISTER R6 ✅ resolved; +1 file vs the old 55 = the new `test_sprint_2p22p0a13_thincell_credibility.py`). `test_v2_modules.py` is **formally EXCLUDED** (needs pytest; not in requirements.txt; already in the broad runner's `SKIP_FILES`).
5. Isolated logic tests for new code (5+ cases including fallback)
6. Smoke test 3 diverse addresses from Heroku post-deploy

### Methodology (§3)

|Source|Role|Production?|
|---|---|---|
|MoJ (data.gov.qa)|Market truth (primary)|✅|
|DCF/Yield|Income (primary for income-producing assets)|✅|
|~~Mthamen (sak.gov.qa)~~|Cost (DRC) reference only|❌ deferred 2026-05-19|
|Listings|Sentiment only|⚠️ display only|

- **Median, not mean.** Always cite n.
- Sample size: n≥20 reliable, 10-19 indicative, 5-9 context, <5 insufficient
- 24-month window default, 36-month fallback when n<20
- Size brackets: 0-400 / 400-600 / 600-900 / 900-1500 / 1500+ m²
- Net yield: 5-6% normal, >6% bargain, <4% weak. Never gross without net.

### Stock stratification (Rule E4, Empirical_Findings)

- `land_priced` (<1.15) → 10-Year Rule
- `aging_stock` (1.15-1.50)
- `modern_stock` (1.50-2.20)
- `luxury_new` (≥2.20)

### Hard ceilings

- Buyer: never above MoJ median + 10%
- Seller: never insist above MoJ median + 30%

### Tower-aware input handling (Sprint 2.16.10)

For asset_type ∈ {tower, compound_large, apartment_building, commercial_building}:
- UI shows `unit_count` + `per_unit_rent` (not standalone `rental_income`)
- Backend: `rental_income_monthly = unit_count * per_unit_rent`
- Skip plot-based sanity check (Sprint 2.16.11 carve-out)
- MUC clause mandatory

### Zoning/Subtype cross-check (Sprint 2.16.14 — Bug A11)

QARS_Point.BUILDING_NO_SUBTYPE was last surveyed 2010-2012. Now classifier
checks against Zoning. If subtype ∈ {1, 6, 11} AND zoning ∈ {CCC, COM, CF,
SCZ, TU, LFR, LInd, IND, MU*} → emit `subtype_zoning_mismatch` flag.
9.1% of GOVERNMENT-category landmarks affected. 0% of Business/Finance.

-----

## Self-correction triggers (full list in §22 + Session_Update §5)

STOP if I:
- Propose a Sprint without running the audit → run §5 first
- Claim a bug based on memory → verify in browser (desktop + mobile)
- Write `&&`-chained command → split per line
- Cite a median without n → add n
- Rationalize MoJ staleness → acknowledge instead
- Treat Mthamen DRC as primary → methodology reference only (Project_Instructions §20.8)
- Try to "correct" Thammen value using Mthamen reasoning → gap is diagnostic
- Rebuild Mthamen's formula in our codebase → IP concern + brittleness
- Use 51/835/17 as timing baseline → A6 catalogued, use 52/903/90
- Propose `rental_income` for tower without `unit_count + per_unit_rent` → Sprint 2.16.10
- Bundle 3+ fixes into one Sprint → prefer single-purpose (marathon 2026-05-18 pattern)
- Propose reviving Mthamen live integration → §20.8, need 3 conditions met
- Propose integration with Qatar government endpoint without Heroku smoke test → §21.6
- Treat Mthamen as Sprint candidate → archived reference only
- Trust QARS_Point subtype as single source without Zoning cross-check → Sprint 2.16.14 / Rule E7
- Add a new FastAPI request model without `model_config = ConfigDict(extra='forbid')` → Sprint 2.16.15 / Bug A2 — silent typo-drop creates user confidence gap
- Attempt `git push heroku master` بدون التحقق من branch + §3 checklist + Sprint integrity + explicit user consent → STOP. راجع Operational_Rules.md #32 (Push & Commit Discipline). Default: لا تدفع، اسأل user صراحة.
- أبدأ audit بقراءة الكود بدل القياس → STOP، راجع Operational_Rules.md #33 (Empirical-First Audits). قِس أولاً (curl/logs/git log)، اقرأ الكود ثانياً.
- أكتب `heroku run python -c "..."` بـ argument معقّد (`&`/`=`/`+`/quotes أو >3 أسطر) → STOP، راجع Operational_Rules.md #34 (File-Based Scripts). اكتب ملف `probe_X.py` منفصل بدلاً منه.
- أكتب syntax مكتبة بدون التحقق من الإصدار على Heroku → STOP، راجع Operational_Rules.md #35 (Library Version Verification). تحقّق requirements.txt + `heroku run python check_version.py` أولاً.
- أذكر رقم empirical بدون التحقق من العيّنة الفعلية → STOP، راجع Operational_Rules.md #36 (Observed-vs-Expected Reporting). اذكر sample size + time window الفعليين، وما لم يُرَ.
- أتجاوز السقف الزمني للـ scouting بدون إذن user → STOP، راجع Operational_Rules.md #37 (Time-Boxed Scouting). أعطِ ما لديك + الناقص + تقدير زمن، واطلب سقفاً جديداً.
- أبني Sprint يخلط 2+ bugs غير مترابطين → STOP، راجع Operational_Rules.md #38 (Single-Purpose Sprint Scope). اقترح Sprints منفصلة، أو اطلب إذن bundling صريحاً مع تبرير التبعية.
- أنفّذ Y بدل X المطلوب بدون 3-جمل justification → STOP، راجع Operational_Rules.md #39 (Deviation Justification Protocol). اذكر: لماذا Y ضروري + ما يُفقد بترك X + ما يحتاج user معرفته لتفسير النتائج.
- أعتمد على replica tests فقط بدون verification ضد production class → STOP، راجع Operational_Rules.md #40 (Replica + Production Verification). أضِف سطراً واحداً على الأقل يستدعي الكود الإنتاجي الفعلي.
- أؤجّل/أستبعد عمل بدون توثيق في الـ docs + شروط إحياء → STOP، راجع Operational_Rules.md #42 (Deferred-Work Documentation). وثّق: ما جُرّب + لماذا أُجِّل + شروط الإحياء + توجيه قاطع للجلسات اللاحقة.
- أقترح Sprint بدون مراجعته خلال عدسة Stage 1/2/3 (E16) → STOP، راجع Operational_Rules.md #50 (Staged-Sprint Discipline). كل Sprint جديد يجاوب: أي stage يخدم؟ هل Stage 1 يمكنه الشحن مستقلاً عن Stage 2 data؟
- أرفع threshold مستنتج من بيانات صغيرة بدون مراجعة domain knowledge → STOP، Sprint 2.21.0.9 رفض ذلك (15.2m clustering أوحى بـ 18m، لكن Anas أكّد أن الفيلات منفصلة فعلياً مع ارتداد كامل — E15). data-driven inference لا تتغلّب على domain confirmation.
- أصمّم تصنيف GPS-centroid-based دون فحص MME setback code → STOP، راجع EMPIRICAL E15. فيلتين منفصلتين code-compliant على نفس قطعة لهما centroid ≥16m؛ أي threshold تحت ذلك false-positive محتمل. استخدم wall-to-wall (E18) بدلاً.
- أطلب من broker إدخال حقل يمكن جلبه آلياً من GIS/MoJ → STOP، راجع EMPIRICAL E17 (1-field minimum input). property identification فقط مطلوب من broker؛ كل شيء آخر auto-fetched ومرئي لمراجعته. Thammen verifies، broker corrects، أبداً العكس.
- أُعلن Sprint مكتمل بمجرد نجاح الـ deploy + الاختبارات دون فحص الـ response content على المسار الذي أصبح متاحاً للمستخدم → STOP، راجع Operational_Rules.md #52 (Latency Unmasks Methodology). كل Sprint يحول 5xx→2xx على مسار كان timeout سابقاً = response content على هذا المسار قابل للفحص لأول مرة → فحص methodology إلزامي post-deploy. Sprint 2.18.1 → 2.18.1.1 هو السابقة الأولى.
- أُصنّف compound_small بناءً على QARS subtype فقط دون فحص extent.total_area_m2 → STOP، راجع EMPIRICAL E20. compound > 15K m² لا يملك MoJ comparable (المسجَّل الأكبر 15,027 m²). Sprint 2.18.1.1 Patch A تروّج تلقائياً إلى compound_large عند extent ≥ 15K → Income Approach refusal pattern نظيف.
- أضيف decomposition (land + building) بدون guard ضد `land_value > valuation_amount` → STOP، راجع Sprint 2.18.1.1 Patch C. القاعدة: في أي function يحسب land_value × area للأصول السكنية، يجب return None لو النتيجة > valuation_amount. الـ guard universal (يلتقط premium-land villa teardowns + MoJ outliers + future bug classes).
- أكتب جملة جديدة تحوي "unlike [closed case]" أو "mirror [closed case] pattern" أو "precedent: [closed case]" → STOP، راجع Operational_Rules.md #53 (Closed cases stay closed — including as comparison anchors). احذف الجملة. الـ finding يقف بذاته. اذكر §X (القاعدة)، ليس الحالة التي أنتجتها.

-----

## Recall phrases (user shortcuts)

| Arabic | Meaning |
|---|---|
| "تذكر Sprint 2.16.X" (X=6..15) | Specific marathon/post-marathon Sprint |
| "تذكر Sprint 2.16.15" | Pydantic extra='forbid' / Bug A2 fix, deployed 2026-05-19 evening |
| "تذكر Bug A2" | Pydantic schema lenience — unknown fields silently dropped |
| "تذكر khazna" | GIS Qatar migration 2026-05-17 |
| "تذكر outage 17 مايو" | GIS outage timeline |
| "تذكر Lusail B201" | Tower Input Disambiguation example |
| "تذكر المثمن" | Mthamen reverse engineering + defer decision (§20.8) |
| "تذكر قرار 19 مايو" | Mthamen defer decision specifically |
| "تذكر Bug A11" | Zoning/Subtype contradiction discovery 2026-05-19 PM |
| "تذكر أشغال 61/875/20" | The reference case for Bug A11 |
| "تذكر Rule E7" | QARS subtype requires Zoning cross-check |
| "تذكر Sprint 2.21.0.9" أو "تذكر Stage 1" | Multi-QARS detection — staged-valuation pattern, no GPS-distance classification, 18m threshold rejected, wall-to-wall (E18) deferred to 2.21.0.10 |
| "تذكر Bou Hamour" أو "تذكر 56/565/21" | The Sprint 2.21.0.9 trigger case — 2 villas on PIN 56090294 (PDAREA=900), physically separate despite 15.2m centroid (full ارتداد + حوش per MME code E15) |
| "تذكر E15" أو "تذكر ارتداد البلدية" | Qatar MME 3m setback code → code-compliant separate villas have centroids ≥16m |
| "تذكر E16" أو "تذكر staged valuation" | Stage 1 (≤5s, ~70%) → Stage 2 (~90%) → Stage 3 (~95%+); every Sprint reviewed through this lens |
| "تذكر E17" أو "تذكر 1-field minimum" | Broker supplies property identification only; everything else auto-fetched; Thammen verifies, broker corrects |
| "تذكر E18" أو "تذكر قاعدة 6 متر" | Stage 2 wall-to-wall classification rule (wall<1m → attached; ≥6m → separate; 1-6m → sub_minimum). Replaces rejected GPS-centroid threshold |
| "تذكر #50" أو "Staged Sprint" | Operational_Rules #50 — every Sprint reviewed through Stage 1/2/3 lens |
| "تذكر Sprint 2.18.0" أو "تذكر parallel factors" | 5-way parallel `property_factors.analyze_property` via `ThreadPoolExecutor(max_workers=5)`. Deployed Heroku v99 (2026-05-23 evening, CHANGELOG_v44). −4s on villa/raw_land paths (multi_qars_56 26.8s→22.8s, khor_land 25.1s→21.2s); fast-paths unchanged; HTTP 503 class still present on compound_small (Sprint 2.18.1 territory). Audit prediction matched measurement within ±2% — first validation of Rule #51 + E19. |
| "تذكر E19" أو "تذكر max_workers" | I/O-bound parallelization of N independent fixed tasks → `max_workers = N`. More workers = idle overhead. Discovered Sprint 2.18.0 §5 mini-audit; pattern applies platform-wide. |
| "تذكر #51" أو "تذكر audit-driven Sprint" | Operational_Rules #51 — canonical performance-Sprint pattern: pre-Sprint §5 audit → audit-derived patch (measured bottleneck, scoped fix) → post-deploy audit comparison. Sprint 2.18.0 proved prediction accuracy ≤±2% across all measured paths. |
| "تذكر Sprint 2.18.1" أو "تذكر parallel BFS" | Parallel `_expand_extent` upfront-prefetch via `ThreadPoolExecutor(max_workers=min(N,20))`. Deployed Heroku v100 (2026-05-23 evening, CHANGELOG_v45). −60s on compound_small (51/835/17 89s→28.9s); HTTP 503×3 → 200×3 (THE WIN). §5 mini-audit corrected the original audit's "5-8s" prediction to honest 22-27s (off by ~3x). Latency goal delivered, but post-deploy visual verification unmasked methodology bug → Sprint 2.18.1.1. |
| "تذكر Sprint 2.18.1.1" أو "تذكر compound misroute" أو "تذكر Patches A+C" | Compound-misroute fix (Anas's verification discovered silent failure on 51/835/17: land=218M vs total=6.8M, building=−211M, pct=−3,107%). Patch A in qatar_gis.full_property_lookup: when classification.asset_type==COMPOUND_SMALL and extent.total_area_m2 >= 15000, promote both to COMPOUND_LARGE → routes via ASSET_TYPE_TO_MOJ_CATEGORY['compound_large']=None → valuation=None → clean Income Approach refusal. Patch C in _decompose_value: universal `if land_value > valuation_amount: return None` (catches premium-land villa teardowns + MoJ outliers too). Deployed Heroku v101 (2026-05-24 morning, CHANGELOG_v46). Anas visual verify 9/9. Threshold = E20. |
| "تذكر #52" أو "تذكر latency unmasks methodology" | Operational_Rules #52 — when a latency Sprint converts 5xx→2xx on a previously-unreachable path, the response *content* on that path is newly verifiable and may have latent bugs. Post-deploy verification scope must include the now-reachable response content, not just the latency metric. First documented case: Sprint 2.18.1 unmasked the compound_small >15K methodology bug; Sprint 2.18.1.1 closed it. |
| "تذكر E20" أو "تذكر 15K compound" | EMPIRICAL_FINDINGS E20 — MoJ "مجمع فلل" sampling max = **15,027 m²**. Compounds with extent ≥ 15K m² have no MoJ comparable; Income Approach with rent input is the only valid methodology. The 15K threshold drives Sprint 2.18.1.1 Patch A. |
| "تذكر #53" أو "تذكر closed cases stay closed" | Operational_Rules #53 — rules derived from a deferred/closed case remain in force, but the originating case itself is not cited as a foil, precedent, or comparison in new documentation. Cite §X (the rule), not the case that produced §X. Self-check: delete any sentence containing "unlike [closed case]" or "mirror [closed case] pattern". Crystallized 2026-05-24, pre-Sprint 2.21.1 MME smoke session. |
| "تذكر Sprint 2.21.2" أو "تذكر Hybrid Foundation" أو "تذكر hybrid_valuation_v1" | Sprint 2.21.2 (CHANGELOG_v47.md, Heroku v107, deployed 2026-05-24 evening). Foundation Sprint — Rule E3 expanded from "MUST NOT enter calculation" sentence to **8 numbered constraints** permitting tier-weighted listing entry via `hybrid_valuation_v1()`. New module `hybrid_valuation.py` exposes `HYBRID_TIER_CONFIG` (D5 T2 discount −12.5%, D6 T3 −17.5%, both provisional) + the function (Cases A/B/C/D + Constraint 7 unit-norm + Constraint 8 T3-alone refusal). Function exists, no engine path calls it yet — production behavior identical to 2.18.1.1. Connectors land in 2.21.3 (T2) + 2.21.4 (T3). 22 test functions / 67 sub-checks (H1+H2+H3+H4+H6 all TRUE); H5 verified 27/27 files pass. |
| "تذكر Rule E3 v2" أو "تذكر 8 constraints" | Rule E3 in `docs/Empirical_Findings.md` was rewritten 2026-05-24 by Sprint 2.21.2. Now 8 numbered constraints: (1) T2 cap 0.40 with T1 + D5 discount; (2) T3 cap 0.15 + D6 discount; (3) T1 floor 0.45 when present; (4) no T1 → indicative ceiling; (5) mandatory MUC ±20% when T1 absent; (6) source-level transparency (E10); (7) like-for-like unit normalization (RICS Red Book); (8) T3 alone insufficient. E1 (no MoJ uplift) preserved. |
| "تذكر Pre-Sprint 2.22.0" أو "تذكر H5 FALSE" | Pre-Sprint 2.22.0 audit (2p22p0_pre/CHANGELOG, 2026-05-24). Tested whether 3-stage UX architecture solves the apartments gap. H5 FALSE was decisive: 52/903/90 apartment_building returns HTTP 200 + valuation_amount=None + 4.7s — failure is data-driven, not latency-driven. 3-stage would just rename "insufficient data" across two stages. Sprint 2.22.0 deferred; BRIEF_2p21p2 (hybrid foundation) returned to top of queue and shipped. H1 TRUE + H3 TRUE + H4 TRUE evidence preserved for future UX-refactor Sprint after 2.21.5. |
| "تذكر Pre-Sprint 2.21.3" أو "تذكر DOM duplication" أو "تذكر arady /listings" | Pre-Sprint 2.21.3 smoke (2p21p3_pre/CHANGELOG, 2026-05-24 evening). 4 of 5 TRUE. arady canonical search URL = **`/listings`** (HTTP 200, 70 listing hits page 1, sitemap.xml available for full inventory). PropertyFinder reachable from Heroku (exact parity with sandbox; raw=142 = sandbox=142). H2 FALSE was a threshold artifact: PropertyFinder DOM duplicates listing nodes ~6× (142 raw → 24 unique on Lusail page 1). **Sprint 2.21.3 connector MUST deduplicate by canonical URL or listing ID.** Detail-page schema confirmed extractable: CSS class `property-price` + regex fallback for QAR/AED + regex for m²/sqm. |
| "تذكر D5/D6" أو "تذكر calibration provisional" | `HYBRID_TIER_CONFIG` ships with D5 T2 discount midpoint −12.5% (range −10%/−15%) and D6 T3 discount midpoint −17.5% (range −15%/−20%), both tagged `provisional, broker-experience-grounded`. Empirical basis: EMPIRICAL_FINDINGS §3 asking-premium ranges (+8% to +20% inverted). Recalibration has NO viable source (secretary feed closed 2026-05-24 + brokerage/Gardenia closed) → D5/D6 stay provisional **indefinitely**; not a blocker (ship with the MUC clause). Do NOT re-add closed-feed framing (no broker-supplied pipeline / no awaiting-secretary dependency). |
| "تذكر إغلاق Confirmed Sales" أو "تذكر no viable source" | BOTH internal sale-data feeds are closed: the secretary source (permanently, 2026-05-24) AND Anas's brokerage (Gardenia). Confirmed Sales DB (Sprint 2.16.16) therefore has **NO viable internal source** → deferred **indefinitely**; it is NOT a data source, dependency, or pillar. Do NOT re-add closed-feed framing (no broker-supplied pipeline; no awaiting-secretary dependency). Revive only if a genuinely PIN-keyed T1 sale source ever appears (none exists). T2 "broker" listings are ad-hoc only (not a stable feed); the Stage-4 field check is broker-agnostic (any vetted broker). |
| "راجع EMPIRICAL_FINDINGS" | Audit rules E1-E23 |
| "اقرأ القسم X" | Activate self-correction trigger from section X |
| "ركذت قاعدة الدفع" أو "تذكر #32" | Push & Commit discipline — Operational_Rules #32 |
| "هل أدفع؟" أو "should I push?" | يُفعّل #32 checklist، أعطِ status report قبل الإجابة |

-----

## Deployment workflow (Windows cmd)

```
cd /d "C:\Thammen\deploy v2"
git add <files>
git commit -m "<Sprint X.Y.Z>: <description>"
git subtree push --prefix "deploy v2" heroku master
git push origin master
```
> **Rule #43:** the app lives in the `deploy v2/` subdir → deploy is `git subtree push --prefix "deploy v2" heroku master`, **not** plain `git push heroku master` (rejected by the buildpack — no `requirements.txt` at slug root). `git push origin master` keeps the GitHub backup current (R1 deploy ritual).

### Post-deploy verification

```
curl -s -X POST https://thammen.qa/api/evaluate ^
  -H "Content-Type: application/json" ^
  -d "{\"zone\":61,\"street\":875,\"building\":20}" > out.json
findstr /C:"<expected_field>" out.json
```

-----

## File structure conventions

```
C:\Thammen\deploy v2\
├── CLAUDE.md                     ← this file
├── docs/                         ← Project Knowledge (read first)
│   ├── Project_Instructions.md
│   ├── Session_Log.md
│   ├── Empirical_Findings.md
│   ├── Custom_Instructions.md
│   ├── Session_Update_2026-05-19.md
│   └── Operational_Rules.md
├── api.py
├── evaluate_unified.py           ← main engine, ENGINE_VERSION at top
├── qatar_gis.py                  ← classifier (Sprint 2.16.6/14)
├── index.html                    ← frontend (RTL, Tajawal)
├── *.bak_<sprint>                ← backups before each Sprint
├── CHANGELOG_v<N>.md             ← one per Sprint
├── test_sprint_<X>_<Y>.py        ← isolated tests per Sprint
└── moj_weekly.csv                ← MoJ data
```

-----

## Audience calibration (§16)

|Audience|English code labels?|Methodology jargon?|Open decisions?|
|---|---|---|---|
|Anas (engineer)|yes|yes|yes|
|Manager|no|light|yes|
|Secretary|**never**|**never**|**never**|

-----

## Final notes

- Reply in Arabic unless code or technical detail makes English clearer
- Be direct about uncertainty and tradeoffs
- Prefer surgical fixes (2-10 lines) over rewrites
- When user says "افعل الأصوب" — exercise engineering judgment, don't ask
- When user challenges a result, re-examine evidence before defending
- Document failed paths as clearly as successful ones (e.g., Mthamen §20.8)

> Reading this file means you've inherited the work of 15+ Sprints, 2 major
> decisions, and a 22-landmark audit. Honor the methodology. Don't reinvent.
